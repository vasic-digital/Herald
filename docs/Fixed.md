<div align="center">

<img src="../assets/logo/herald_logo_square_128.png" alt="Herald" width="96" height="96" />

</div>

# Herald — Fixed

| Field | Value |
|---|---|
| Revision | 19 |
| Created | 2026-05-20 |
| Last modified | 2026-05-30 |
| Status | active |
| Status summary | **r19 (v1.0.0 closure-migration) atomically lands (§11.4.19) 51 code-verified HRDs Issues→Fixed after a re-verification audit (grep/read the impl + run the test + confirm the matching e2e invariant + confirm the `docs/qa/<run-id>/` dir; nothing migrated on self-reported status alone). The 51:** the 7 flavor constitution bindings HRD-019..025 (`<flavor>/internal/bindings` Pipelines; 7/7 bindings test suites PASS `-race`; e2e E90-E96; per-HRD `docs/qa/`); the entire §43 command catalogue HRD-029..056 (30 commands wired into Cobra roots via `registerGitOps`/`registerSysOps`/etc., cmd-test suites PASS, e2e E97-E122, per-HRD `docs/qa/`); the Batch-D `commons_infra.pgxTaskRepository` ops HRD-085..089 (all 15 methods real impls not `ErrUnsupported`, migration `000013_task_resource_snapshots`, e2e E123, `docs/qa/HRD-085-089-*`); Wave 7 HRD-116/118/119/120/121 (qaherald Slack `MessengerClient` + e2e E124-E131 + paired §1.1 gate `tests/test_wave7_mutation_meta.sh` + multi-channel §11.4.85 stress/chaos + `MESSENGER_CHANNELS.md` guide; all hermetic tests PASS); ATMOSphere features HRD-148/149/151/152/153/154 (`commons_workable`+`commons_watch` build+test PASS, change→CloudEvent→Runner bridge + inbound action registry + `pherald watch` daemon + quiet-hours resolver, e2e E139 `TestRunWatch_EndToEndOutbound` PASS hermetic). **NOT migrated (evidence-gated, left in Issues.md):** HRD-115 (Slack channel adapter — operator-gated on LIVE `docs/qa/HRD-115-LIVE-*/` round-trip evidence per §107.x; only hermetic transcript exists, E127 LIVE-Slack SKIP-with-reason — closing now would be a §107 PASS-bluff); HRD-117 (spec V3 doc-only — §106); HRD-131/150/155/156/157/158 (externally gated); HRD-146/147/081/008/015 (not-started/upstream/operator). Prior r18 atomically lands (§11.4.19) HRD-090 dead-letter handling Completed→Fixed — `commons_infra.pgxTaskRepository.MoveToDeadLetter` (was the last Batch-D `ErrUnsupported` stub): migration `000014_dead_letter_tasks` + tx-atomic JSONB-snapshot move + terminal-mark; new 13th OPERATIONAL constitution event class `ClassQueueDeadLetter` + `QueueEvent` + `DeadLetter()` emit (wired nil-tolerantly; governance §42.2 set unchanged). Four-layer closure: 3 unit + real-PG integration (3 sinks) + §11.4.85 stress/chaos (50 concurrent race-clean, atomic under fault) + paired §1.1 mutation gate (4/0) + e2e E138; commits `549261d`+`9c765f6`; evidence `docs/qa/HRD-090-20260529T052228Z/` + `docs/qa/HRD-090-stress-chaos-20260529T054500Z/`. Governance-plane subscriber follow-up = HRD-147 (Issues.md).** Prior r17 atomically lands (§11.4.19) the completed Wave 8 Track B (MTProto user-account harness for §11.4.98 full-automation testing) — HRD-139 (MTProto Client live wiring + qaherald mtproto CLI), HRD-140 (Subscribe roundtrip), HRD-141 (Wave 6 closed loop), HRD-142 (Wave 6.5 lifecycle) all New→Fixed (these HRDs were tracked directly in the TaskList rather than Issues.md and graduate directly into Fixed.md per §11.4.19 the moment closure landed).** Wave 8 Track B closure proves §11.4.98 (full-automation anti-bluff mandate) end-to-end: 3 MTProto-driven live tests (E135 Subscribe / E136 Wave 6 closed loop / E137 Wave 6.5 lifecycle) all PASSING LIVE under §11.4.98-pure interpretation, captured under `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/`. Three legacy NON-COMPLIANT tests graduate to §11.4.90 Obsolete in Issues.md r36 with Obsolete-Details cite of §11.4.98 + §108.m superseding-mandate: `TestSubscribe_LiveBotAPI`, `tests/test_wave6_live_loop.sh`, lifecycle `--manual` mode. v0.6.0 tag (`844f340` annotated) was already cut on `d843ddf` covering Wave 8 Track B closure + the §11.4.98+§11.4.99 anchoring + 9-Helix-submodule §11.4.98+§11.4.99 propagation. Prior r16 atomically migrates (§11.4.19) the completed Tgram-completeness wave — HRD-133/134/135/136/137/138 (all 6 Tgram-completeness HRDs) Issues→Fixed.** Wave A: HRD-135 lifecycle handlers (`fe2a7c8` — OnEdited / OnMyChatMember / serviceHandler / ErrBotKicked sentinel) + HRD-136 webhook secret_token validator (`6d7d543` — `crypto/subtle.ConstantTimeCompare`, 401-no-leak posture) + HRD-138 operator deep-dive guide (`ff9b9c0` — 810-line `docs/guides/TELEGRAM.md`, 11 §-sections, 3 siblings). Wave B: HRD-133 token-redaction symmetry (`8162d14` — `sanitizeTgramError` wraps 6 error-return sites in Send/SendReply) + HRD-134 429 retry_after honored (`8162d14` combined — `withRetry429` wraps all 3 telebot `bot.Send` sites, telebot.FloodError typed extraction, ctx-aware sleep, 3-attempt 60s-clamp). Wave C: HRD-137 stress+chaos suite (this commit — `TestTgram_Stress_MultiRecipientFanOut` 20×20=400 calls/20 chats deterministic + `TestTgram_Chaos_GetUpdatesPollerResilience` SKIP→PASS conversion via one-line `subscribe.go` baseURL seam fix; 8-phase fault matrix deterministic across `-race -count=3` — every iteration IDENTICAL `recovery=6 fault(500=12 hang=8 close=3 ok=6) goroutines 3→4`). Full tgram suite **26/26 PASS, 0 SKIP** post-Wave-C. All 6 HRDs carry `docs/qa/HRD-NNN-20260528T*Z/transcript.txt` evidence per §107.x. Prior r15 atomically migrated (§11.4.19) the completed GAP-3 §11.4.85 stress+chaos suite — HRD-122..HRD-130 (Task→Completed) — and the HRD-132 Runner idempotency-window fix (Bug→Fixed): 10 rows Issues→Fixed.** GAP-3 commits `02d272d`→`d2a60d3` (HRD-122 `commons/stresschaos/` scaffold, HRD-123 Gin `/v1/events`, HRD-124 `/v1/compliance`+`/v1/safety_state`, HRD-125 Runner — surfaced HRD-132, HRD-126 `pherald listen` inbound, HRD-127 claude_code dispatch, HRD-128 container/resource §12.6-safe, HRD-129 e2e E81-E88, HRD-130 paired §1.1 mutation gate); HRD-132 fix `354b883` (claim-before-dispatch at Runner Stage 2 — `INSERT events_processed … ON CONFLICT DO NOTHING` is the authoritative exactly-once dispatch gate; latent `runner.go:132` `CachedRcpt.WasReplay` race closed; HRD-125 assertion upgraded to `sends==1`) + wave3-M4-retire `4f45f26`. Full `scripts/e2e_bluff_hunt.sh` 78 PASS / 0 FAIL; HRD-130 gate 6 PASS / 0 FAIL (5 load-bearing mutations); HRD-132 evidence `docs/qa/HRD-125-stress-chaos-2026-05-27T131251-gap3/` (`dispatch_exactly_once=1`). HRD-131 (SQLite SSoT migration Phase 1) remains OPEN. Prior r14 captured Wave 6 (pherald inbound runtime + Claude Code headless bridge — code-doc closure). HRD-100 closed atomically with 12 Wave-6 commit SHAs + 8 new e2e invariants E63-E70 (currently SKIP-with-documented-reason — converting to PASS once operator runs the live closed-loop at T10b and commits `docs/qa/HRD-100/`). Tag v0.4.0 DEFERRED to T13b (operator-supplied live evidence per §107.x — code-doc closure ships in this commit; runtime evidence lands separately).** Wave 6 surfaces: new Cobra subcommand `pherald listen` (Telegram `getUpdates` long-poll + `OnText`/`OnPhoto`/`OnDocument`/`OnVoice` + bot self-filter + sha256 content-addressed attachment download); Opus model pin in spawned `claude` argv (`--model claude-opus-4-7`); envelope pre-text — verbatim operator wording "We have received new message from our communication channel <channel-name>. ..." preceding the `<<<HERALD-DISPATCH-v1>>>` structured block; `<<<HERALD-REPLY>>>` parser with `action` routing (`reply` / `issue.open` / `event.emit`); `tgram.Adapter.SendReply` wiring `reply_to_message_id`; `--qa-out-dir` JSONL journaling per §107.x. Wave 6 mutation gate (`tests/test_wave6_mutation_meta.sh`) lands 3 paired hermetic mutations (M1 blank bot self-filter → echo-loop signature; M2 Opus → Sonnet model swap → E67 FAIL; M3 drop `opts.ReplyTo` in SendReply → E68 FAIL). Prior r13 captured Wave 4b (TOON `application/toon` content negotiation; tag v0.3.0). r12 captured Wave 4a (HTTP/3 + Brotli + Alt-Svc + TLS 1.3; tag v0.2.0). r11 closed HRD-016 (Wave 3b pherald Runner live). r10 closed HRD-011 (Telegram live). |
| Issues | see `Issues.md` — r43 (Issues.md) captures this r19 §11.4.19 atomic migration of 51 code-verified HRDs (flavor bindings HRD-019..025, §43 catalogue HRD-029..056, Batch-D pgx HRD-085..089, Wave 7 HRD-116/118/119/120/121, ATMOSphere features HRD-148/149/151/152/153/154); 17 items remain open in Issues.md. |
| Issues summary | 17 still open in `Issues.md`: HRD-008, HRD-015, HRD-081 (operator/upstream); HRD-115 (Slack adapter — operator-gated on LIVE round-trip evidence per §107.x); HRD-117 (spec doc-only); HRD-131, HRD-150, HRD-155, HRD-156, HRD-157, HRD-158 (ATMOSphere/SSoT externally gated); HRD-146, HRD-147 (dispatch-hardening / governance-subscriber follow-ups); HRD-143/144/145 (Obsolete). |
| Fixed | HRD-001..HRD-007, HRD-009, HRD-009b, HRD-010, HRD-011, HRD-012, HRD-013, HRD-014, HRD-016, HRD-017, HRD-018, HRD-019..HRD-025, HRD-026, HRD-027, HRD-028, HRD-029..HRD-056, HRD-080, HRD-085..HRD-089, HRD-090, HRD-092, HRD-093, HRD-094, HRD-095, HRD-096, HRD-097, HRD-098, HRD-099, HRD-100, HRD-110, HRD-111, HRD-112, HRD-113, HRD-114, HRD-116, HRD-118, HRD-119, HRD-120, HRD-121, HRD-122, HRD-123, HRD-124, HRD-125, HRD-126, HRD-127, HRD-128, HRD-129, HRD-130, HRD-132, HRD-133, HRD-134, HRD-135, HRD-136, HRD-137, HRD-138, HRD-139, HRD-140, HRD-141, HRD-142, HRD-148, HRD-149, HRD-151, HRD-152, HRD-153, HRD-154 |
| Fixed summary | v1.0.0 closure-migration (r19 — 51 HRDs: flavor constitution bindings HRD-019..025, §43 command catalogue HRD-029..056, Batch-D pgxTaskRepository ops HRD-085..089, Wave 7 multi-channel HRD-116/118/119/120/121, ATMOSphere workable-items features HRD-148/149/151/152/153/154 — each re-verified against committed code + passing tests + e2e invariant + docs/qa before migration). Wave 8 Track B (HRD-139/140/141/142 — MTProto user-account harness proves §11.4.98 full-automation end-to-end with 3 live MTProto-driven tests PASSING LIVE 2026-05-28; v0.6.0 tag covers the closure); Tgram-completeness wave (HRD-133/134/135/136/137/138 — 6 HRDs across 3 sub-waves A/B/C, full tgram suite 26/26 PASS 0 SKIP); GAP-3 §11.4.85 stress+chaos suite (HRD-122..HRD-130 Completed) + HRD-132 Runner idempotency-window fix (claim-before-dispatch, exactly-once dispatch `sends==1`); spec V1→V3 r9; Go module foundation + Foundation M1/M2/M3; HRD-010 commons_storage live wiring; HRD-011 Telegram live (live `message_id=5` evidence 2026-05-22); HRD-012 Claude Code dispatcher live; HRD-016 pherald `/v1/events` Runner wiring (Wave 3b live — 7-stage pipeline + e2e E37-E42); universal §11.4.73 + §11.4.74 mandates propagated; I6 gate refined; Wave 2 flavor scaffolds (HRD-092..097) closed atomically; Wave 3a substrate (HRD-099 commons_auth JWT verifier) + 2 live REST routes (HRD-028, HRD-098) closed atomically. |
| Continuation | see `CONTINUATION.md`. |

## Table of contents

- [Recently fixed](#recently-fixed)

## Recently fixed

| ID | Type | Criticality | Title | Closed | Commit | Reference |
|---|---|---|---|---|---|---|
| HRD-019 | task | high | `cherald` constitution bindings — `cherald/internal/bindings` Pipeline registers the 32 cherald-owned §42.3 rules as concrete `commons_constitution.Evaluator` impls routed through 5 event classes (`.policy.violation`/`.gate.failed`/`.gate.recovered`/`.credential.leak`/`.spec.revision_drift`/`.catalogue.miss`) over the Batch-A emit+persist+audit foundation; `POST /v1/compliance/evaluate` write surface drives detect→emit→persist→audit, visible on the existing `GET /v1/compliance` pull surface. Vertical-slice bespoke detectors landed (§11.4.29 naming, §11.4.10/.10.A credentials, §11.4.44 doc-revision-header, §11.4.18 script-docs, §11.4.73 spec-drift, §11.4.74 catalogue-miss); remaining 25 rules use the shared marker-check mechanism (verdict decided upstream by the §43 catalogue command / sweep) with the full emit-routing framework in place — deepening each remaining bespoke detector is per-rule follow-up. | 2026-05-30 | `f33b5e4` (v1.0.0 Batch B) | spec V3 §42.3 cherald rows + §42.5 step 2; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A wiring per §11.4.74). Evidence: docs/qa/HRD-019-20260527T200036Z/ (REST round-trip transcript + stress_chaos/). e2e: E90 proposed. |
| HRD-020 | task | high | `sherald` host-safety + repo-safety constitution bindings — `sherald/internal/bindings` Pipeline registers the 15 sherald-owned §42.3 host/repo-safety rules (§2, §9.1/.2/.3, §11.4.24/.26/.32/.36/.41/.47/.71, §12.1/.2/.3/.6) as concrete `commons_constitution.Evaluator` impls routed through 6 SAFETY event classes (`.host.safety.breach`/`.repo.safety.breach`/`.gate.recovered`/`.gate.failed`/`.bundle.updated`/`.policy.violation`) over the Batch-A emit+persist+audit foundation, mirroring the proven HRD-019 cherald Pipeline pattern. The 3 named DETECTION hooks landed as REAL pure classifiers — destructive-op detector (§9.1: rm/git-reset/clean without backup → `.repo.safety.breach`), force-push interceptor (§11.4.41/§9.2: force-push without merge-first OR session-auth → `.repo.safety.breach`), mem-budget watcher (§12.6: reported used_fraction > 60% → `.host.safety.breach`) — plus forbidden-host-op (§12.1) + prereq-marker detectors for the remaining rows. CRITICAL §12-safety: every detector DETECTS+REPORTS from a Subject string description; NONE performs the destructive op, force-pushes, suspends the host, or allocates memory (the mem-budget watcher reads a fabricated fraction — no real exhaustion). The §43 command bodies (HRD-033/046/056) supply the live op-interception feeding these detectors. | 2026-05-30 | `5ebad4f` (v1.0.0 Batch B) | spec V3 §42.3 sherald rows + §42.5 step 3; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A wiring + reuses the existing `commons_constitution.SafetyEvent`/`HostSafetyBreach`/`RepoSafetyBreach` emitter surface per §11.4.74). Evidence: docs/qa/HRD-020-20260527T201932Z/ (detect→emit→persist→audit round-trip transcript for §9.1/§11.4.41/§12.6/§12.1 + §11.4.85 stress_chaos/). §11.4.92 5-pass — Pass-1: TDD RED (no-impl build-fail) → GREEN, `go test -race -count=3 ./sherald/internal/bindings/...` deterministic 15/15 PASS ×3. Pass-2 blast-radius: new pkg `sherald/internal/bindings` only; sherald/go.mod gains commons_constitution + transitive replace block; existing sherald main/routes/safety untouched (Pipeline not yet wired into serve — REST `/v1/safety/*` surface is follow-up). Pass-3 cross-feature: independent of the existing in-memory safety.Aggregator + GET /v1/safety_state (Wave 3b); no shared state; the §32 Runner pipeline is unaffected (sherald owns a separate Pipeline like cherald). Pass-5 anti-bluff: real MemoryBus+store+ladder+audit round-trip with captured emitted wire-event payloads (NOT metadata-only); §12-safe — TestDetectors_NeverPerformOps + structural witness confirm no real destructive op performed. e2e: E91 proposed (conductor to wire — asserts a sherald §9.1 destructive-op Subject → `.repo.safety.breach` detected+persisted+audited via the bindings Pipeline). NOTE: surfaced a latent race in `commons_constitution/eventbus.go` Publish PublishedByType sync.Map check-then-act (under-counts by 1 under load) — out of this unit's edit scope, flagged for conductor. |
| HRD-021 | task | middle | `bherald` CI/test bindings — `bherald/internal/bindings` Pipeline registers the 22 bherald-owned §42.3 CI/test gate-result rules (§1, §1.1, §11.4.2/.3/.4/.5/.7/.9/.13/.14/.24/.27/.30/.39/.43/.46/.48–.52/.67) as concrete `commons_constitution.Evaluator` impls routed through 3 event classes (`.gate.failed`/`.gate.recovered` via the build/test gate-result class + `.policy.violation` + `.repo.safety.breach` for the shared §11.4.30 build-artifact row) over the Batch-A emit+persist+audit foundation, mirroring the proven HRD-019/020 Pipeline pattern. bherald is the BUILD/CI flavor; it emits CI/test gate-result constitution events that integrate with the consuming project's CI workflow. 3 bespoke pure detectors landed: gate-result classification (§1/§11.4.50 — PASS/FAIL/flaky from a recorded gate outcome → `.gate.failed`/`.gate.recovered`), test-tier-verify (§11.4.27/§40.2 — the 8-tier test matrix; a missing tier FAILs), anti-bluff-PASS detection (§11.4.2/.5 — a gate that PASSes WITHOUT a captured-evidence artefact is a §11.4 PASS-bluff → FAIL). Remaining rows use the shared marker-check mechanism (verdict decided upstream by the §43 command/CI gate) with the full emit-routing framework in place. | 2026-05-30 | `3fb753a` (v1.0.0 Batch B) | spec V3 §42.3 bherald rows + §42.5 step 4; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A `commons_constitution` wiring + reuses the existing `GateEvent`/`GateFailed`/`GateRecovered` emitter surface per §11.4.74). v1.0.0 Batch B unit 3. Evidence: docs/qa/HRD-021-<run-id>/ (gate-fail→detect→emit `.gate.failed`→persist→audit→query round-trip transcript + §11.4.85 stress_chaos/). §11.4.92 5-pass — Pass-1: TDD RED→GREEN, `go test -race -count=3 ./bherald/internal/bindings/...` deterministic PASS ×3. Pass-2 blast-radius: new pkg `bherald/internal/bindings` only; bherald/go.mod gains commons_constitution + transitive replace block; existing bherald main/stubs untouched. Pass-3 cross-feature: mirrors HRD-019/020 emit-class routing; no shared state with cherald/sherald (each flavor owns a separate Pipeline). Pass-5 anti-bluff: real MemoryBus+store+ladder+audit round-trip with captured emitted wire-event payloads (NOT metadata-only). e2e: E92 proposed (conductor to wire — asserts a bherald gate-result FAIL Subject → `.gate.failed` detected+persisted+audited via the bindings Pipeline). |
| HRD-022 | task | middle | `rherald` release constitution bindings — `rherald/internal/bindings` Pipeline registers the 4 rherald-owned §42.3 release-lifecycle rules (§4 tag mirroring, §5 changelog + multi-format export [shared with cherald], §11.4.38 installable-asset evidence, §11.4.40 full-suite retest before release tag) as concrete `commons_constitution.Evaluator` impls routed through 2 event classes (`.release.gate.blocked` via the existing `ReleaseEvent`/`ReleaseGateBlocked` emitter surface for §4/§11.4.38/§11.4.40 + `.policy.violation` for the shared §5 changelog row; a release recovery maps to the shared `.gate.recovered` companion since the release class family has no distinct "recovered" leaf) over the Batch-A emit+persist+audit foundation, mirroring the proven HRD-019/020/021 Pipeline pattern. rherald is the RELEASE flavor. 4 bespoke PURE detectors landed: tag-mirror-parity (§4 — tag present on parent but missing on any owned mirror, `with_tag<mirrors`, → `.release.gate.blocked`), changelog-conformance (§5 — non-Conventional-Commits changelog OR stale multi-format export → `.policy.violation`), pre-tag-retest-gate (§11.4.40 CRITICAL — tag attempted without a green all-8-§40.2-tier retest → `.release.gate.blocked`), installable-asset-evidence (§11.4.38 — release asset fails its install check → `.release.gate.blocked`). CRITICAL §12-safety: every detector CLASSIFIES a recorded release-op outcome string; NONE tags, pushes, force-pushes, runs git, downloads, or installs — the live release-op interception is scope-locked to the §43 follow-ups (HRD-031/032/045). | 2026-05-30 | `3fb753a` (v1.0.0 Batch B) | spec V3 §42.3 rherald rows + §42.5 step 5; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A `commons_constitution` wiring + reuses the existing `ReleaseEvent`/`ReleaseGateBlocked` emitter surface per §11.4.74). v1.0.0 Batch B unit 4. Evidence: docs/qa/HRD-022-20260528T015202Z/ (release-gate-blocked detect→emit `.release.gate.blocked`→persist→audit→query round-trip transcript for §4/§5/§11.4.38/§11.4.40 + §11.4.85 stress_chaos/ — 1200 concurrent release-blocks 0 errors, 1200 emits + 1200 audit rows, bus-drop chaos 800/800 faults surface 0 swallowed). §11.4.92 5-pass — Pass-1: TDD RED (injected always-pass bluff into §11.4.40 retest detector → 4 sub-cases FAIL) → reverted → GREEN, `go test -race -count=3 ./rherald/internal/bindings/...` deterministic 14/14 PASS ×3. Pass-2 blast-radius: new pkg `rherald/internal/bindings` only; rherald/go.mod gains commons_constitution + google/uuid + transitive replace block (database submodule); existing rherald main/stubs untouched (Pipeline not yet wired into a serve plane — rherald is CLI-only, DefaultPort=0; the §43 `rherald tag mirror`/`changelog generate`/`gate retest` command bodies are HRD-031/032/045 follow-ups). Pass-3 cross-feature: mirrors HRD-019/020/021 emit-class routing; no shared state with cherald/sherald/bherald (each flavor owns a separate Pipeline); the shared §5 row is registered independently in both rherald (conformance/export-staleness facet) and cherald packages. Pass-5 anti-bluff: real MemoryBus+store+ladder+audit round-trip with captured emitted wire-event payloads (NOT metadata-only); §12-safe — no real tag/push/git/install performed. e2e: E93 proposed (conductor to wire — asserts a rherald §11.4.40 tag-without-retest Subject → `.release.gate.blocked` detected+persisted+audited via the bindings Pipeline). |
| HRD-023 | task | middle | `pherald` project bindings (§2 commit+push, §3 submodule propagation, §11.4.11/.15/.21/.22/.34/.37/.42/.55/.66/.71/.74) — Batch B: `pherald/internal/bindings/` Pipeline + §42.3-owned rule set + 5 bespoke PURE detectors (commit-push-discipline / submodule-propagation-order / pre-push-fetch-guard / fetch-before-edit) wired through the commons_constitution Evaluator+audit foundation; -race -count=3 green, e2e E94. Live op-interception deferred to §43 HRD-029/030/043/044/049/053. | 2026-05-30 | `3fb753a` (v1.0.0 Batch B) | spec V3 §42.3 pherald rows + §42.5 step 6; evidence `docs/qa/HRD-023-20260527T210354Z/`; Catalogue-Check: reuse commons_constitution (Batch-A) |
| HRD-024 | task | middle | `iherald` constitution-rule escalation bindings (§11.4.10/.10.A credential-leak page-out, §11.4.21 + §11.4.66 operator-blocked escalation) — Batch B: `iherald/internal/bindings/` Pipeline routing `.credential.leak` (forced-critical) + `.policy.violation` + 5 bespoke detectors wired through commons_constitution Evaluator+audit; -race -count=3 green, e2e E95, no-real-secret attestation. Live paging deferred to §43 / HRD-024-paging. | 2026-05-30 | `3fb753a` (v1.0.0 Batch B) | spec V3 §42.3 iherald rows + §42.5 step 7; evidence `docs/qa/HRD-024-20260527T210714Z/`; Catalogue-Check: reuse commons_constitution (Batch-A) |
| HRD-025 | task | low | `scherald` scheduled-audit bindings (§11.4.45 periodic Status.md sweep + daily/weekly/monthly compliance digest) — Batch B: `scherald/internal/bindings/` Pipeline routing `.policy.violation` + 3 bespoke detectors (status-sweep / digest-cadence / stale-item) wired through commons_constitution Evaluator+audit; -race -count=3 green, e2e E96. Live cron/scheduler deferred to §43 HRD-047. | 2026-05-30 | `3fb753a` (v1.0.0 Batch B) | spec V3 §42.3 scherald rows + §42.5 step 8; evidence `docs/qa/HRD-025-20260528-hrd025-batchb-u7/`; Catalogue-Check: reuse commons_constitution (Batch-A) |
| HRD-029 | task | middle | §2 `pherald commit-push` — single-entrypoint locked commit + multi-mirror push — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-30 | `502ebf6` (Batch C C1) | spec §43.2 / §2; Catalogue-Check: TBD (likely existing as constitution-submodule script); evidence `docs/qa/HRD-029-20260527T213201Z/` |
| HRD-030 | task | middle | §3 `pherald submodule propagate` — owned-submodule walk in propagation order — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-30 | `502ebf6` (Batch C C1) | spec §43.2 / §3; Catalogue-Check: TBD; evidence `docs/qa/HRD-030-20260527T213201Z/` |
| HRD-031 | task | middle | §4 `rherald tag mirror` — assert tag exists on every owned submodule — Batch C: implemented as rherald §43 release command (`rherald/cmd/rherald/release_cmds.go`) feeding the HRD-022 rherald binding; reuses shared `commons/gitops`; hermetic -race green | 2026-05-30 | `130ec19` (Batch C C4) | spec §43.2 / §4; Catalogue-Check: TBD; docs/qa/HRD-031-20260528T040000Z/ |
| HRD-032 | task | low | §5 `rherald changelog generate` — Conventional Commits → `docs/changelogs/<v>.md` + multi-format export — Batch C: implemented as rherald §43 release command (`rherald/cmd/rherald/release_cmds.go`) feeding the HRD-022 rherald binding; reuses shared `commons/gitops`; hermetic -race green | 2026-05-30 | `130ec19` (Batch C C4) | spec §43.2 / §5 / §36; Catalogue-Check: TBD; docs/qa/HRD-032-20260528T040000Z/ |
| HRD-033 | task | high | §9.1 `sherald destructive guard <op>` — wrap rm/git-reset/git-push-force with prerequisite checks — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-30 | `ed6c675` (Batch C C2) | spec §43.2 / §9.1; Catalogue-Check: TBD; docs/qa/HRD-033-20260528T030000Z/ |
| HRD-034 | task | middle | §9.3 `sherald backup-snapshot <target>` — hardlinked snapshot helper — Batch C straggler: implemented in `sherald/cmd/sherald/sysops_cmds.go` (os.Link hardlinks, refuses overwrite, classifies §9.3 SubjectBackup → `.gate.recovered`); hermetic inode-equality -race green | 2026-05-30 | `ba27a0e` (Batch C straggler) | spec §43.2 / §9.3; Catalogue-Check: TBD; evidence `docs/qa/HRD-034-20260528T060000Z/` |
| HRD-035 | task | middle | §11.4.2 `bherald evidence capture <test_id>` — captured-evidence recorder — Batch C: implemented as bherald §43 build/CI command (`bherald/cmd/bherald/build_cmds.go`) feeding the HRD-021 bherald binding; hermetic -race green | 2026-05-30 | `130ec19` (Batch C C5) | spec §43.2 / §11.4.2 + §11.4.5; Catalogue-Check: TBD; docs/qa/HRD-035-20260528T040000Z/ |
| HRD-036 | task | high | §11.4.10 `cherald creds scan` — gitleaks/trufflehog integration, emits `.credential.leak` — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green (routed to §11.4.10 credential-leak; findings REDACTED — no secret value printed) | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.10; Catalogue-Check: TBD — likely an existing OSS scanner; docs/qa/HRD-036-20260528T050000Z/ |
| HRD-037 | task | low | §11.4.12 `cherald docs sync` — regen Issues_Summary / Fixed_Summary / Status_Summary — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.12; Catalogue-Check: TBD (likely existing constitution-submodule script); docs/qa/HRD-037-20260528T050000Z/ |
| HRD-038 | task | low | §11.4.18 `cherald script-docs check` — assert sibling .md for every scripts/**/*.sh — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green (routed to §11.4.60 — §11.4.62 has no cherald rule) | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.18; Catalogue-Check: TBD; docs/qa/HRD-038-20260528T050000Z/ |
| HRD-039 | task | low | §11.4.19 / .23 `cherald fixed align` + `cherald colorize` — Issues/Fixed format + HTML colorizer — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.19 + §11.4.23; Catalogue-Check: TBD; docs/qa/HRD-039-20260528T050000Z/ |
| HRD-040 | task | high | §11.4.26 `sherald constitution pull` — wrap fetch + rebase + validation gate, emits `.bundle.updated` — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-30 | `ed6c675` (Batch C C2) | spec §43.2 / §11.4.26 + §11.4.32; Catalogue-Check: TBD; docs/qa/HRD-040-20260528T030000Z/ |
| HRD-041 | task | middle | §11.4.27 `bherald test-tier verify` — 8-tier matrix verification — Batch C: implemented as bherald §43 build/CI command (`bherald/cmd/bherald/build_cmds.go`) feeding the HRD-021 bherald binding; hermetic -race green | 2026-05-30 | `130ec19` (Batch C C5) | spec §43.2 / §11.4.27 + §40.2; Catalogue-Check: TBD; docs/qa/HRD-041-20260528T040000Z/ |
| HRD-042 | task | low | §11.4.31 `cherald submanifest verify` — Submodule-Dependency-Manifest gate — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.31; Catalogue-Check: TBD; docs/qa/HRD-042-20260528T050000Z/ |
| HRD-043 | task | high | §11.4.36 `pherald install-upstreams` — install_upstreams wrapper — Batch C: implemented as gitops command (§11.4.74 FindScript-resolved constitution script); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-30 | `502ebf6` (Batch C C1) | spec §43.2 / §11.4.36; Catalogue-Check: extend constitution submodule's install_upstreams.sh; evidence `docs/qa/HRD-043-20260527T213201Z/` |
| HRD-044 | task | middle | §11.4.37 `pherald fetch-guard` — pre-edit fetch + rebase enforcement — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-30 | `502ebf6` (Batch C C1) | spec §43.2 / §11.4.37; Catalogue-Check: TBD; evidence `docs/qa/HRD-044-20260527T213201Z/` |
| HRD-045 | task | high | §11.4.40 `rherald gate retest` — pre-tag full-suite retest gate — Batch C: implemented as rherald §43 release command (`rherald/cmd/rherald/release_cmds.go`) feeding the HRD-022 rherald binding; reuses shared `commons/gitops`; hermetic -race green | 2026-05-30 | `130ec19` (Batch C C4) | spec §43.2 / §11.4.40; Catalogue-Check: TBD; docs/qa/HRD-045-20260528T040000Z/ |
| HRD-046 | task | high | §11.4.41 `sherald force-push gate` — merge-first + per-session-auth enforcement — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-30 | `ed6c675` (Batch C C2) | spec §43.2 / §11.4.41 + §9.2; Catalogue-Check: TBD; docs/qa/HRD-046-20260528T030000Z/ |
| HRD-047 | task | middle | §11.4.45 / .56 `scherald status-digest` — periodic Status.md sweep + Status_Summary regen — Batch C straggler: implemented in `scherald/cmd/scherald/digest_cmds.go` feeding the HRD-025 scherald binding (§11.4.45 status-sweep; detect/report + `--apply` regenerates Status_Summary); hermetic -race green | 2026-05-30 | `ba27a0e` (Batch C straggler) | spec §43.2 / §11.4.45 + §11.4.56; Catalogue-Check: TBD; evidence `docs/qa/HRD-047-20260528T060000Z/` |
| HRD-048 | task | low | §11.4.53 `cherald fixed-summary sync` — standalone Fixed_Summary backfill — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.53; Catalogue-Check: TBD (likely existing script); docs/qa/HRD-048-20260528T050000Z/ |
| HRD-049 | task | middle | §11.4.55 `pherald reopen <HRD-NNN>` — Issues→Fixed reversal + Reopens history — Batch C: implemented as gitops command (the Wave 6.5 inbound Fixed→Issues migration); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-30 | `502ebf6` (Batch C C1) | spec §43.2 / §11.4.55; Catalogue-Check: no-match (Herald-specific HRD flow); evidence `docs/qa/HRD-049-20260527T213201Z/` |
| HRD-050 | task | middle | §11.4.59 `cherald readme sync` — README doc-link regen + multi-format re-export — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.57 + §11.4.59; Catalogue-Check: TBD; docs/qa/HRD-050-20260528T050000Z/ |
| HRD-051 | task | high | §11.4.60 `cherald composite-gate` — canonical implementation of CM-DOCS-COMPOSITE-SYNC — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.60; Catalogue-Check: TBD; docs/qa/HRD-051-20260528T050000Z/ |
| HRD-052 | task | middle | §11.4.65 `cherald export` — bulk Markdown export wrapper (md/html/pdf/docx) — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.65 + §36; Catalogue-Check: extend (built atop Pandoc + WeasyPrint); docs/qa/HRD-052-20260528T050000Z/ |
| HRD-053 | task | high | §11.4.71 `pherald pre-push` — fetch + investigate + integrate hook — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-30 | `502ebf6` (Batch C C1) | spec §43.2 / §11.4.71; Catalogue-Check: TBD; evidence `docs/qa/HRD-053-20260527T213201Z/` |
| HRD-054 | task | middle | §11.4.73 `cherald spec-version check` — Revision-vs-edits drift detection — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.73; Catalogue-Check: no-match (very-new mandate); docs/qa/HRD-054-20260528T050000Z/ |
| HRD-055 | task | middle | §11.4.74 `cherald catalogue-check <pr>` — scan PR for Catalogue-Check + survey runner over vasic-digital + HelixDevelopment — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-30 | `aef0207` (Batch C C3) | spec §43.2 / §11.4.74; Catalogue-Check: no-match (very-new mandate); docs/qa/HRD-055-20260528T050000Z/ |
| HRD-056 | task | high | §12.6 `sherald mem-budget watch` — daemon-mode 60% threshold watcher emitting `.host.safety_breach` — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-30 | `ed6c675` (Batch C C2) | spec §43.2 / §12.6; Catalogue-Check: TBD; docs/qa/HRD-056-20260528T030000Z/ |
| HRD-085 | task | middle | Implement single-task admin operations on `commons_infra.pgxTaskRepository` — `GetByID`, `Update`, `Delete`. Required for Requeue + MoveToDeadLetter call chains and operator tooling. Currently all three return `ErrUnsupported (HRD-085)`. Scope: read one BackgroundTask row by ID; full-row Update including JSONB columns; soft- or hard-Delete decision per §107. | 2026-05-30 | `5899623` (v1.0.0 Batch D) | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (Herald already implements Enqueue/Dequeue/LogEvent on the same interface; these three rows finish the single-task surface). |
| HRD-086 | task | middle | Implement worker-side running-task state mutations on `commons_infra.pgxTaskRepository` — `UpdateStatus`, `UpdateProgress`, `UpdateHeartbeat`, `SaveCheckpoint`. Required by River workers + the stuck-detector reaper. Currently all four return `ErrUnsupported (HRD-086)`. Scope: status transitions guarded by valid-state-machine check; progress as `(float64, message)`; heartbeat as `updated_at = now()` writeback; checkpoint as opaque `[]byte` blob persisted alongside the task row. | 2026-05-30 | `5899623` (v1.0.0 Batch D) | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (worker-side write surface — the hot path for long-running task progress reporting). |
| HRD-087 | task | low | Implement admin / stats / audit reads on `commons_infra.pgxTaskRepository` — `GetByStatus`, `GetPendingTasks`, `CountByStatus`, `GetTaskHistory`. Drives the `/v1/queue/*` introspection endpoints + Peek delegate. Currently all four return `ErrUnsupported (HRD-087)`. Scope: status-filtered paginated reads; pending-task slice for Peek; status-bucketed counts; per-task execution-history rows from the `task_execution_history` table. | 2026-05-30 | `5899623` (v1.0.0 Batch D) | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (read-only introspection — composes with HRD-016 `/v1/queue/*` Gin handlers). |
| HRD-088 | task | middle | Implement queue-recovery reads on `commons_infra.pgxTaskRepository` — `GetStaleTasks`, `GetByWorkerID`. Drives the stuck-task reaper + worker-crash recovery loop. Currently both return `ErrUnsupported (HRD-088)`. Scope: stale-threshold filter against `updated_at`; per-worker assignment lookup for re-dispatch after a worker disappears. | 2026-05-30 | `5899623` (v1.0.0 Batch D) | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (recovery-loop surface — required before any worker is allowed to crash without operator intervention). |
| HRD-089 | task | low | Implement resource-monitor I/O on `commons_infra.pgxTaskRepository` — `SaveResourceSnapshot`, `GetResourceSnapshots`. Persists per-task CPU/memory/IO samples for capacity-planning + post-mortem. Currently both return `ErrUnsupported (HRD-089)`. Scope: append-only insert keyed by `(task_id, captured_at)`; per-task paginated read in reverse-chronological order. Schema target: a `task_resource_snapshots` table not yet in the §9.6 migration set — adding it is part of this HRD. | 2026-05-30 | `5899623` (v1.0.0 Batch D) | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (resource telemetry — composes with `digital.vasic.observability` OTel pipeline but is durable, not stream-only). |
| HRD-116 | task | middle | Wave 7 T7 — qaherald Slack `MessengerClient` + channel-keyed builder (`qaherald/internal/messenger/{slack.go,builder.go}`): raw-HTTP client implementing all 11 `MessengerClient` methods (Me/Send/SendPhoto/SendDocument/SendVoice/GetUpdates/WaitForReply/Download/Preflight/GetChatMember/Close); `GetUpdates` via `conversations.history`+`oldest=<ts>` watermark (Slack has no `getUpdates`); `MessageID` = dotted-float ts string; channel-keyed `Build(BuildConfig)` (tgram/slack/unknown→error). Token NEVER in errors — `sanitizeError()` defense + `TestSlack_Send_ErrorDoesNotLeakToken` pins it with a planted `xoxb-SUPERSECRET-must-not-leak-7777`. 30 hermetic httptest tests `-race` green; compile-time pin `var _ MessengerClient = (*SlackClient)(nil)`. KNOWN-FUTURE-CLEANUP: ships `files.upload` V1 (deprecated by Slack 2025-Q1 in favor of two-step V2) — functionally equivalent for the httptest seam, swappable without API breakage; documented in `slack.go` header. | 2026-05-30 | `6811114` (Wave 7 T7) | docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T7; mirrors `qaherald/internal/messenger/telegram.go`; evidence `docs/qa/HRD-116-20260528T090000Z/`; Catalogue-Check: reuse Slack Web API endpoints via raw HTTP (NOT slack-go SDK — different layer from HRD-115; offset bookkeeping is qaherald-controlled; 2026-05-28). |
| HRD-118 | task | high | Wave 7 T9 — e2e_bluff_hunt.sh E124-E131 multi-channel invariants: E124 channels registry resolution, E125 tgram self-filter post-Wave-7 (pure-refactor invariant), E126 slack adapter (interface satisfied + auth.test + chat.postMessage wire bytes + registry wiring), **E127 LIVE Slack** (env-gated `HERALD_SLACK_BOT_TOKEN`+`HERALD_SLACK_CHANNEL_ID`+`TestSlack_Live_Send` triple-guard to prevent silent "no tests to run" PASS-bluff), E128 per-channel inbox isolation, E129 generalized IsSelfEcho (Username+UserID+empty-self), E130 multi-channel pherald listen wiring, E131 qaherald messenger.Build dispatch. sc_anchor on the T6+T7 committed transcripts. | 2026-05-30 | `17ecd0b` (Wave 7 T9) | spec V3 r13 §11.4; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T9; Catalogue-Check: n/a (e2e wiring against shipped tests). |
| HRD-119 | task | high | Wave 7 T10 — paired §1.1 mutation gate `tests/test_wave7_mutation_meta.sh` (4 PASS / 0 FAIL): **M1** blank `IsSelfEcho` → `TestIsSelfEchoUsername` FAILs (echo-loop hazard caught) → restored → PASS; **M2** drop slack `channels.Register` in `init()` → new `TestSlackRegisteredViaInit` FAILs → restored → PASS; **M3** drop slack `thread_ts` append in `send.go` → `TestSlackSendReplyUsesThreadTS` FAILs → restored → PASS; **+ quiescence** assertion (no `MUTATED W7-` residue in 3 production target files post-restore). Reuses Wave 6 canonical `.git/MUTATION_IN_PROGRESS` lockfile + trap-on-EXIT + pre-flight dirty-tree-and-residue guard + byte-for-byte `assert_restored`. FOREGROUND-ONLY (§107.y / no-concurrent-mutation operator mandate). | 2026-05-30 | `6737040` (Wave 7 T10) | spec V3 §11.4 + §1.1 + §107.y; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T10; canonical precedent `tests/test_wave6_mutation_meta.sh`; evidence `docs/qa/HRD-119-20260528T100000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-119-WAVE7-MUTATION-GATE-20260528`); Catalogue-Check: n/a (gate). |
| HRD-120 | task | high | Wave 7 T11 — §11.4.85 multi-channel stress + chaos. Extended HRD-126's `pherald/cmd/pherald/listen_stress_chaos_test.go` (NOT duplicated to a new file): `TestListen_MultiChannel_StressBurst` (2 channels × 200 concurrent events through ONE runListen — pins no-loss/no-double + **per-channel attribution** via `perChannelCountingReplier` keyed on `rcpt.Channel` proving the channelRouter does NOT cross-wire; throughput ~9-19k ev/s under -race; clean shutdown <3s + no goroutine leak) + `TestListen_MultiChannel_ChaosOneChannelDies_OtherKeepsGoing` (tgram fires 1 event then errors; slack ticks 50 events at 2ms — pins **concurrency-of-fan-in under fault** via conjunction: ≥5 slack dispatches landed BEFORE fail-loud teardown AND tgram error surfaces tagged `channel="tgram-stub"` + `subscribe`). 12/12 PASS over `-race -count=3` (3 iterations × 2 new + 2 sibling HRD-126); race-detector clean. runListen contract found: **fail-loud-tears-down-siblings** (per-channel supervised restart is a deferred Wave 8 design, doc-comment warns against silent change). Evidence anchors: `multichannel_no_drop_under_burst=1` (stress) + `multichannel_concurrency_proven=1` + `multichannel_one_channel_death_surfaced=1` (chaos). | 2026-05-30 | `86dfa1e` (Wave 7 T11) | spec V3 §11.4.85; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T11; canonical precedent HRD-126 + commons/stresschaos; evidence `docs/qa/HRD-120-20260528T110000Z/stress_chaos/transcript.txt` (anchor `QA-ANCHOR: HRD-120-WAVE7-MULTICHANNEL-STRESS-CHAOS-20260528`); Catalogue-Check: extend commons/stresschaos + HRD-126 burstSubscriber. |
| HRD-121 | task | low | Wave 7 T12 — `docs/guides/MESSENGER_CHANNELS.md` operator guide (610 lines, 11 §-sections + 3 siblings .html/.pdf/.docx): §1 architecture (commons.Channel outbound vs commons_messaging/channels.Channel inbound + registry + IsSelfEcho + per-channel inbox; ASCII fan-in diagram), §2 `HERALD_CHANNELS` selector (fail-loud on unknown name), §3 Telegram live setup (BotFather → token → chat-id), §4 Slack live setup (api.slack.com/apps → Socket Mode + scopes + `xoxb-`/`xapp-` tokens; §107 token-redaction sanitization note citing `TestSlack_Send_ErrorDoesNotLeakToken`), §5 inbox isolation under `${HERALD_INBOX_DIR}/<channel>/<sha256>.<ext>`, §6 anti-echo-loop self-filter (cross-the-wire-then-cache contract), §7 URL schemes for both adapters, §8 14-item "add a new channel" implementer checklist + companion "what you do NOT touch" list (framework hot paths come for free), §9 10-entry troubleshooting cookbook (echo-loop / channel_not_found / invalid_auth / wrong inbox / HERALD_CHANNELS=unknown), §10 6-checklist operator pre-deploy audit mirroring OPERATOR_CREDENTIALS.md style, §11 references (cross-links spec V3 §11/§32.2/§43 + Wave 7 plan + adapter source paths + every HRD-110..121). Generated via `REGEN_ALL=1 bash scripts/export_docs.sh` (new-guide bootstrap). | 2026-05-30 | `a541acc`/Wave 7 T12 | docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T12; evidence `docs/qa/HRD-121-20260528T120000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-121-WAVE7-T12-MESSENGER-CHANNELS-DOC-20260528`); Catalogue-Check: n/a (operator doc). |
| HRD-148 | feature | high | **ATMOSphere integration WS-1 — `commons_workable` SQLite SSoT foundation (LANDED + tested 2026-05-29).** New L1 module: SQLite store (pure-Go `modernc.org/sqlite`, no CGO) mirroring the workable-items schema v2 (`items` PK `(atm_id,current_location)` + `item_history` + `meta`, WAL, 10-value status closed set); full CRUD repo (Create/GetByID/Update/Delete/List, loud not-found, closed-set status validation); per-property `Diff(prev,curr)` change-feed emitting `item.created`/`item.deleted`/`item.status.changed`/`item.field.changed`/`item.content.updated` with exact old→new; `ParseTracker` matching ATMOSphere's REAL `## §… [ATM-NNN]` + `**Status:**`/`**Type:**`/`**Severity:**` format. 18 real-SQLite tests `-race -count=3` green. Keystone for R1/R3/R6. | 2026-05-30 | `92f0136` (WS-1) | Plan `~/Documents/ATMOSphere_Herald_Integration_Plan.md` §4 (HRD-W1/W2); Catalogue-Check: reuse `modernc.org/sqlite`, schema mirrors `constitution/scripts/workable-items/schema.sql` (2026-05-29). |
| HRD-149 | feature | high | **ATMOSphere integration WS-2 — `commons_watch` file/DB watcher (LANDED + tested 2026-05-29).** New L1 module: fsnotify watcher (direct dep) over the `.db` SSoT + MD trackers, debounce-coalesce + SQLite-WAL polling fallback (WAL writes hit the `-wal` sidecar + may not raise reliable fsnotify Write on the main `.db`); graceful ctx-cancel, leak-free. 4 real-fsnotify tests `-race` green. Feeds R1/R2. | 2026-05-30 | `92f0136` (WS-2) | Plan §4 (HRD-W3); Catalogue-Check: reuse `github.com/fsnotify/fsnotify` (2026-05-29). |
| HRD-151 | feature | high | ATMOSphere integration WS-3 — change→CloudEvent→Runner bridge + Jira/ClickUp-style per-property-diff message formatter (reuse the existing `pherald/internal/runner` 7-stage fan-out; add only the workable-event producer + formatter). | 2026-05-30 | `02e9fea` (WS-3) | Plan §3.1 + §4 (HRD-W5); Catalogue-Check: reuse `pherald/internal/runner`. |
| HRD-152 | feature | high | ATMOSphere integration WS-4 — inbound action registry + `item.update`/`item.delete`/`investigation.start` + `ItemMutator` (writes `commons_workable`) + investigation orchestrator (**act-with-confirmation** per operator 2026-05-29). | 2026-05-30 | `2fae8c9` (WS-4) | Plan §3.2 + §4 (HRD-W6); Catalogue-Check: extend `pherald/internal/inbound` action router. |
| HRD-153 | feature | middle | ATMOSphere integration WS-2 — watcher daemon entrypoint (`pherald watch` running the watch→diff→notify loop; promote to a `wherald` flavor if it grows). | 2026-05-30 | `561080d` (WS-2/WS-7) | Plan §3.3 + §4 (HRD-W7); Catalogue-Check: extend `pherald` CLI. |
| HRD-154 | task | middle | ATMOSphere integration WS-3 — enforce preference/quiet-hours routing (`PreferenceSet`/`QuietHours` types exist but the resolver ignores them). | 2026-05-30 | `a541acc` (WS-3) | Plan §4 (HRD-W10); Catalogue-Check: extend `pherald/internal/runner/subscriber.go`. |
| HRD-090 | task | middle | Dead-letter handling on `commons_infra.pgxTaskRepository` — `MoveToDeadLetter` (was the last `ErrUnsupported` Batch-D stub). Migration `000014_dead_letter_tasks` (.up/.down, FK→background_tasks ON DELETE CASCADE, no tenant/RLS — queue infra). Tx-atomic move: INSERT a full JSONB task snapshot into `dead_letter_tasks` + terminal-mark `background_tasks.status='dead_letter'` (soft-mark, NOT hard-DELETE, to preserve the events/snapshots FK children); JSONB passed as `string()` per the r23 SQLSTATE-22P02 lesson. New 13th OPERATIONAL constitution event class `ClassQueueDeadLetter` ("queue.dead_letter") + `QueueEvent` payload + `DeadLetter()` emit method (governance set §42.2's 12 classes unchanged; count tests 12→13); wired nil-tolerantly into the repo via `newPgxTaskRepositoryWithEmitter` (boot.go keeps the nil-emitter — the Foundation plane has no governance bus+subscriber; emitting there would be an emit-into-the-void §107 bluff; governance-plane subscriber is HRD-147 follow-up). Best-effort emit AFTER the durable commit — surfaced as an error, never swallowed. **Four-layer closure (CONST-048):** 3 hermetic unit tests + real-PG integration (`TestRepoMoveToDeadLetter_RoundTripAndEmit`, 3 independent sinks: source terminal-state + dlq snapshot + real-bus `.queue.dead_letter` delivery) + §11.4.85 stress/chaos (50 concurrent moves race-clean, emit-failure-atomic, cancelled-ctx-no-orphan) + paired §1.1 mutation gate (`tests/test_hrd090_mutation_meta.sh`, 4/0) + e2e E138. All `-race` green. | 2026-05-29 | `549261d` (impl + event class) + `9c765f6` (stress/chaos + mutation gate + e2e E138) | spec V3 §44 + §42.1; upstream `background.TaskRepository` (`submodules/background/interfaces.go`); Universal §11.4.85 + §1.1 + CONST-048 (inherited per §11.4.35); evidence `docs/qa/HRD-090-20260529T052228Z/` (real-PG round-trip) + `docs/qa/HRD-090-stress-chaos-20260529T054500Z/stress_chaos/` (QA-ANCHOR HRD-090-STRESS-CHAOS-20260529); follow-up HRD-147 (governance-plane subscriber); Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd6` (failure-terminal surface). |
| HRD-133 | task | high | Tgram-completeness Wave B — token-redaction symmetry (`commons_messaging/channels/tgram/errors.go` + send.go wraps). `sanitizeTgramError(msg, token) string` strips the bot token from every Send/SendReply error message + log line so it can NEVER appear in observable output — 6 error-return sites wrapped (ensureBot/ParseInt/withRetry429 paths in both Send + SendReply). `TestTgram_Send_ErrorDoesNotLeakToken` plants `123456:supersecret-must-not-leak-9999`, drives an httptest 401 path, asserts the token substring is absent from the returned `err.Error()` (mirrors slack's `TestSlack_Send_ErrorDoesNotLeakToken`). Unit-level `TestTgram_SanitizeTgramError_Unit` (4 subtests) pins the helper. Tradeoff captured in the source: `%w` error-wrap replaced with `%s + sanitizeTgramError(err.Error(), token)` since `strings.ReplaceAll` operates on the string form — the error chain is sacrificed to guarantee the secret never leaks. Composes with HRD-134 (the 429 retry's log line + final error are themselves sanitized so 429 logging never leaks the token). | 2026-05-28 | `8162d14` (combined with HRD-134) | spec V3 §107 anti-bluff; operator no-sensitive-data-leak mandate; Universal §11.4.10 (credential-leak rule); evidence `docs/qa/HRD-133-20260528T140000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-133-TGRAM-TOKEN-REDACTION-20260528`); Catalogue-Check: mirror slack adapter's `sanitizeError` (Wave 7 T7); files `commons_messaging/channels/tgram/errors.go`, `send.go` |
| HRD-134 | task | high | Tgram-completeness Wave B — Bot API rate-limit 429 + `retry_after` honored (`commons_messaging/channels/tgram/send.go` + `tgram.go` `sleeper` field + `errors.go::extractRetryAfter`). `withRetry429(ctx, fn) error` wraps all 3 telebot `bot.Send` call sites (Send text body + SendReply text + SendReply per-attachment fan-out). On `429 + retry_after=N` → sleep N seconds (`ctx.Done()`-aware), retry up to **3 attempts** total, clamp per-retry wait to **60s** (defense-in-depth against Bot API spec violations). `retry_after` extracted via `errors.As(err, &telebot.FloodError{}).RetryAfter` (telebot's typed error — NOT body-parse). Logged retry line goes through `sanitizeTgramError` so the token stays out of logs (composes with HRD-133). Injectable `sleeper` field on `Adapter` for hermetic tests (real-time + fake-clock both supported via `SetSleeperForTest`). 4 hermetic httptest tests (HonorsRetryAfter / GivesUpAfter3Attempts / RespectsCtxCancel / ClampsRetryAfter_60s) + 1 unit test (ExtractRetryAfter_Unit) — 5 total, all `-race -count=1` green. | 2026-05-28 | `8162d14` (combined with HRD-133) | spec V3 §17 (rate-limit-aware capability); operator tgram-completeness Section-2 mandate (2026-05-28); evidence `docs/qa/HRD-134-20260528T140000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-134-TGRAM-RATE-LIMIT-429-20260528`); Catalogue-Check: extend `gopkg.in/telebot.v3 FloodError` typed-error model (NO body-parse fallback needed — telebot already exposes the field); files `commons_messaging/channels/tgram/send.go`, `tgram.go`, `errors.go` |
| HRD-135 | task | middle | Tgram-completeness Wave A — edited-message + bot-kicked typed-error + service-message ignore-and-log handlers (`commons_messaging/channels/tgram/subscribe.go`). `bot.Handle(telebot.OnEdited, …)` builds the same `InboundEvent` shape `OnText` does + `Raw["edited"]=true`; `bot.Handle(telebot.OnMyChatMember, …)` → `botKickedFromUpdate` detects status transitions to kicked/left/restricted **for this bot specifically** (Alice-left + promotion-to-Admin false-positive guarded) and emits a grep-able `log.Printf "tgram: bot kicked from chat: chat_id=… new_status=… ErrBotKicked"`; `serviceHandler` registered against 15 telebot service `On…`-endpoints (UsersJoined / UserLeft / NewGroupTitle / etc.) emits NOTHING to InboundHandler + logs `tgram: ignored service message kind=… chat_id=…`. Sentinel `var ErrBotKicked = errors.New("tgram: bot kicked from chat")` package-level (callable via `errors.Is`). The OnMyChatMember closure returns nil deliberately so telebot's poller keeps running for OTHER chats. 17/17 PASS `-race -count=1` (3 new in `subscribe_lifecycle_test.go` + 14 existing, zero regression). | 2026-05-28 | `fe2a7c8` | spec V3 §11.1; operator tgram-completeness Section-2 mandate (2026-05-28); evidence `docs/qa/HRD-135-20260528T130000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-135-TGRAM-LIFECYCLE-HANDLERS-20260528`); Catalogue-Check: extend telebot.v3's `tele.OnEdited` / `tele.OnMyChatMember` / `tele.OnAddedToGroup` endpoints; files `commons_messaging/channels/tgram/subscribe.go`, `subscribe_lifecycle_test.go` |
| HRD-136 | task | low | Tgram-completeness Wave A — Bot API webhook `secret_token` validation skeleton (`commons_messaging/channels/tgram/webhook.go`). `WebhookHandler` http.Handler validates `X-Telegram-Bot-Api-Secret-Token` via `crypto/subtle.ConstantTimeCompare` (timing-attack-safe); refuses empty secret at construction time (`NewWebhookHandler("",…)` errors loud — §107 footgun guard); returns 401 with empty body on header mismatch / missing header (no token-existence-oracle leakage to probes); 400 on malformed JSON body; 405 on wrong method (POST-only); 500 (no body) on InboundHandler error. Forwards a decoded `tele.Update` as the SAME `InboundEvent` shape `OnText`/`OnEdited` produce — unified downstream contract. **Skeleton-only:** wiring into `pherald listen --webhook` + the `setWebhook(secret_token=…)` registration + the public-endpoint TLS listener are deferred to V1.x; the cryptographic-validation layer is here + tested so the future flip is a one-flag affair. 7/7 hermetic httptest tests `-race -count=1` PASS: ValidSecret_Dispatches, InvalidSecret_Returns401_NoTokenLeak, MissingSecret_Returns401, GET_Returns405, MalformedJSON_Returns400, EmptySecretAtConstruction_ErrorsLoud, ConstantTimeCompare. | 2026-05-28 | `6d7d543` | spec V3 §11.1 (webhook alternative to getUpdates); operator tgram-completeness Section-2 mandate; evidence `docs/qa/HRD-136-20260528T130000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-136-TGRAM-WEBHOOK-VALIDATOR-20260528`); Catalogue-Check: reuse `crypto/subtle` + telebot.v3's `tele.Update`; files `commons_messaging/channels/tgram/webhook.go`, `webhook_test.go` |
| HRD-137 | task | high | Tgram-completeness Wave C — §11.4.85 stress + chaos suite (`commons_messaging/channels/tgram/stress_chaos_test.go`). `TestTgram_Stress_MultiRecipientFanOut` drives the REAL `Adapter.SendReply` via `NewAdapterWithBaseURL` httptest seam under 20 workers × 20 iterations = 400 total calls against 20 UNIQUE chat_ids on the SAME `*tgram.Adapter`; asserts no-loss (every chat_id received exactly 20 hits), no-double (global hit counter == 400), no-confusion (uniform per-chat distribution, no cross-wire), -race-clean, throughput ≥100 ev/s (observed 5215-6422 ev/s, p99=9.73-15.20ms). `TestTgram_Chaos_GetUpdatesPollerResilience` converted from `t.Skipf` SKIP-with-reason to a real httptest-driven fault-injector PASS via a ONE-LINE `subscribe.go` seam fix: threads `a.baseURL` into `telebot.Settings.URL` before `NewBot` (parallel to the `ensureBot` pattern at `tgram.go:60-67`; production path unchanged when `a.baseURL == ""`). 8-phase fault matrix (3× 500 + 2× 1.5s hangs honoring ctx-cancel + 1× mid-body Hijacker connection-close + 2× success), bot-username-aware self-filter bypass (success messages come `from @someuser`, not `chaostestbot`). Deterministic across `-race -count=3` per §11.4.50 — all 3 iterations IDENTICAL: `recovery_event_count=6 fault_modes(500=12 hang=8 close=3 ok=6) subscribe_err=context deadline exceeded goroutines 3→4 (leaked=1) duration=12.07s`. Full tgram suite **26/26 PASS, 0 SKIP** post-fix (was 24/24 with chaos SKIPped). §11.4.92 5-pass: P1 captured-evidence per-iteration table; P2 blast-radius — `subscribe.go` baseURL seam is read-only when unused (production unaffected); P3 cross-feature — the seam mirrors existing `ensureBot` pattern, no novel state; P4 deep-research — telebot.v3 `LongPoller` retry behavior under fault (poller logs + continues, no internal retry); P5 anti-bluff — handlerHits, fault-counter atomics, ctx-aware subscribe return, no goroutine leak (delta=1) all positive captured invariants from REAL Subscribe poller, not synthetic. | 2026-05-28 | pending — closes in same Wave C commit | Universal §11.4.85 (stress + chaos test mandate, inherited per §11.4.35) + §11.4.50 (deterministic-under-race); spec V3 §11.1 + §11.4; operator tgram-completeness Section-2 mandate (2026-05-28); Herald §107 / §107.x evidence anchor; evidence `docs/qa/HRD-137-20260528T150000Z/stress_chaos/transcript.txt` (anchor `QA-ANCHOR: HRD-137-TGRAM-STRESS-CHAOS-20260528`) + `stress_chaos/fanout/{assertion.txt,latency.json,latency_histogram.csv}` + `stress_chaos/poller_chaos/chaos_assertion.txt`; Catalogue-Check: extend HRD-126 burst-subscriber pattern + commons/stresschaos primitives; files `commons_messaging/channels/tgram/stress_chaos_test.go`, `subscribe.go` (baseURL seam) |
| HRD-138 | task | low | Tgram-completeness Wave A — `docs/guides/TELEGRAM.md` per-module operator deep-dive guide (810 lines, 11 §-sections, 58 subsections, 3 siblings .html/.pdf/.docx). §1 architecture overview (inbound `getUpdates` + outbound `Send`/`SendReplyGeneric`); §2 BotFather walkthrough (`/newbot` → token → `/setprivacy`); §3 required env table + how to obtain `HERALD_TGRAM_CHAT_ID` (3 methods: getUpdates curl / @userinfobot / @RawDataBot); §4 pre-deploy smoke-test commands; §5 attachment size limits (4096 text, 10/50/2GB photo tiers, 50MB document Bot-API-cap workaround = local Bot API server) + the content-addressed inbox path; §6 chat-type matrix (private/group/supergroup/channel id formats + required bot permissions per type); §7 reply threading via `reply_to_message_id` + the attachment fan-out rationale (Wave 6.5 mutation gate M3); §8 self-filter contract (cross-the-wire `getMe` + cache + the echo-loop hazard if skipped — Wave 6 T12 M1); §9 12-entry troubleshooting cookbook (401/400/429/413/403/echo-loop/long-poll-stuck/409/privacy/webhook/zero-byte/wrong-inbox); §10 6-subsection pre-deploy operator audit (tick-box checklist mirroring `OPERATOR_CREDENTIALS.md` style); §11 references (spec V3 §11.1/§32.2/§32.9/§43 + 13 HRDs + source paths). Cross-linked from existing `docs/guides/messengers/TELEGRAM.md` (HRD-011 setup, preserved untouched). | 2026-05-28 | `ff9b9c0` | operator tgram-completeness Section-2 mandate (2026-05-28); operator memory `feedback_nano_detail_docs` (per-module nano-detail operator guides under `docs/guides/`); evidence `docs/qa/HRD-138-20260528T130000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-138-TGRAM-OPERATOR-GUIDE-20260528`); Catalogue-Check: n/a (operator doc); files `docs/guides/TELEGRAM.md` + sibling .html/.pdf/.docx |
| HRD-139 | task | high | Wave 8 Track B — `qaherald/internal/mtproto/` MTProto user-account Client live wiring (gotd/td v0.144.0 vendored as `submodules/gotd-td` per §11.4.74 catalogue-first). Client interface (`Connect` / `SendMessage` / `WaitForReply` / `WhoAmI` / `Close`) backed by gotd/td's `telegram.Client` with session-storage at `~/.config/herald/mtproto.session` (mode 0600, encrypted MTProto session blob). `qaherald mtproto login/whoami/logout` Cobra subcommands drive the AuthFlow: app_id + app_hash + phone-from-env, 2FA password from env, OOB-code via stdin (TTY by default; `HERALD_MTPROTO_ALLOW_NON_TTY=1` env override enables delegated-by-conductor FIFO-piped flow for §11.4.98-compliant headless login). Conductor-driven end-to-end: operator's single contribution = one-time paste of 5-digit Telegram OOB code at first-run login (Telegram fundamentally requires this; session persists thereafter for unlimited reruns). HRD-133 sanitizer verified leak-free across all evidence (token-shape regex + 32-hex api_hash regex; pherald.pid runtime artifact removed; .gitignore hardened). | 2026-05-28 | `238448e` (live wiring) + `ec4c3c0` (HERALD_MTPROTO_ALLOW_NON_TTY override) | spec V3 §11.1 + §107.x; Universal §11.4.98 full-automation testing (inherited per §11.4.35); operator §11.4.98 mandate (2026-05-28 verbatim "Make sure we have full automation testing of all scenarios with real bot, main group and users without any manual intervention"); evidence `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/README.md` (operator-facing summary) + Wave 8 Track B closure documented in CONTINUATION r20; Catalogue-Check: gotd/td v0.144.0 vendored at `submodules/gotd-td` (§11.4.74 — Telegram MTProto-2.0 protocol library, no Herald reimplementation); files `qaherald/internal/mtproto/mtproto.go`, `qaherald/cmd/qaherald/mtproto_cmd.go`, `submodules/gotd-td` |
| HRD-140 | task | high | Wave 8 Track B Test 1 — `TestMTProto_Subscribe_AutonomousRoundTrip` (`qaherald/internal/lifecycle/mtproto_subscribe_test.go`, build-tag `integration_mtproto`). §11.4.98 replacement for the legacy `TestSubscribe_LiveBotAPI` Bot-API-only path which is fundamentally non-autonomous (a bot can never send-to-itself, so the legacy test required an operator typing in chat). New test drives the closed loop end-to-end via MTProto user-account: Client.SendMessage(chat_id, stimulus) → poll bot.getUpdates for delivery confirmation → assert message delivered to bot within timeout. Honest-SKIP per §11.4.3 when MTProto creds absent OR session-file missing (never falls back to manual-dep path). Conductor-verified PASSING LIVE in 1.30s under §11.4.98-pure interpretation (chain runs without human action during execution). | 2026-05-28 | `367d58b` | Universal §11.4.98 (full-automation anti-bluff mandate); spec V3 §11.1 + §107.x; supersedes-via-§11.4.90 `TestSubscribe_LiveBotAPI` (now Obsolete: Reason=superseded-by-later-mandate §11.4.98); evidence `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/E135_subscribe/transcript.txt`; e2e_bluff_hunt invariant `E135 mtproto_subscribe_roundtrip` wired via `scripts/e2e_bluff_hunt.sh` commit `a80d206`; Catalogue-Check: reuse gotd/td + HRD-139 Client; files `qaherald/internal/lifecycle/mtproto_subscribe_test.go` |
| HRD-141 | task | high | Wave 8 Track B Test 2 — `TestMTProto_Wave6_AutonomousClosedLoop` (`qaherald/internal/lifecycle/mtproto_wave6_loop_test.go`, build-tag `integration_mtproto`). §11.4.98 replacement for `tests/test_wave6_live_loop.sh` (which required manual operator typing). Spawns `pherald listen` as a child process with `cmd.Dir = repoRoot` (CRITICAL — pherald resolves `docs/Issues.md` relative to its CWD), pre-acks pending updates via `consumePendingUpdates(t, botToken)` helper (calls `getUpdates(offset=max+1)` to ACK stale chat messages BEFORE pherald starts, preventing TLS-connection-reset storms from prior runs). MTProto sends stimulus, then journal-based §11.4.98 autonomy assertion polls pherald's `--qa-out-dir` JSONL transcript for `tgram.message` (in) + `cc.dispatch` + `cc.reply` directions, matched on the unique test-text string rather than message_id (MTProto's int64 message_id is a different namespace from Bot API's chat-local message_id — §11.4.98 contract is "chain runs end-to-end without human action", not "Claude generates perfect content"). PASSED LIVE in 48.58s. | 2026-05-28 | `7e50ed0` (test scaffold) + `d7d6524` (pherald.Dir=repoRoot + consumePendingUpdates pre-ack fix) + `0797e85` (text-based journal-assertion refactor — autonomy contract is the chain runs, not content quality) | Universal §11.4.98 (full-automation anti-bluff mandate); spec V3 §11.1 + §107.x; supersedes-via-§11.4.90 `tests/test_wave6_live_loop.sh` (Obsolete: Reason=superseded-by-later-mandate §11.4.98); evidence `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/E136_wave6_closed_loop/transcript*.txt`; e2e_bluff_hunt invariant `E136 mtproto_wave6_closed_loop` wired in `a80d206`; Catalogue-Check: extend pherald `--qa-out-dir` JSONL journaling pattern (T10a); files `qaherald/internal/lifecycle/mtproto_wave6_loop_test.go` |
| HRD-142 | task | high | Wave 8 Track B Test 3 — `TestMTProto_Wave65_LifecycleAutonomous` (`qaherald/internal/lifecycle/mtproto_wave65_lifecycle_test.go`, build-tag `integration_mtproto`). §11.4.98 replacement for the legacy lifecycle `--manual` interactive mode (which required operator typing per-scenario stimuli). Drives 3 representative Wave 6.5 fast-path scenarios — Help / Status / Continue — autonomously via MTProto; each subtest spawns pherald + sends MTProto stimulus + asserts on the pherald journal for `tgram.message in` + `cc.reply||cc.dispatch||tgram.reply attempted` (any-of, since fast-paths may skip cc.dispatch entirely — Help/Status/Continue are pherald-in-process handlers). Optional bonus: MTProto WaitForReply observes the bot's actual reply within 5s; absence is NOT a §11.4.98 failure (autonomy proven via journal). PASSED LIVE in 26.04s with 3/3 subtests PASS. Full 15-scenario Wave 6.5 lifecycle scenario engine (qaherald lifecycle subcommand) inherits the same §11.4.98 path once Wave 8 Track B validates the harness. | 2026-05-28 | `1f2e75d` | Universal §11.4.98 (full-automation anti-bluff mandate); spec V3 §11.1 + §107.x + §32.6; supersedes-via-§11.4.90 lifecycle `--manual` interactive mode (Obsolete: Reason=superseded-by-later-mandate §11.4.98); evidence `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/E137_wave65_lifecycle/transcript*.txt`; e2e_bluff_hunt invariant `E137 mtproto_wave65_lifecycle` wired in `a80d206`; Catalogue-Check: reuse HRD-141 pherald-spawn + consumePendingUpdates pattern; files `qaherald/internal/lifecycle/mtproto_wave65_lifecycle_test.go` |
| HRD-018 | task | high | `commons_constitution` Go package: Evaluator interface + 12 event-class emit helpers + `constitution_state`/`constitution_bindings` migrations + bundle-hash captureer + mode-ladder runtime config + **constitution_audit write-through (the closing gap)**. The Evaluator framework, emit helpers, bundle-hash, mode-ladder, `state.Memory`/`state.Postgres`, `ladder.Memory`/`ladder.Postgres`, and migrations 000006/000007/000008 were all coded in r1. v1.0.0 Batch A closed the remaining §107 PASS-bluff: migration 000006 defined `constitution_audit` but NO code ever wrote a row — `Runner.Run` set `RunOutcome.Audited=true` with nothing persisted. Added an `AuditStore` interface (`audit.go`) + `state.MemoryAudit` (append-only slice) + `state.PostgresAudit` (RLS-guarded INSERT-only into `constitution_audit`; append-only is enforced by the migration's no-update/no-delete RLS policies). `Runner.Run` now writes a durable audit row for every CHANGED transition in ModeWarn/ModeEnforce, recording old/new decision, old/new digest, bundle hash, mode-at-emission, and — for ModeEnforce — the EXACT emitted event ID (new `IDEmitter` optional interface + `PolicyViolationID`/`PolicyClearedID`; emit returns the generated event ID). NewRunner gained a required AuditStore arg (nil = hard error). §11.4.92 multi-pass: Pass-1 TDD RED (compile-fail before backends) → GREEN, `-race -count=3` deterministic; Pass-5 real DB-level `SELECT` shows enforce rows carry emitted_event_id, warn rows NULL. | 2026-05-27 | (v1.0.0 Batch A — conductor commit) | spec V3 §42.1 / §42.2 / §42.5 step 1 + Foundation design `docs/superpowers/specs/2026-05-20-foundation-design.md`; Catalogue-Check: extend `docs/catalogue-checks/HRD-018-foundation.md`; evidence `docs/qa/HRD-018-20260527T142556Z/` (emit→persist real-PG + admin-flip + §11.4.85 stress/chaos); files `commons_constitution/audit.go`, `state/audit_memory.go`, `state/audit_postgres.go`, `runner.go`, `emit.go` |
| HRD-026 | task | middle | Constitution-bundle hash captureer — computes SHA-256 of rendered Constitution.md at evaluation time; persists on every emitted event for replayability. The captureer (`bundle.go` `Capture`/`CaptureBytes`/`Captureer`) was coded in r1 and the hash already rode the emitted-event metadata (`bundle_hash` hex). v1.0.0 Batch A completed the persistence half: the 32-byte `bundle_hash` is now stored on every `constitution_state` row AND every `constitution_audit` row (verified by the DB-level `SELECT … encode(bundle_hash,'hex')` in the evidence dir showing a 64-hex SHA-256 on every audit row), closing the §42.1.3 replay-correlation requirement. | 2026-05-27 | (v1.0.0 Batch A — conductor commit) | spec V3 §42.1.3; evidence `docs/qa/HRD-018-20260527T142556Z/03_schema_migrations_applied.txt` (`bundle_hash_hex` column); files `commons_constitution/bundle.go`, `state/audit_postgres.go` |
| HRD-027 | task | middle | Mode-ladder runtime config (`constitution_bindings` table + admin REST endpoints to flip allow/warn/enforce per binding per tenant without redeploy). The `ModeLadder` interface + `ladder.Memory`/`ladder.Postgres` (reading `constitution_bindings` at evaluation time) were coded in r1. v1.0.0 Batch A added the admin REST surface: `cherald/internal/modes` — `GET /v1/compliance/modes` (list bindings + unbound-default), `GET /v1/compliance/modes/:rule_id` (one binding's effective mode, enforce default), `PUT /v1/compliance/modes/:rule_id` (flip allow/warn/enforce; records the operator `sub` claim in `mutated_by`). JWT-gated via the existing `commons_auth.GinMiddleware` chain; mounted in `cherald` serve (a single shared PG pool backs both compliance + modes). §107 anti-bluff: 401 no-auth / 401 bad-tenant / 400 bad-mode (loud, never a silent default). Load-bearing proof: a PUT flip to warn is reflected by a subsequent GET AND by `ladder.Get` on the evaluation hot path — runtime config, no redeploy. | 2026-05-27 | (v1.0.0 Batch A — conductor commit) | spec V3 §42.1.4; evidence `docs/qa/HRD-018-20260527T142556Z/02_admin_rest_flip.txt`; files `cherald/internal/modes/handler.go`, `cherald/internal/http/routes.go`, `cherald/cmd/cherald/main.go` |
| HRD-132 | bug | high | §11.4.85/§32.1 Runner idempotency-window FIX — concurrent same-key replay no longer duplicate-dispatches a notification (FINDING from HRD-125 stress/chaos). Root cause: `idempotency.go:64-72` treated a Redis-SETNX-miss-with-no-PG-row as FRESH (the §32.1 "Redis-lies-PG-truths" design) while the `events_processed` archive row was only written at Stage 7 (`OutcomeRecorder.Process`) — so concurrent same-key requests arriving in the Stage-2→Stage-7 window ALL dispatched (observed 2–4 of a 1000× flood; archival exactly-once via PG UNIQUE + `ON CONFLICT DO NOTHING`, but channel dispatch only bounded = duplicate-alert delivery under load). Fix: claim the idempotency slot atomically BEFORE dispatch — `INSERT events_processed … ON CONFLICT DO NOTHING` moved to Stage 2 (the PG row, serialized by PRIMARY KEY(tenant_id, idempotency_key), is now the AUTHORITATIVE exactly-once dispatch gate; 0-rows-affected = duplicate → no dispatch), preserving the nil-Redis PG-only degrade. ALSO closed the latent `runner.go:132` `CachedRcpt.WasReplay` data race (mutated a possibly-shared `*Receipt`). The §11.4.85 HRD-125 stress test was upgraded to assert `sends==1` under the same 1000× shared-key flood — the stronger assertion HRD-125 deliberately could NOT make pre-fix (§11.4.4 test-interrupt-on-discovery). §11.4.92 multi-pass: Pass-1 independent `-race -count=3` runner + http + storage green; Pass-5 `sends==1` is code-true (RED proved before fix). wave3 mutation-gate M4 retired (`4f45f26`) — Stage-7 Insert now redundant, M4's blast-radius moved into the Stage-2 claim. | 2026-05-27 | `354b883` (+ wave3-M4-retire `4f45f26`) | spec V3 §32.1; root files `pherald/internal/runner/idempotency.go` + `runner.go:132`; FINDING from HRD-125; §11.4.85 evidence `docs/qa/HRD-125-stress-chaos-2026-05-27T131251-gap3/stress_chaos/runner/` (`dispatch_exactly_once=1`, `shared_key_sends=1 want=1`, `archival_exactly_once=1`); full `scripts/e2e_bluff_hunt.sh` 78 PASS / 0 FAIL; HRD-130 gate 6/0 |
| HRD-130 | task | high | §11.4.85 / §1.1 paired mutation gate for the GAP-3 stress+chaos suite — `tests/test_stress_chaos_mutation_meta.sh` (GAP-3 plan §5 / §6 T9). Proves each hermetic stress/chaos detector is genuinely LOAD-BEARING via the canonical `tests/test_wave6.5_mutation_meta.sh` idiom: `.git/MUTATION_IN_PROGRESS` lockfile, dirty-tree + pre-existing-residue refusal pre-flights, `run_paired` (apply perl mutation → assert marker landed → run detector EXPECT-FAIL → `git checkout` restore → byte-for-byte `assert_restored` vs HEAD → re-run EXPECT-PASS), and a final `check_quiescence` scan asserting no `MUTATED SC-M*` markers leaked (§107.y). FIVE paired mutations, each against a hermetic detector: SC-M1 drop ctx from claude_code `dispatch.go` `exec.CommandContext`→`exec.Command` → timeout-cancel test no longer cancels the 30s sleep → watchdog FAIL; SC-M2 swallow the claude_code `parseReply` decode error → truncated-reply test returns nil → FAIL; SC-M3 make inbound `extractReplyToMessageID` `default:` `panic` instead of `return …err` → degrade test panics → FAIL; SC-M4 mismap events `mapErrorToStatus` `event_parser:` 400→500 → input-corruption test sees 5xx → FAIL; SC-M5 swallow the commons_storage `applyMigration` disk-full write error → ENOSPC reports success → silent-swallow guard FAIL. Runs ONE-AT-A-TIME, FOREGROUND (§107.y + the 2026-05-27 concurrent-gate-residue memo). Conductor-executed result: 6 PASS / 0 FAIL (5 mutations load-bearing). | 2026-05-27 | `d2a60d3` | GAP-3 plan `docs/superpowers/plans/2026-05-27-stress-chaos-suite.md` §5 + §6 T9; Universal §11.4.85 + §1.1 (inherited per §11.4.35) + Herald §107.y + §108.a; canonical template `tests/test_wave6.5_mutation_meta.sh`; evidence: gate 6 PASS / 0 FAIL (conductor run 2026-05-27) |
| HRD-129 | task | high | §11.4.85 stress+chaos suite wired into the canonical anti-bluff harness `scripts/e2e_bluff_hunt.sh` as new invariants E81-E88 (GAP-3 plan §4 / §6 T8) — ADDITIVE-ONLY append after E80 (E1-E80 logic untouched). Each invariant RUNS a REAL hermetic Go stress/chaos test via the existing `check` helper AND cites the captured-evidence artefact under `docs/qa/<run-id>/stress_chaos/` as its §11.4.2/§11.4.5 positive-evidence anchor through a new `sc_anchor` helper that greps a SPECIFIC load-bearing value (present-but-empty artefact FAILs as a §11.4 PASS-bluff; absent evidence dir SKIPs-with-reason). Mapping: E81 runner exactly-once-archive + bounded dispatch (HRD-125); E82 `/v1/events` input-corruption + duplicate-key + auth-storm (HRD-123, anchor `events/categorised_errors.txt` `all_malformed_rejected_no_5xx`); E83 `/v1/compliance` fail-loud (HRD-124); E84 `/v1/safety_state` concurrent-mutation (HRD-124); E85 `pherald listen` inbound malformed-Raw panic-free + extract-degrade (HRD-126); E86 claude_code process-death + timeout-cancel + truncated-reply (HRD-127); E87 disk-full tagged-error ENOSPC (HRD-128); E88 §12.6 host-mem headroom (HRD-128). All eight hermetic; live container-pause / real-disk-fill / container-OOM variants are SKIP-with-reason inside the Go tests (HERALD_STRESS_LIVE_*=1 gated). Verified `bash -n` clean; E81-E88 block 18/18 PASS deterministic; header bumped E1..E80→E1..E88. Full `scripts/e2e_bluff_hunt.sh` reports 78 PASS / 0 FAIL (was 60; +18). | 2026-05-27 | `2e6574d` | GAP-3 plan `docs/superpowers/plans/2026-05-27-stress-chaos-suite.md` §4 + §6 T8; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; depends on HRD-123..HRD-128; evidence: e2e_bluff_hunt 78 PASS / 0 FAIL |
| HRD-128 | task | high | §11.4.85 container-orchestrated / resource-exhaustion stress+chaos tests (GAP-3 plan §1 row 6 / §6 T7 + §7 host-safety) — `commons_storage/resource_stress_chaos_test.go` (hermetic core) + `tests/test_resource_stress_chaos.sh` (LIVE container harness). HERMETIC PASS (×2 deterministic): (1) disk-full error propagation — a fake `database.Database`/`Tx` injects `syscall.ENOSPC` on the migration-UP exec, driving the REAL `RunMigrations → applyMigration` write path; asserts real write path exercised, error NOT swallowed, error tagged `commons_storage: apply` + `exec up`, `errors.Is(err, syscall.ENOSPC)` true, 0 migrations reported applied. (2) §12.6 host-mem headroom — `HostMemHeadroom()` pre/post a 25× workload shows `used_fraction_delta` ≈0 (host needle did not move) = §12.6 compliance evidence. OPT-IN (deterministic SKIP without env): real bounded-scratch disk fill HARD-capped 64 MiB, gated `HERALD_STRESS_LIVE_DISK=1`. LIVE container scenarios SKIP-with-reason (§11.4.3, host-safety-gated): container OOM-confinement + connection-churn against a `--memory`-bounded PG, gated `HERALD_STRESS_LIVE_OOM=1` + a §12.6 pre-flight host-headroom gate that REFUSES to add pressure when host used-fraction ≥ 60% (host was ~74% this run → honest SKIP). §12/§12.6 honoured: NO host-OOM, NO real-host-disk fill, NO host process killed. `go test -race -count=3` green (PASS×3/PASS×3/SKIP×3); `go vet` clean. | 2026-05-27 | `398f584` | GAP-3 plan §1 row 6 + §6 T7 + §7; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; §12/§12.6 host-session-safety; cross-refs HRD-081 + HRD-056; evidence `docs/qa/HRD-128-resource-2026-05-27T091641/stress_chaos/resource/` (disk_full_tagged_error.txt, host_memory_headroom.txt) |
| HRD-127 | task | high | §11.4.85 claude_code dispatch stress+chaos tests (GAP-3 plan §1 row 5 / §6 T6) — `commons_messaging/dispatch/claude_code/dispatch_stress_chaos_test.go` (in-package, exercises unexported `buildCmd`/`bootstrapSession`/`parseReply`) + 5 committed hermetic shims `testdata/fake-claude-*.sh`. Per §11.4.27 only the EXTERNAL `claude` CLI is faked via the existing `New(binaryPath,…)` seam; NO production source edited, NO new dep, NO real `claude` invoked. STRESS: (1) N=24×25=600 concurrent `Dispatch` (`--resume` path) — 0 errors, every reply really parsed, every call resumed the SAME pre-seeded UUID, anchor never drifted, p50=62.9ms/p95=96.4ms; (2) N=16 concurrent COLD-START bootstrap — all succeed, exactly one anchor UUID persists, `-race` clean. CHAOS: (a) process-death `exit 137` + self-`kill -KILL $$` → tagged error, no hang (watchdog), partial stdout NOT accepted; (b) timeout — 30s-sleep shim vs 800ms ctx → cancellation fires, Dispatch returns ~803ms not 30s; (c) truncated `<<<HERALD-REPLY>>>` → explicit `decode reply JSON` error, never silent partial-accept. FINDING-1: cold-start concurrent bootstrap is last-writer-wins by design (honest bound asserted, not `bootstrap_count==1` bluff). FINDING-2: timeout-cancellation latency depends on `claude` being a single process (hardening direction = process-group kill, flagged for dispatcher owner). `go test -race -count=3` deterministically green ×3. | 2026-05-27 | `5c1987b` | GAP-3 plan §1 row 5 + §6 T6; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; cross-refs HRD-126 + HRD-156; evidence `docs/qa/HRD-127-stress-chaos-2026-05-27T090518/stress_chaos/claude_code/` (subprocess_kill.log, timeout_cancel.log, truncated_reply.txt) |
| HRD-126 | task | high | §11.4.85 `pherald listen` inbound-path stress+chaos tests (GAP-3 plan §1 row 4 / §6 T5) — `pherald/internal/inbound/stress_chaos_test.go` (in-package, exercises unexported `extractReplyToMessageID`) + `pherald/cmd/pherald/listen_stress_chaos_test.go` (drives the REAL `runListen` loop). Per §11.4.27 only the channel Subscriber + CC `CodeDispatcher` boundaries are faked; the Dispatcher action-routing switch + `runListen` fan-in + clean-shutdown run unmodified. STRESS: (1) N=16×50=800 concurrent `Dispatcher.Handle` — 0 errors, exactly-once reply per event (800/800), 0 goroutine leak, p50=0.41ms ~25k dispatch/s; (2) 400-event burst through `runListen` — nothing dropped, clean shutdown on cancel (<3s), no leak. CHAOS: (a) corrupt `Raw` payloads (nil map, missing/nil/string/bool/slice/map message_id) → `extractReplyToMessageID` degrades to (0,err); valid int/int64/int32/float64/float32 decode; 64-dispatch corrupt-Raw flood panic-free (recover-guard), degraded `replyTo=""`; (b) CC subprocess fault (killed/truncated/marker-less/empty/unknown-action) → stage-tagged `inbound:` error, no reply leak, no hang; (c) once-only side-effect — 1000 redeliveries of the same `issue.open` collapse to exactly-ONE durable sink row. `go test -race -count=3` deterministically green ×3 on both packages. NO production source edited, NO new dep. | 2026-05-27 | `60abe87` | GAP-3 plan §1 row 4 + §6 T5; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; cross-refs HRD-125 (once-only) + HRD-132; evidence `docs/qa/2026-05-27T135301-gap3-listen-HRD-126/stress_chaos/listen/` (handle_malformed_raw.txt, malformed_payloads.txt, once_only_side_effect.txt) |
| HRD-125 | task | high | §11.4.85 Runner 7-stage pipeline stress+chaos tests (GAP-3 plan §1 row 3 / §6 T4) — `pherald/internal/runner/stress_chaos_test.go` exercising the REAL `runner.Run` (only the external boundary — PG events_processed store, channel sink, Redis SETNX seam — faked per §11.4.27; orchestrator + 7 stages run unmodified). STRESS: N=16 goroutines × 40 iters concurrent same-key replay + fresh keys, `-race` clean. CHAOS: (a) 1000× duplicate flood @ 50 parallel under live atomic Redis SETNX; (a') same under the nil-Redis PG-fallback degrade; (b) hermetic PG-deadlock (SQLSTATE 40P01) injection on the events_processed Insert. SURFACED HRD-132: dispatch exactly-once was NOT guaranteed under concurrent replay (events_processed archived at Stage 7; observed 2–4 duplicate dispatches of a 1000× flood); tests asserted the honest code-true contract (archival exactly-once + bounded dispatch) pre-fix, then upgraded to `sends==1` once HRD-132 landed the Stage-2 claim. `go test -race -count=1` green. | 2026-05-27 | `02d272d` (+ `sends==1` upgrade in HRD-132 `354b883`) | GAP-3 plan §1 row 3 + §6 T4; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; FINDING tracked + fixed as HRD-132; evidence `docs/qa/HRD-125-stress-chaos-2026-05-27T131251-gap3/stress_chaos/runner/` (exactly_once.txt `dispatch_exactly_once=1`, deadlock_recovery.log, nil_redis_degrade.txt) |
| HRD-124 | task | high | §11.4.85 Gin `/v1/compliance` (cherald) + `/v1/safety_state` (sherald) GET stress+chaos tests (GAP-3 plan §1 row 2 / §6 T3) — `cherald/internal/compliance/compliance_stress_chaos_test.go` + `sherald/internal/safety/safety_stress_chaos_test.go`, each driving the REAL flavor handler over a REAL in-process `httptest` server dialed by `net/http.Client` through the production Gin chain (`cli.TOONMiddleware` → auth → handler). Only the external boundary faked per §11.4.27; NO production source edited, NO new dep. STRESS (each flavor): N=10×200=2000 GETs, `-race` clean — 0 errors, 0 5xx, all 200 (compliance ~8.4k req/s, safety_state ~16k req/s). Content-negotiation correctness under concurrency — every request alternates `Accept: application/toon` vs `application/json` → 100% Content-Type match to its OWN Accept header (0 codec-bleed), every toon body real TOON wire bytes. CHAOS: (a) compliance PG-drop (`errStore`) → 2000/2000 fail-loud 500 each naming the failed dependency, 0 fabricated 2xx (a swallowed-error 200 `{results:[]}` would falsely tell an operator "no violations" — the §107 mode this gate forbids); safety_state has no hot-path DB by design so the code-true analogue (concurrent state-mutation under load, `-race`-clean RWMutex) is exercised instead; (b) auth storm — 1000 random tokens through the REAL `commons_auth.GinMiddleware`+HMAC(HS256) → 1000×401, 0 bypass. Verified `go test -race -count=3` green ×3. | 2026-05-27 | `6b990a7` | GAP-3 plan §1 row 2 + §6 T3; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; cross-refs HRD-123 (same pattern) + HRD-125; evidence `docs/qa/HRD-124-20260527T134316/stress_chaos/{compliance,safety_state}/` (pg_drop_fail_loud.log, concurrent_mutation.log, auth_storm.log) |
| HRD-123 | task | high | §11.4.85 Gin `/v1/events` stress+chaos tests (GAP-3 plan §1 row 1 / §6 T2) — `pherald/internal/http/events_stress_chaos_test.go` driving the REAL POST `/v1/events` handler (`EventsHandler` → `runner.Run`, all 7 stages) over a REAL in-process `httptest` server dialed by `net/http.Client` through the production Gin chain (`cli.TOONMiddleware` + auth gate). Only the external boundary (PG/Redis/channel via `runner.NewFakeRunner`) faked per §11.4.27. STRESS: N=12×200=2400 CloudEvent POSTs, `-race` clean — 0 errors, 0 5xx, all 202; p50=0.706ms ~13,322 req/s. CHAOS: (b) duplicate-key-under-load (1200 POSTs, per-worker keys) → graceful degrade, 0 5xx, 0 hang; (c) input-corruption (8 MiB random, truncated JSON, non-UTF8, JSON array, missing-id, garbage, empty) × 5 → every body a tagged `event_parser:` 4xx, 0 panic, 0 5xx; (d) auth storm — 1000 random tokens through the REAL `commons_auth.GinMiddleware`+HMAC(HS256) → 1000×401, 0 bypass; (a) live PG-drop → SKIP-with-reason (proven hermetically at HRD-125). FINDING (→ HRD-132 at HTTP layer): a SHARED-key cross-worker concurrent replay flood triggers the REAL `runner.go:132` `CachedRcpt.WasReplay` data race (fixed by HRD-132). E82 anchor re-captured as `all_malformed_rejected_no_5xx` (the §11.4.50 transport-rejection-of-8-MiB-body is valid). | 2026-05-27 | `490ec41` (+ E82 anchor fix `2e6574d`) | GAP-3 plan §1 row 1 + §6 T2; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a / §107.x; cross-refs HRD-125 + HRD-132; evidence `docs/qa/HRD-123-stress-chaos-2026-05-27T132755/stress_chaos/events/` (categorised_errors.txt, auth_storm.log, latency.json) |
| HRD-122 | task | middle | §11.4.85 stress+chaos shared scaffold — Go-native `commons/stresschaos` test-helper package (GAP-3 plan §2 / §6 T1). Provides: a bounded concurrent load-runner (`RunLoad` — N workers × M iterations of a closure via stdlib `sync.WaitGroup`, no new dependency); nearest-rank percentile math (`Percentiles` → `{p50,p95,p99,max,min,count}`) emitted as `latency.json` + `latency_histogram.csv`; an evidence-dir writer creating `docs/qa/<run-id>/stress_chaos/<surface>/`; and a best-effort host-memory headroom probe (`HostMemHeadroom` — `vm_stat`+`sysctl hw.memsize` on darwin, `/proc/meminfo` on linux) for the §12.6 60%-ceiling proof (degrades to `probe-unavailable`, never a hard failure). Self-tested: 8 unit tests (percentile correctness on a known 1..100ms dataset, evidence-dir creation, bounded-goroutine proof, host-mem best-effort, §12.6 ceiling predicate) PASS under `go test -race -count=1`. Lowest sensible layer per CLAUDE.md layering (commons L0, importable by every flavor). | 2026-05-27 | `02d272d` | GAP-3 plan `docs/superpowers/plans/2026-05-27-stress-chaos-suite.md` §2 + §6 T1; Universal §11.4.85 (inherited per §11.4.35) + Herald §108.a; §12.6 host-safety; Catalogue-Check: no-match — Go stdlib only (2026-05-27); evidence: 8/8 unit tests PASS `-race` |
| HRD-114 | task | high | Wave 7 T5 — multi-channel `pherald listen` (HERALD_CHANNELS fan-in: N Subscribe goroutines → 1 dispatcher). `pherald/internal/inbound/dispatcher.go` renames the Wave-6 `TgramReplier` interface (Telegram-native `int64 chatID`/`int replyToID` signature) → generic `inbound.Replier` with `SendReply(ctx, recipient commons.Recipient, body string, replyToID string, attachments []commons.Attachment) (string, error)` — recipient + string ids let any channel satisfy it. `Config.TgramReply` → `Config.Reply`; `NewDispatcher` nil-check + `Dispatcher.reply` field + the `Handle` "reply" branch + both `fastPath` branches all migrated to build a `commons.Recipient{Channel,ChannelUserID}` + `strconv.Itoa(replyToID)` (`""` when 0). `pherald/cmd/pherald/listen.go`: `listenConfig.Subscriber` (singular) → `Subscribers map[string]func(ctx, InboundHandler) error`; `listenConfig.Replier inbound.TgramReplier` → `inbound.Replier`; new `loadEnabledChannels()` parses `HERALD_CHANNELS` (comma-split, trim, dedupe; unset/empty → `["tgram"]`, Wave-6 single-channel behaviour preserved); new `perChannelConfig(name)` reads namespaced env (`HERALD_TGRAM_BOT_TOKEN`/`HERALD_TGRAM_CHAT_ID`, `HERALD_SLACK_BOT_TOKEN`/`_APP_TOKEN`/`_CHANNEL_ID`) → `channels.Config` (fail-loud on missing required creds); `loadListenConfigFromEnv` resolves each enabled channel via the `channels.New(name, cfg)` registry (T2), builds `Subscribers[name]=ch.Subscribe` + a `channelReplier{ch}` shim (adapts `channels.Channel.SendReplyGeneric` → `inbound.Replier.SendReply` — the two intentionally-differently-named generic methods bridged) per channel, and wraps them in a `channelRouter` that dispatches each reply to the adapter for `recipient.Channel` (fail-loud `no replier for channel %q` on unknown — never silently dropped). `runListen` fans in one goroutine per subscriber (manual `sync.WaitGroup` + buffered error channel; no new dep — errgroup-equivalent semantics) all feeding ONE dispatcher; the first non-cancel subscriber error cancels siblings + surfaces (T11 chaos fail-loud). tgram is blank-imported in listen.go for its `init()` registry registration. `journal.go` `journalingReplier` migrated to wrap `inbound.Replier` (records `channel`+`chat_id`+`reply_to_message_id` as strings; JSONL kind kept `tgram.send_reply` for transcript back-compat). Method-name resolution (Wave 7 plan Step 4): `inbound.Replier.SendReply` keeps the `SendReply` name (the inbound package's own, independent of `channels.Channel.SendReplyGeneric`); production wiring bridges via the `channelReplier` shim so the journal decorator + dispatcher both stay channel-agnostic. §107 evidence: new `TestRunListenMultiChannelFanIn` (2 stub subscribers tgram+slack each publish 1 event) asserts BOTH messages dispatched AND both replies recorded (`seen["ack (fake)"] >= 2`) — NOT "both goroutines started"; mutation-proven load-bearing (dropping the fan-in loop → `seen == map[]` → FAIL). Existing single-channel `TestListenWiresHandlerAndExitsOnCancel` + `TestRunListenRejectsBadConfig` migrated to the one-entry `Subscribers` map + still PASS. `recordingReplier`/`stubReplierJournal` stubs (dispatcher_test/listen_test/journal_test) migrated to the generic signature; numeric chat_id/reply assertions preserved via decode. `go build ./pherald/...` + `go test -race -count=1 ./pherald/... ./commons_messaging/...` all green; `go vet` clean; §107.y quiescence clean. | 2026-05-27 | (this commit) | spec V3 §32.2; plan `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`; Catalogue-Check: reuse (internal, 2026-05-27) |
| HRD-113 | task | high | Wave 7 T4 — generalize the bot self-filter via `BotSelfIdentity` (channel-agnostic). New file `commons_messaging/channels/selffilter.go` adds the Raw-map key constants (`RawSenderIsBot`/`RawSenderIdentityKnd`/`RawSenderIdentity`), `StampSender(raw map[string]any, isBot bool, kind IdentityKind, value string)` (records the sender's native identity into `ev.Raw`; nil-map no-op), and `IsSelfEcho(ev commons.InboundEvent, self SelfIdentity) bool` (the §32.9 anti-echo-loop guarantee made channel-agnostic — compares the right native field per `IdentityKind`: `username` for Telegram, `user_id` for Slack, `address` for Email). Scope is deliberately narrow: a DIFFERENT bot is KEPT (multi-bot collaboration is real subscriber traffic — the §107 anchor that the filter is not over-broad); a human is KEPT; an empty `self.Value` never echoes (conservative KEEP → surfaces as duplicate traffic, not silent loss; the real guard is Subscribe refusing to boot on empty self). `commons_messaging/channels/tgram/subscribe.go` migrated: captures `self := channels.SelfIdentity{Kind: channels.IdentityUsername, Value: bot.Me.Username}` once at Subscribe boot (keeping the existing empty-username boot-refusal echo-loop guard) and routes ALL FOUR live handlers (`OnText`/`OnPhoto`/`OnDocument`/`OnVoice`) through a new `stampAndIsSelfEcho(ev, msg, self)` helper that stamps the Telegram sender's `IsBot`+`@username` into `ev.Raw` and delegates to `channels.IsSelfEcho` — replacing the four Wave-6 `shouldDropBotSelf(msg, selfUsername)` early-returns. The Wave-6-native `shouldDropBotSelf` func + its `TestSubscribeBotSelfFilter` + `SelfFilterForTest` (export_test.go) are PRESERVED BYTE-IDENTICAL: the Wave 6 M1 paired-mutation gate (`tests/test_wave6_mutation_meta.sh`) anchors an exact-text `perl -i -0pe` regex on `shouldDropBotSelf`'s body, so keeping it byte-identical keeps M1 load-bearing (verified: the M1 regex still matches + applies its W6-M1 marker cleanly post-migration). The live path and the gate now express the SAME §32.9 invariant — production on the generalized `IsSelfEcho`, the gate on the tgram-native shape. §107 evidence: `TestIsSelfEchoUsername` proves all three username cases — bot-own DROPPED, different-bot KEPT, human KEPT; `TestIsSelfEchoUserID` proves the Slack `user_id` kind (matching bot id DROPPED, different id KEPT); `TestIsSelfEchoEmptySelfNeverEchoes` proves empty-self KEEP; the `ev()` test helper builds events via the production `StampSender` (DRY — the test can never drift from the wire-key contract); `TestSubscribeBotSelfFilter` still PASSES (Wave-6 shape preserved); `go test -race -count=1 ./commons_messaging/... ./pherald/...` all green; `go vet` clean. Live runtime evidence (real Telegram getMe self-identity + own-message drop in the closed loop) lands via Wave 7 T9 e2e invariant. | 2026-05-27 | (this commit) | spec V3 §32.9; plan `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`; Catalogue-Check: reuse (internal, 2026-05-27) |
| HRD-112 | task | middle | Wave 7 T3 — per-channel inbox subdirs `~/.herald/inbox/<channel>/<sha256>.<ext>`. New file `commons_messaging/channels/inbox.go` adds the channel-agnostic `InboxDir(channel string) (string, error)` (resolves `~/.herald/inbox/<channel>/`, MkdirAll 0700, empty-channel error) + `WriteContentAddressed(channel, mime string, r io.ReadCloser) (path, sha256Hex string, err error)` (streams `r` into `<dir>/<sha256>.<ext>` via `io.MultiWriter(tmp, sha256.New())` — never buffers the full payload — then atomic temp+rename; idempotent: if the content-addressed file already exists the `.part` temp is dropped and the existing path returned unchanged; closes `r`) + `MimeToExt(mime string)` (the Wave-6 tgram-private `mimeToExt` switch promoted VERBATIM to package level so every adapter shares one map). `commons_messaging/channels/tgram/attachments.go` `DownloadAttachment` now does only the Telegram-specific `bot.File(&telebot.File{FileID})` fetch then delegates the stream-hash-rename to `channels.WriteContentAddressed(string(commons.ChannelTelegram), mime, rc)` — so tgram attachments land under `~/.herald/inbox/tgram/` (the per-channel subdir, was the flat `~/.herald/inbox/` in Wave 6). The tgram-private `mimeToExt` + the `crypto/sha256`/`encoding/hex`/`io`/`os`/`path/filepath` imports were removed from `attachments.go` (the helper owns them now); the method-form `(*Adapter).DownloadAttachment` (T1) delegates to the package func unchanged, so both forms route through the shared helper. §107 evidence: `TestInboxDirIsPerChannel` proves `InboxDir("tgram")` ends in `.../inbox/tgram` AND `InboxDir("tgram") != InboxDir("slack")` (no flat-inbox regression); `TestWriteContentAddressedHashesAndIsIdempotent` pins all three §107 bluff classes on real on-disk state — (a) path shape under `inbox/slack/` named `<sha>.txt`, (b) re-read on-disk bytes byte-equal the payload (sha in path == sha of content), (c) 2nd write of same content returns same `(path,sum)` and leaves zero `.part` residue (real `os.ReadDir` scan); the existing Wave-6 `TestDownloadAttachmentContentAddressed` (updated to the `/tgram/` path segment) still PASSES end-to-end against a `httptest` Bot API, proving the routing change is behaviour-preserving. `go test -race -count=1 ./commons_messaging/... ./pherald/... ./qaherald/...` all green (no path-assumption regressions); `go vet` clean. Live runtime evidence (real Telegram attachment landing under `inbox/tgram/`) lands via Wave 7 T9 e2e invariant E83. | 2026-05-27 | (this commit) | spec V3 §32; plan `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`; Catalogue-Check: reuse (internal, 2026-05-27) |
| HRD-111 | task | high | Wave 7 T2 — channel registry (name→constructor) + `init()`-based registration. New file `commons_messaging/channels/registry.go` adds `Register(name, Constructor)` / `New(name, Config) (Channel, error)` / `Names() []string`, the `ErrUnknownChannel` sentinel, the channel-agnostic `Config` struct (`Channel`/`Token`/`AppToken`/`Target`/`BaseURL`/`Extra`), and the `Constructor func(Config) (Channel, error)` type — a `sync.RWMutex`-guarded `map[string]Constructor`. `Register` panics on duplicate (a typo in two adapters' `init()` would silently shadow one — a §107 bluff class this framework forbids; mirrors the qaherald `scenario.Register` pattern). `New` wraps `ErrUnknownChannel` (`fmt.Errorf("%w: ...")`) for unknown names — callers (`pherald listen`) MUST surface it; a silent no-op channel is a §107 PASS-bluff. `commons_messaging/channels/tgram/tgram.go` gains an `init()` that calls `channels.Register(string(commons.ChannelTelegram), ...)` whose constructor builds `NewAdapterWithBaseURL(cfg.Token, cfg.Target, cfg.BaseURL)` when `cfg.BaseURL != ""` (httptest seam) else `NewWithCreds(cfg.Token, cfg.Target)` — so a blank import self-registers the adapter. §107 evidence: `TestRegistryResolvesRegisteredChannel` proves `New` actually constructs + returns a working `Channel` (`c.Name()=="fake-rt"`, not a no-op); `TestRegistryUnknownChannelErrors` proves `errors.Is(err, ErrUnknownChannel)` for unknown names; `TestRegistryDuplicatePanics` proves the duplicate-`Register` panic; `TestTgramRegisteredViaInit` (blank import `_ ".../tgram"`) proves the `init()` actually registered `tgram` in `Names()` (NOT a no-op — verified failing before the `init()` landed: `Names()=[dup-rt fake-rt]` without tgram, PASS after). The shared `fakeChannel` test stub uses `SendReplyGeneric` (the T1-resolved interface method name, NOT `SendReply`). `go test -race ./commons_messaging/...` green (tgram regression-free); all 16 workspace modules build; `go vet ./commons_messaging/...` clean. Live runtime evidence (real channel resolved by name + round-trip) lands via Wave 7 T9 e2e invariants E81/E82. | 2026-05-27 | (this commit) | spec V3 §11.0; plan `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`; mirrors qaherald `scenario.Register`; Catalogue-Check: reuse (internal pattern, 2026-05-27) |
| HRD-110 | task | high | Wave 7 T1 — extract `commons_messaging/channels.Channel` interface (pure refactor). New package `commons_messaging/channels/` defines `Channel` (embeds the §11.0 `commons.Channel` — Name/Capabilities/Send/Subscribe/HealthCheck — plus the three inbound-runtime methods Telegram implicitly defined: `SendReplyGeneric` string-typed reply, `BotSelfIdentity` returning `SelfIdentity{Kind,Value}`, `DownloadAttachment`), the `SelfIdentity` value type, and the `IdentityKind` constants (`IdentityUsername`/`IdentityUserID`/`IdentityAddress`). `*tgram.Adapter` gains `BotSelfIdentity` (getMe via ensureBot; empty-username → §107 echo-loop-hazard error), `SendReplyGeneric` (string→int64/int adapter over the native Wave-6 `SendReply`), and a method-form `DownloadAttachment` (routes the package-level func through ensureBot's live bot) — so `var _ channels.Channel = (*tgram.Adapter)(nil)` compiles. Naming divergence from plan Step 4 text resolved + documented in `channel.go` package doc: the interface reply method is named `SendReplyGeneric` (not `SendReply`) so tgram satisfies it WITHOUT touching its native, §107-mutation-gate-anchored int64/int `SendReply` (whose migration to the generic signature is T5's scope) — keeping T1 a pure refactor with zero cross-package churn. §107 evidence: `var _ channels.Channel = (*tgram.Adapter)(nil)` compile-time assertion in `channel_test.go` (a renamed method / drifted signature breaks the build); `TestTgramSatisfiesChannel` + `TestSelfIdentityType` PASS; full `go test -race ./commons_messaging/...` green (tgram regression-free — pure-refactor proof); all 16 workspace modules build; `go vet ./commons_messaging/...` clean. Live runtime evidence (real getMe self-identity + reply round-trip) lands via Wave 7 T9 e2e invariant E81. | 2026-05-27 | (this commit) | spec V3 §11.0 + §32.2; plan `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`; Catalogue-Check: reuse (extends existing `commons.Channel` §11.0 contract, 2026-05-27) |
| HRD-101 | task | high | Wave 6.5 ticket-lifecycle layer — §32.6 deterministic classifier (`Bug:`/`Issue:`/`Task:`/`Implementation:`/`Investigation:`/`Query:`/`Question:`/`Help:`/`Status:`/`Continue:`/`Done:`/`Resolve:`/`Reopen:`/`Override:` prefix routing with `Type`+`Criticality`+`Confidence` scores; natural-language no-prefix → `query` fallback) + DocsIssueOpener (§32.9 — real `docs/Issues.md` HRD-NNN allocator, concurrent-safe via lockfile) + RunnerEventEmitter (§33.7 — direct `runner.Run` → CloudEvent dispatch, no HTTP loopback) + `Help:`/`Status:`/`Continue:` fast-path handlers (bypass Claude Code, sub-second latency — S2 proved 0.74s, no CC bootstrap overhead) + `Done:`/`Reopen:` atomic Issues↔Fixed migration (rollback-via-sha256 test) + `tgram.SendReply` outbound attachment fan-out (multipart dispatch) + wiring all of these into `pherald listen`. §107.x live evidence (`docs/qa/HRD-101-lifecycle-2026-05-23T03-16-17-w6.5live/`): S1 natural-language query — real Opus CC dispatch ~31s incl. bootstrap, threaded reply (wire-byte proof inbound `message_id=30` → reply `sent_message_id=31` with `reply_to_message_id=30`); S2 `Help:` fast-path — classifier `Type:help_command Confidence:1`, CC dispatch ABSENT (event absence + 0.74s latency prove no CC call), BuiltinHelp catalogue text returned (`reply_to_message_id=32` → `sent_message_id=33`); transcript.jsonl captures both directions of all four messages (S1 query, Help:, Status: also exercised live). Reproducible test harness `tests/test_wave6.5_lifecycle.sh` covers 15 scenarios (Bug:/Task:/Query: + attachments + Done:/Reopen: + classifier edge cases). Spec V3 r13: §32.6/§32.9/§33.6/§33.7 subsections landed. Tag `v0.5.0`. | 2026-05-23 | `17fc394` | spec V3 §32.6 + §32.9 + §33.6 + §33.7 + Herald §107.x; live evidence `docs/qa/HRD-101-lifecycle-2026-05-23T03-16-17-w6.5live/` (S1+S2 transcript.jsonl + README) |
| HRD-100 | task | high | Wave 6 — pherald inbound runtime + Claude Code headless bridge (code-doc closure). New Cobra subcommand `pherald listen` runs the closed-loop runtime: Telegram `getUpdates` long-poll (`OnText`/`OnPhoto`/`OnDocument`/`OnVoice`) + bot self-filter (`bot.Me.Username` anti-echo guard captured at Subscribe construction; refuses to start if `getMe` returned no username) + sha256 content-addressed attachment download into `~/.herald/inbox/<sha256>.<ext>` (idempotent — duplicate downloads do not re-write) + Claude Code dispatch with Opus pinned in the spawned argv (`claude --model claude-opus-4-7 --resume <UUID> --print <envelope>`) + envelope pre-text — verbatim operator wording (`"We have received new message from our communication channel <channel-name>. <classification sentence>. <attachment list>"`) preceding the `<<<HERALD-DISPATCH-v1>>>` structured block + `<<<HERALD-REPLY>>>` parser with `action` routing (`reply` default / `issue.open` / `event.emit`) + `tgram.Adapter.SendReply(ctx, chatID, text, replyToID, attachments)` wiring `reply_to_message_id` from the original `message_id`. `--qa-out-dir <path>` flag journals every inbound + outbound event as JSONL per §107.x. Closing 12 atomic Wave-6 commits T1=`ad87d7f` (HERALD_PROJECT_NAME resolver + .env.example), T2=`0d55274` (Opus model pin), T3=`14626f7` (envelope pre-text), T4=`b4018a3` (bot self-filter), T5=`d9156f1` (OnPhoto/OnDocument/OnVoice + sha256 download), T6=`a22a054` (`pherald listen` Cobra subcommand), T7=`a3b238a` (`pherald/internal/inbound/` Dispatcher + action routing), T8=`5f92f8a` (`tgram.SendReply` with `reply_to_message_id`), T9=`f7913b8` (live closed-loop e2e script `tests/test_wave6_live_loop.sh`), T10a=`520e0c6` (`--qa-out-dir` flag + JSONL journaling middleware), T11=`01bfd1a` (e2e_bluff_hunt E63-E70 invariants), T12=`96c7c6b` (Wave 6 mutation gate `tests/test_wave6_mutation_meta.sh` — 3 paired hermetic mutations). §107 evidence: 8 new e2e invariants E63-E70 land in this commit (all currently SKIP with documented reasons — E63 lifecycle / E64 bot self-filter / E65 attachment sha256 / E66 envelope pre-text / E67 Opus argv / E68 `reply_to_message_id` wire-bytes / E69 action routing issue.open / E70 live closed-loop). Conversion SKIP → PASS lands at T10b when the operator exports `HERALD_TGRAM_BOT_TOKEN` + `HERALD_TGRAM_CHAT_ID` + `HERALD_CLAUDE_BIN` and runs `bash tests/test_wave6_live_loop.sh --qa-out-dir docs/qa/HRD-100-<run-id>/`. Tag `v0.4.0` is DEFERRED to T13b (post-T10b live evidence per §107.x) — code-doc closure ships here. | 2026-05-22 | (this commit; 12 Wave-6 SHAs above) | spec V3 §32.9 + §33.6 + §33.7 + §43 catalogue + Herald §107.x; Catalogue-Check: no-match → Herald-internal package `pherald/internal/inbound/` |
| HRD-016 | task | middle | pherald `/v1/events` Runner wiring (REST API surface via Gin Gonic per spec §41) — §32 7-stage event-ingest pipeline lands as 7 concrete structs under `pherald/internal/runner/` (EventParser, IdempotencyChecker, TenantResolver, PolicyGate, SubscriberResolver, ChannelDispatcher, OutcomeRecorder) orchestrated by `runner.Runner.Run`. `pherald/internal/http/events.go` exposes the live HTTP handler; `pherald/cmd/pherald/{main,stubs}.go` lazily build the Runner + JWT verifier inside the serve subcommand so other paths still work without `HERALD_PG_DSN`/`HERALD_AUTH_MODE`. HTTP contract: 202 Accepted + Receipt JSON on fresh event, 200 + `X-Herald-Replay: true` on replay, 401 on missing/invalid JWT, 400 on parse failure. §107 evidence: 23/23 runner package tests green (per-stage + 3 integration scenarios: happy/duplicate/deny); 6 new e2e invariants E37-E42 (PG :24100 gate-skipped when absent — honest SKIP-with-reason per §11.4.3); mutation gate `tests/test_wave3_mutation_meta.sh` gains M2 (Duplicate=false unconditional → E38 breaks), M4 (skip events_processed.Insert → E38/E42 break). M3 SKIP-with-reason pending Wave 3c deny-evaluator e2e. Commits T1..T10: `eb9b2f4`, `40ad60f`, `2fdb7b5`, `4c4af24`, `3988114`, `5468897`, `6ea29a4`, `be71db1`, `c7b89a4`, (this commit). | 2026-05-22 | (this commit) | spec V3 §32.1.1 + §41 + §44.N; Catalogue-Check: no-match → vendor (Gin external; Runner stages Herald-internal per §11.4.74) |
| HRD-011 | task | middle | Telegram channel adapter live integration — telebot.v3 vendored (`submodules/telebot`); HealthCheck + Send + Subscribe + outbound_delivery_evidence persistence all live-wired with §107 bluff guards. Closed atomically with operator-session live evidence 2026-05-22: real bot delivery `message_id=5` from `@atmosphere_worker_bot` to chat `2057253161` (validated via `getChat`) using the extended wizard CLI's `--bot-token` (env: HERALD_TGRAM_BOT_TOKEN) + `--chat-id` (validated via `getChat` before persist) + `--non-interactive` flags. Token→getMe→getChat→sendMessage chain confirmed end-to-end in commit `140a2f1` session. | 2026-05-22 | (this commit) | spec V3 §11.1; Catalogue-Check: no-match → vendor telebot.v3 as submodules/telebot per §11.4.74; live evidence: wizard run 2026-05-22 + Telegram Bot API `sendMessage` returning `ok=true, message_id=5` |
| HRD-099 | task | low | commons_auth/ JWT verifier — HMAC + JWKS hybrid + Gin middleware + claims helpers, vendored Herald-internal per §11.4.74 catalogue-check no-match. Catalogue evidence: `docs/catalogue-checks/HRD-099-commons-auth.md`. Renumbered from HRD-093 (Wave 3a executing-time anchor) in the post-Wave-3a doc-cleanup pass; Wave 3a commits `dbbea95..0cc6fad` keep the old HRD-093 in their commit messages as history. | 2026-05-21 | dbbea95 (impl) + r9 doc-cleanup (renumber) | spec V3 §41 + Wave 3 design §4; docs/catalogue-checks/HRD-099-commons-auth.md |
| HRD-098 | task | middle | sherald `/v1/safety_state` live — process-local Aggregator + background mem-sampler (15s tick) + Handler with JWT gate via commons_auth.GinMiddleware. Replaces the Wave 2 501-stub. §107 evidence: e2e E46 (no-auth → 401), E47 (valid HMAC → 200 + current_mem_percent>0 + last_destructive_op=null + uptime_seconds>=1 + open_events=0). Paired mutation M5 (zero-mem Aggregator) proves E47 catches the regression. E48 SKIP-with-reason — destructive-op trigger awaits HRD-033 body. | 2026-05-21 | (this commit) | spec V3 §18.0 + §41 + §44.M; e2e E46-E47 |
| HRD-028 | task | low | cherald `/v1/compliance` live — paginated + filter-aware `constitution_state` pull surface with JWT gate via commons_auth.GinMiddleware. Backed by ConstitutionStore.ListQuery extended with Since/Until/Offset (commit 6276437). §107 evidence: e2e E43 (no-auth → 401), E44 (valid HMAC + empty tenant → 200 + total=0 + page=1 + page_size=50). E45 SKIP-with-reason — cross-binary cherald-reads-pherald-writes deferred to Wave 3b (Runner not live yet). | 2026-05-21 | (this commit) | spec V3 §42.1.5 + §41 + §44.M; e2e E43-E44 |
| HRD-092 | task | middle | commons/cli/ shared CLI scaffold (Wave 2) — NewRootCmd + VersionCmd + ServeCmd (with Middleware hook) + StubCmd + healthz/readyz/metrics handlers. Vendored as Herald-internal per §11.4.74 catalogue-check no-match. As-built evidence: §44.M of spec V3 r8 + docs/catalogue-checks/HRD-092-commons-cli.md. | 2026-05-21 | (this commit) | spec V3 §44.M; Catalogue-Check: no-match → vendor |
| HRD-093 | task | middle | sherald flavor scaffold — System Herald binary serving :24793 with cli.ServeCmd, 5 §43 stubs (HRD-033/034/040/046/056) + /v1/safety_state 501 stub → HRD-098. | 2026-05-21 | (this commit) | spec V3 §18.0 + §44.M; Catalogue-Check: no-match → vendor |
| HRD-094 | task | middle | cherald flavor scaffold — Constitution Herald binary serving :24792 with cli.ServeCmd, 11 §43 stubs + /v1/compliance 501 stub → HRD-028. | 2026-05-21 | (this commit) | spec V3 §18.0 + §44.M; Catalogue-Check: no-match → vendor |
| HRD-095 | task | low | bherald flavor scaffold — Build Herald CLI-only binary with 3 §43 stubs (HRD-035/041/045). | 2026-05-21 | (this commit) | spec V3 §18.0 + §44.M; Catalogue-Check: no-match → vendor |
| HRD-096 | task | low | rherald flavor scaffold — Release Herald CLI-only binary with 3 §43 stubs (HRD-031/032/045). | 2026-05-21 | (this commit) | spec V3 §18.0 + §44.M; Catalogue-Check: no-match → vendor |
| HRD-097 | task | low | iherald + scherald flavor scaffolds — Incident Herald serving :24794 with /v1/webhooks/page → HRD-024 + Scheduled-audit Herald CLI-only with 1 §43 stub (HRD-047). Paired since iherald has zero §43 stubs. | 2026-05-21 | (this commit) | spec V3 §18.0 + §44.M; Catalogue-Check: no-match → vendor |
| HRD-012 | task | middle | Claude Code dispatcher live integration — `claude --resume <UUID> --print <envelope>` exec + `<<<HERALD-REPLY>>>` JSON parse + `claude_code_sessions` persistence per §33. §107 evidence: Plan 2 Task 6 commit `702b5a3` (live PASS 24.24s — real claude CLI round-trip, structured reply parsed, Outcome+Summary non-empty) + Task 7 commit `4718c0e` (live PASS 36.23s — session_state upsert under HeraldSystemTenant with exact-equality assertions on session_uuid + anchor_path + last_response JSONB round-trip). HRD-085..HRD-090 stay open for upstream-defined TaskRepository methods not exercised by the Dispatch+session hot path. | 2026-05-21 | (this commit) | spec V3 §33 + §33.2; Catalogue-Check: no-match → `claude` is external binary not library; extend digital.vasic.database@<pinned> for live pool. |
| HRD-010 | task | middle | commons_storage live wiring — pgx pool + RLS-enforcing WithTenantContext (discovered + fixed RLS-bypass bug via E14 falsifiability) + 9 migrations + background queue (digital.vasic.background bound via pgxTaskRepository) + Redis ACL (digital.vasic.cache) + pherald migrate up/status/down/validate subcommand + 3 new §107 e2e invariants (E14/E15/E16) + HRD-085..090 registered for queue-repository stubs | 2026-05-20 | (this commit) | spec V3 §9.6 + §16; Catalogue-Check: extend digital.vasic.database@<pinned> + digital.vasic.background@2d46dd60 + digital.vasic.cache@<pinned>; Models + Concurrency submodules added |
| HRD-080 | task | low | Refine I6 inheritance-gate invariant from blanket `.gitmodules`-forbidden to "no `constitution/` entry in `.gitmodules`" — paired meta-test `test_i6_refinement_meta.sh` with 3 anti-bluff subtests. Enables Foundation M2/M3 Helix-stack submodule installs. | 2026-05-20 | (this commit) | spec V3 §44.9 |
| HRD-017 | task | middle | Propagate Universal §11.4.73 (main-spec versioning + revision discipline) and §11.4.74 (submodule-catalogue-first discovery) into parent constitution Constitution.md + CLAUDE.md + AGENTS.md | 2026-05-20 | constitution `34a82b3` | Universal §11.4.73, §11.4.74 |
| HRD-014 | task | middle | pherald CLI scaffold — Cobra root + version + 5 stubbed subcommands; `pherald version --json` returns canonical build info | 2026-05-20 | `e627c76` | spec V3 §3 + §18.2 |
| HRD-013 | task | middle | commons_messaging + null:// adapter — full §11.0 Channel contract impl with ring buffer + fail_rate/latency/ceiling URL params + 8-case unit test suite | 2026-05-20 | (this commit) | spec V3 §11.14 |
| HRD-009b | task | low | commons_prefix module — §8.2 deterministic 3-letter prefix algorithm with CamelCase split + collision-resolution via fnv1a32 + table-driven tests | 2026-05-20 | (this commit) | spec V3 §8.2 |
| HRD-009 | task | middle | commons module — full §11.0 Go type contract (Channel + Capabilities + DeliveryEvidence + OutboundMessage + Body + Attachment + Recipient + Action + Priority + Receipt + InboundHandler + InboundEvent + Subscriber + SubscriberAlias + CloudEventEnvelope + TraceContext + Branding + ChannelID + PreferenceSet + …) + Clock abstraction + UUIDv7 helper + DefaultBranding factory | 2026-05-20 | (this commit) | spec V3 §11.0 + §3.5 + §4.3 + §6.3 |
| HRD-007 | task | middle | V3 r3 cross-doc sync + tracking-doc scaffold (Issues/Fixed/Status/Status_Summary/Issues_Summary/Fixed_Summary/CONTINUATION) | 2026-05-20 | `741cccd` | spec V3 §30.8 |
| HRD-006 | task | middle | V3 r2 flavor refinement — 9 flavors × per-channel interaction tables | 2026-05-20 | `f8b8073` | spec V3 §30.7 |
| HRD-005 | task | middle | V3 r1 operator-product layer (§31..§36 + §18.2 expansion) | 2026-05-20 | `e26a8dc` | spec V3 §30.6 |
| HRD-004 | task | middle | V2 r3 — Go type contract closure + operational ops detail | 2026-05-20 | `f4ebba1` | archived V2 §30.5 |
| HRD-003 | task | middle | V2 r2 — close prose↔definition gaps + add operational guidance | 2026-05-19 | `9648545` | archived V2 §30.1 |
| HRD-002 | task | middle | V2 r1 — architectural authoring (CloudEvents/Watermill/OTel/RLS/SLSA/9 flavors) | 2026-05-19 | `96b7cc6` | archived V2 §29 |
| HRD-001 | task | middle | V1 — initial MVP specification + Review section + Recommendations | 2026-05-19 | `b421fe1` | archive V1 §"Review" |
