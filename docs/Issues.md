<div align="center">

<img src="../assets/logo/herald_logo_square_128.png" alt="Herald" width="96" height="96" />

</div>

# Herald — Issues

| Field | Value |
|---|---|
| Revision | 41 |
| Created | 2026-05-20 |
| Last modified | 2026-05-29 |
| Status | active |
| Status summary | **r41 (ATMOSphere↔Herald integration Phase 2 CORE landed + pushed; remaining HRDs externally gated). LANDED + anti-bluff-tested + pushed:** HRD-148 `commons_workable` (SQLite store+CRUD+ATMOSphere parser+diff), HRD-149 `commons_watch` (fsnotify+WAL-poll), HRD-151 workflow change→CloudEvent→Runner bridge + diff formatter, HRD-152 inbound item.update/item.delete/investigation.start + ItemMutator + CONFIRM flow (wired into `pherald listen --db`), HRD-153 `pherald watch` end-to-end daemon (real-SQLite+fsnotify+diff+dispatch e2e), HRD-154 preference/quiet-hours routing (TZ-aware), e2e invariant E139, `docs/guides/WORKABLE_ITEMS_INTEGRATION.md` (+siblings), `docs/qa/HRD-153-*` evidence. **EXTERNALLY GATED (need operator/remote/cross-repo):** HRD-156 live Telegram round-trip (MTProto credential bootstrap), HRD-150/HRD-155 operationalize the constitution workable-items tool + SSoT materialization (HelixConstitution cross-repo), HRD-157 Phase 3 remote integration (register `tools/herald` + materialize `docs/workable_items.db` + live evidence on the ATMOSphere host), HRD-158 covenant QWEN.md propagation (push to HelixConstitution — high blast radius, operator confirmation required). Prior r40 (ATMOSphere↔Herald integration Phase 2 kickoff — filed HRD-148..HRD-158, the work-stream HRDs from `~/Documents/ATMOSphere_Herald_Integration_Plan.md`). HRD-148 (`commons_workable` SQLite SSoT foundation) + HRD-149 (`commons_watch` watcher) LANDED + `-race` green (in_progress until the full watch→notify feature + e2e land). Count 32→43.** Prior r39 (§11.4.19 atomic migration: HRD-090 dead-letter handling Completed→`Fixed.md` — all four closure layers met: code + unit + real-PG integration + §11.4.85 stress/chaos + paired §1.1 mutation gate (4/0) + e2e E138; HRD-147 filed for the governance-plane subscriber follow-up). Issues count steady at 32 (−HRD-090 +HRD-147).** Prior r38 (HRD-146 filed — `pherald listen` dispatch hardening, from the 2026-05-29 "worker OOM" Phase-1 investigation that found the OOM premise did NOT match committed reality; the real latent risk is unbounded per-message dispatch growth — `docs/research/oom-investigation-2026-05-29.md`).** Prior r37 (HRD-090 dead-letter handling — code + unit + real-PG integration landed; stays in_progress pending §11.4.85 stress+chaos + paired §1.1 mutation gate + e2e invariant).** `MoveToDeadLetter` implemented (migration `000014_dead_letter_tasks`, tx-atomic JSONB-snapshot move + terminal-mark, new 13th operational event class `ClassQueueDeadLetter`/`QueueEvent`/`DeadLetter()` wired nil-tolerantly into `pgxTaskRepository`); 3 hermetic unit tests + `TestRepoMoveToDeadLetter_RoundTripAndEmit` real-PG integration (3 independent sinks: source terminal-state + dlq snapshot + real-bus event delivery) `-race` green; evidence `docs/qa/HRD-090-20260529T052228Z/`. NOT migrated to Fixed — closure bar (CONST-048 four-layer) not yet met. Prior r36 (§11.4.90 Obsolete graduation of 3 legacy NON-COMPLIANT live tests + Wave 8 Track B closure recorded against Fixed.md).** Three legacy live tests that fundamentally required manual operator action during execution graduate to `Obsolete` status per Universal §11.4.90 + Herald §108.e, with full **Obsolete-Details** lines (Since / Reason=superseded-by-later-mandate §11.4.98 / Superseding-item + triple-check evidence per §11.4.6 no-guessing): HRD-143 supersedes `TestSubscribe_LiveBotAPI` via HRD-140; HRD-144 supersedes `tests/test_wave6_live_loop.sh` via HRD-141; HRD-145 supersedes `qaherald lifecycle --manual` via HRD-142. Each Obsolete row cites a literal source-comment quote / file size / Cobra-flag line confirming the supersession is grounded in code that still exists, never a bare assertion. v0.6.0 tag (`844f340`) already covers HRD-139..142 in Fixed.md r17. Prior r35 (Tgram-completeness Wave C — HRD-137 §11.4.85 stress+chaos COMPLETE — SKIP→PASS conversion + atomic Issues→Fixed migration of all 6 tgram-completeness HRDs per §11.4.19).** Wave C closes the 6-HRD tgram-completeness wave (HRD-133..138). `TestTgram_Stress_MultiRecipientFanOut` already PASS (Wave-B-era multi-recipient fan-out stress). `TestTgram_Chaos_GetUpdatesPollerResilience` converted from `t.Skipf` SKIP-with-reason to real httptest-driven fault injection via a one-line `subscribe.go` seam fix (threads `a.baseURL` into `telebot.Settings.URL` before `NewBot` — parallel to the `ensureBot` pattern at `tgram.go:60-67`; production path unchanged when `a.baseURL == ""`). 8-phase fault matrix (3× 500 + 2× 1.5s hangs honoring ctx-cancel + 1× mid-body Hijacker connection-close + 2× success), bot-username-aware self-filter bypass (success messages come `from @someuser`, not `chaostestbot`), assertion thresholds chosen deterministic-first: `recoveryFloor=3` (observed `recovery_event_count=6` every iteration), `goroSlack=16`, `settleWaitMs=2000`. Deterministic across `-race -count=3` per §11.4.50 — all 3 iterations IDENTICAL: `recovery=6 fault(500=12 hang=8 close=3 ok=6) goroutines 3→4 (leaked=1) duration=12.07s`. Full tgram suite **26/26 PASS, 0 SKIP** post-fix (was 24/24 with chaos SKIPped). Evidence `docs/qa/HRD-137-20260528T150000Z/stress_chaos/` (anchor preserved verbatim, §2 SKIP→PASS section appended, chaos_assertion.txt rewritten with measured PASS-state values). §11.4.92 5-pass: P1 captured-evidence per-iteration table; P2 blast-radius — `subscribe.go` baseURL seam is read-only when unused (production unaffected); P3 cross-feature — the seam mirrors existing `ensureBot` pattern, no novel state; P4 deep-research — telebot.v3 `LongPoller` retry behavior under fault (poller logs + continues, no internal retry); P5 anti-bluff — handlerHits, fault-counter atomics, ctx-aware subscribe return, no goroutine leak (delta=1) are all positive captured invariants from REAL Subscribe poller, not synthetic. **Atomic migration:** HRD-133, HRD-134, HRD-135, HRD-136, HRD-137 (this), HRD-138 ALL move Issues→Fixed in the same commit per §11.4.19 (Wave A/B previously parked them in_progress in Issues.md awaiting Wave C closure — now landed together). **Prior r34 (Tgram-completeness Wave B combined — HRD-133 token-redaction + HRD-134 429 retry_after) marks BOTH Open→in_progress; Wave B is now CLOSED (5 of 6 tgram-completeness tasks done).** Both HRDs land in ONE commit because they share `commons_messaging/channels/tgram/send.go`'s error-and-retry paths atomically — splitting them would require partial-file commits with messy `git add -p` (precedent: C4+C5 / D-stragglers were the same shape). **HRD-133** ships `sanitizeTgramError` (new `errors.go`) wrapping 6 error-return sites in Send + SendReply so the Bot API token NEVER appears in any error / log message; `TestTgram_Send_ErrorDoesNotLeakToken` plants `123456:supersecret-must-not-leak-9999` + asserts absence (mirrors `TestSlack_Send_ErrorDoesNotLeakToken`). **HRD-134** ships `withRetry429` wrapping all 3 telebot `bot.Send` call sites; on `429 + retry_after=N` sleeps N seconds (`ctx.Done()`-aware), retries up to 3 attempts, clamps per-retry to 60s; `retry_after` extracted via `errors.As(err, &telebot.FloodError{}).RetryAfter` (telebot's typed error — no body parse). Composition: the retry's log lines + final error are themselves sanitized so 429 logging never leaks the token. Injectable `sleeper` field on `Adapter` for hermetic tests. 7 new tests `-race -count=1` (2 HRD-133 + 5 HRD-134) → tgram suite now 24/24 PASS. Parts 4+5 of 6 in the tgram-completeness wave. **Next: Wave C HRD-137** (multi-recipient fan-out stress + getUpdates resilience) on a now-stable send.go. **Prior r33 (Tgram-completeness Wave A SA-c — HRD-138 operator deep-dive guide) marked HRD-138 Open→in_progress and closed Wave A (3 of 6 tgram-completeness tasks).** `docs/guides/TELEGRAM.md` (810 lines, 11 §-sections, 58 subsections + 3 siblings .html/.pdf/.docx) is the per-module nano-detail operator guide called for by the operator memory `feedback_nano_detail_docs`. Sections: §1 architecture; §2 BotFather walkthrough; §3 env table + 3 chat_id discovery methods; §4 pre-deploy smoke-test commands; §5 attachment size limits + content-addressed inbox; §6 chat-type matrix (private/group/supergroup/channel id formats + bot permissions); §7 reply threading + attachment-fan-out rationale; §8 self-filter + echo-loop hazard (Wave 6 T12 M1); §9 12-entry troubleshooting cookbook (401/400/429/413/403/echo-loop/long-poll/409/privacy/webhook/zero-byte/wrong-inbox); §10 6-subsection pre-deploy audit (tick-boxes); §11 references (spec V3 + 13 HRDs + source paths). Cross-linked from the pre-existing `docs/guides/messengers/TELEGRAM.md` (HRD-011 setup, preserved untouched). Part 3 of 6 in the tgram-completeness wave — Wave A complete (HRD-135 + HRD-136 + HRD-138). **Next: Wave B HRD-133 + HRD-134** (combined subagent — both touch send.go: token-redaction symmetry + 429 retry_after honored), then **Wave C HRD-137** (multi-recipient stress+chaos). **Prior r32 (Tgram-completeness Wave A SA-b — HRD-136 webhook validator skeleton) marked HRD-136 Open→in_progress.** New `commons_messaging/channels/tgram/webhook.go` lands the Bot API webhook `secret_token` validation layer for the future V1.x webhook-path operator (spec V3 §11.1 alternative to `getUpdates`). `WebhookHandler` validates the `X-Telegram-Bot-Api-Secret-Token` header via `crypto/subtle.ConstantTimeCompare` (timing-attack-safe), refuses empty secret at construction (`NewWebhookHandler("",…)` errors — §107 footgun guard), returns 401 with EMPTY body on header mismatch/missing (no token-existence-oracle leakage to probes), 400 on malformed JSON, 405 on wrong method, 500 (no body) on InboundHandler error. Decoded `tele.Update` is forwarded as the SAME `InboundEvent` shape `OnText`/`OnEdited` produce — unified downstream contract. **Skeleton-only:** wiring into `pherald listen --webhook` + `setWebhook(secret_token=…)` registration + TLS-bound public listener deferred to V1.x; the cryptographic layer is here + tested so the future flip is one-flag. 7/7 hermetic httptest tests `-race`: ValidSecret_Dispatches, InvalidSecret_Returns401_NoTokenLeak, MissingSecret_Returns401, GET_Returns405, MalformedJSON_Returns400, EmptySecretAtConstruction_ErrorsLoud, ConstantTimeCompare. Part 2 of 6 in the tgram-completeness wave. **Prior r31 (Tgram-completeness Wave A SA-a — HRD-135 lifecycle handlers) marked HRD-135 Open→in_progress.** `commons_messaging/channels/tgram/subscribe.go` gains 3 new event handlers per the operator's tgram-completeness mandate (Section 2 of the 2026-05-28 live-gap analysis): **OnEdited** rebuilds the same `InboundEvent` shape `OnText` does + `Raw["edited"]=true`; **OnMyChatMember** detects bot kicked/left/restricted status transitions (with Alice-left and promotion-to-Admin false-positive guards) + emits a grep-able `log.Printf` carrying `ErrBotKicked` text + the chat_id; **15 service-message endpoints** (UsersJoined/UserLeft/NewGroupTitle/etc.) registered to a single `serviceHandler` that emits nothing to `InboundHandler` but logs `tgram: ignored service message kind=… chat_id=…`. Sentinel `var ErrBotKicked` package-level so callers can `errors.Is`. 17/17 PASS `-race -count=1` (3 new in `subscribe_lifecycle_test.go` + 14 existing, zero regression). Part 1 of 6 in the tgram-completeness wave. **Prior r30 (v0.6.0 Wave 7 T12 — operator guide; Wave 7 CODE-COMPLETE) marked HRD-121 Open→in_progress.** `docs/guides/MESSENGER_CHANNELS.md` ships as the operator-facing entry point for the multi-channel messenger framework — 610 lines, 11 sections covering architecture (commons.Channel outbound vs commons_messaging/channels.Channel inbound; ASCII fan-in diagram), HERALD_CHANNELS selector (fail-loud on unknown), Telegram + Slack live-setup walkthroughs (BotFather + api.slack.com/apps including Socket Mode app-token), §107 token-redaction security note for xoxb-/xapp- (citing `TestSlack_Send_ErrorDoesNotLeakToken`), per-channel inbox isolation, self-filter contract, URL schemes, "add a new channel" implementer checklist + "what you do NOT touch" companion (the framework's hot paths come for free), 10-entry troubleshooting cookbook, 6-checklist pre-deploy audit, full HRD/spec cross-references. Generated 3 siblings (.html/.pdf/.docx) via `REGEN_ALL=1 scripts/export_docs.sh`. **Wave 7 (T1-T12) is now code-complete:** HRD-110..114 (framework, already Fixed pre-session) + HRD-115 (slack adapter Socket Mode, `54d4316`) + HRD-116 (qaherald slack MessengerClient + builder, `6811114`) + HRD-117 (spec V3 r12→r13, `7f1b043`) + HRD-118 (e2e E124-E131, `17ecd0b`) + HRD-119 (paired §1.1 mutation gate 4/0, `6737040`) + HRD-120 (multi-channel §11.4.85 stress+chaos 12/12, `86dfa1e`) + HRD-121 (this row). Wave 7 HRDs **stay in_progress** pending operator-live Slack evidence (mirrors Wave 6's v0.4.0 deferred-to-T13b pattern); v0.6.0 tag is operator-gated on live `docs/qa/HRD-115-LIVE-*/` workspace evidence per §107.x. **Prior r29 (v0.6.0 Wave 7 T11 — multi-channel §11.4.85 stress+chaos) marked HRD-120 Open→in_progress.** Extended `pherald/cmd/pherald/listen_stress_chaos_test.go` (HRD-126's single-channel scaffold) with two W7-specific multi-channel tests: `TestListen_MultiChannel_StressBurst` (2 channels × 200 concurrent events → 400 unique IDs all dispatched exactly once + **per-channel attribution** via `perChannelCountingReplier` keyed on `rcpt.Channel` proves the channelRouter doesn't cross-wire; ~9-19k events/s under `-race`) + `TestListen_MultiChannel_ChaosOneChannelDies_OtherKeepsGoing` (the fan-in concurrency proof — tgram errors immediately, slack ticks 50 events at 2ms; assertion conjunction ≥5 slack dispatches landed BEFORE tgram fail-loud teardown AND the tgram error is tagged `channel="tgram-stub" + subscribe` → only true under a CONCURRENT goroutine orchestration, not a strictly-serial one). **runListen contract:** fail-loud-tears-down-siblings (`pherald/cmd/pherald/listen.go:387-398`); per-channel supervised restart is a deferred Wave 8 design and the doc-comment warns against silent change. 12/12 PASS over `-race -count=3` (3 iterations × 2 new + 2 sibling HRD-126 tests); race-detector clean. Evidence anchors `multichannel_no_drop_under_burst=1` (stress) + `multichannel_concurrency_proven=1` (chaos) at `docs/qa/HRD-120-20260528T110000Z/`. **Prior r28 (v0.6.0 Wave 7 T10 — paired §1.1 mutation gate) marked HRD-119 Open→in_progress.** `tests/test_wave7_mutation_meta.sh` ships 3 paired hermetic mutations against the Wave 7 multi-channel framework — **M1** blank `IsSelfEcho` (echo-loop hazard), **M2** drop slack `channels.Register` in `init()` (registry-resolution broken), **M3** drop slack `thread_ts` append (reply threading degrades) — each mutated build observed FAIL on the matching e2e detector + restored byte-for-byte + post-restore PASS. New `TestSlackRegisteredViaInit` detector added to `slack_test.go` (it asserts `channels.Names()` contains `"slack"` — the only test that would FAIL under M2 specifically; without it M2 would silently "no tests to run" PASS-bluff). Gate run = **4 PASS / 0 FAIL** (3 mutations + 1 quiescence assertion, no `MUTATED W7-` residue in 3 production target files post-restore). Reuses Wave 6 canonical lockfile + trap + assert_restored + pre-flight guard verbatim. **FOREGROUND-ONLY** (§107.y / `feedback_no_concurrent_mutation_gates` operator mandate). **Prior r27 (v0.6.0 Wave 7 T9 — e2e multi-channel invariants) marked HRD-118 Open→in_progress.** `scripts/e2e_bluff_hunt.sh` gains **E124-E131** (8 invariants anchored to live test names already shipped in HRD-110..117): E124 channels registry (resolve + unknown-errors), E125 tgram self-filter post-Wave-7 generalization (`TestSubscribeBotSelfFilter`), E126 slack adapter (TestSlackSatisfiesChannel + auth.test + chat.postMessage + registry wiring + sc_anchor to T6 docs/qa), **E127 LIVE Slack** triple-guarded (`HERALD_SLACK_BOT_TOKEN` + `HERALD_SLACK_CHANNEL_ID` + `grep TestSlack_Live_Send` — the third guard prevents the §107 "no tests to run = exit 0 = PASS-bluff" gotcha when env is set but no live-test exists), E128 per-channel inbox isolation + content-addressed idempotency, E129 generalized IsSelfEcho (Username + UserID + empty-self echo-loop hazard), E130 multi-channel pherald listen wiring (`TestListenWiresHandlerAndExitsOnCancel`), E131 qaherald messenger.Build dispatch (tgram + slack + unknown-errors-loud + sc_anchor to T7 docs/qa). **Prior r26 (v0.6.0 Wave 7 T8 — spec V3 r12→r13) marked HRD-117 Open→in_progress.** Spec edits document the already-shipped Wave 7 framework + Slack: §11.0 gains the inbound-runtime extension note (`commons_messaging/channels.Channel` embeds `commons.Channel` + `SendReplyGeneric` + `BotSelfIdentity` + `DownloadAttachment` + `init()`-registered registry + generalized `IsSelfEcho` via `SelfIdentity{Kind: IdentityUsername|IdentityUserID|IdentityAddress, …}` + per-channel `~/.herald/inbox/<channel>/<sha256>.<ext>`); §32.2 marks Telegram + Slack **LIVE** (Wave 6/7 + Wave 7 T6 Socket Mode); §43 catalogue gains the multi-channel `pherald listen` row (`HERALD_CHANNELS=tgram,slack`, per-channel namespaced env, one Subscribe goroutine per enabled channel into one dispatcher). NO new product code — spec catches up to shipped reality (T1-T7 already landed). Per plan §T8 in `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`. **Prior r25 (v0.6.0 Wave 7 T7 — qaherald Slack MessengerClient) marked HRD-116 Open→in_progress.** `qaherald/internal/messenger/{slack.go,builder.go}` ships a raw-HTTP Slack client (mirroring `telegram.go`'s design — NOT the slack-go SDK; offset/watermark bookkeeping is qaherald-controlled) implementing all 11 `MessengerClient` methods + a channel-keyed `Build(BuildConfig)` dispatcher. Key Slack-specific design calls: `GetUpdates` via `conversations.history`+`oldest=<ts>` watermark (Slack has no `getUpdates` equivalent); `MessageID` = dotted-float ts string (e.g. `"1654.0001"`); `slackResponse.Channel` is `json.RawMessage` because Slack's `channel` field is a string in `chat.postMessage` but an object in `conversations.info`. **§107 security:** the xoxb token NEVER appears in errors — `sanitizeError()` defends + `TestSlack_Send_ErrorDoesNotLeakToken` pins the redaction by planting `xoxb-SUPERSECRET-must-not-leak-7777` and asserting it is absent from the resulting error string. 30 hermetic httptest tests `-race` green (17 new + 13 pre-existing tgram tests still green — zero regression); compile-time interface pin `var _ MessengerClient = (*SlackClient)(nil)`. KNOWN-FUTURE-CLEANUP: ships `files.upload` V1 (deprecated by Slack 2025-Q1 in favor of two-step V2 flow) — documented as a swappable internal in `slack.go`'s file-doc header. **Prior r24 (v0.6.0 Wave 7 T6 — Slack channel adapter) marked HRD-115 Open→in_progress.** Vendored `submodules/slack-go` @ v0.16.0 (commit `209ec09`) + `commons_messaging/go.mod` `replace`. `commons_messaging/channels/slack/` (slack.go / healthcheck.go / selfidentity.go / send.go / attachments.go / subscribe.go) implements `channels.Channel` (the Wave 7 interface HRD-110 defined): `Send` (chat.postMessage), `SendReply`(thread_ts) + `SendReplyGeneric` (interface delegate w/ files.uploadV2 attachment fan-out), `Subscribe` (Socket Mode loop), `BotSelfIdentity` (auth.test → IdentityUserID, cached), `DownloadAttachment` (files.info → content-addressed `inbox/slack/<sha>.<ext>` via `channels.WriteContentAddressed`), `HealthCheck`. URL scheme `slack://<bot-token>@<workspace>/<channel-id>[?app_token=…]` (bot-token + channel-id mandatory; app_token required only for Subscribe). 22 hermetic httptest tests (33 incl. subtests) `-race` green; inheritance gate 15 PASS / 0 FAIL post-`.gitmodules`-edit; `TestSlackSatisfiesChannel` confirms `var _ channels.Channel = (*slack.Adapter)(nil)` compiles. Per plan §T6 in `docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md`. **Prior r23 (v1.0.0 Batch D — commons_infra pgxTaskRepository) marked HRD-085/086/087/088/089 Open→in_progress, REAL-PG verified.** Implemented 16 of 17 previously-`ErrUnsupported` `pgxTaskRepository` methods (`commons_infra/task_repository.go`): HRD-085 GetByID/Update/Delete (soft-delete), HRD-086 UpdateStatus/UpdateProgress/UpdateHeartbeat/SaveCheckpoint, HRD-087 GetByStatus/GetPendingTasks/CountByStatus/GetTaskHistory, HRD-088 GetStaleTasks/GetByWorkerID, HRD-089 SaveResourceSnapshot/GetResourceSnapshots + new migration `000013_task_resource_snapshots`. (`MoveToDeadLetter` remains `ErrUnsupported` → HRD-090, by design.) **§107 real-PG evidence:** all 14 integration round-trips persist+read-back against live herald-postgres :24100 (`go test -tags=integration -run TestRepo ./commons_infra/...` → 14 PASS / 0 FAIL); transcript `docs/qa/HRD-085-089-20260528T070000Z/integration_realpg.log`; e2e **E123** wired (PG-gated, SKIP-with-reason when :24100 absent). The real-PG run caught + fixed a latent pre-Batch-D bug: `LogEvent` passed marshalled JSON as `[]byte` (encoded as bytea → PG rejects bytea→jsonb, SQLSTATE 22P02) — now `string(payload)`, matching `Create`'s cast pattern. **Prior r22 (v1.0.0 Batch C — §43 catalogue COMPLETE) marked the two stragglers HRD-034 (`sherald backup-snapshot`, §9.3 hardlinked snapshot) + HRD-047 (`scherald status-digest`, §11.4.45 Status.md sweep) Open→in_progress.** With C1-C5 + these two, the ENTIRE §43 command catalogue (HRD-029..056, 30 commands across pherald/sherald/cherald/rherald/bherald/scherald) is implemented + e2e-proven (E97-E122). backup-snapshot creates `os.Link` hardlinks (inode-verified) → `.gate.recovered`; status-digest sweeps the status doc → `.policy.violation` on stale/missing. Hermetic `-race` green; e2e **E121-E122** wired. **Prior r21 (v1.0.0 Batch C cluster C3 — cherald, COMPLETE) marked the 11 cherald §43 docs/compliance commands (HRD-036/037/038/039/042/048/050/051/052/054/055) Open→in_progress + landed the §107 cherald lazy-verifier fix.** C3a docs-pipeline (`cherald/cmd/cherald/docops_cmds.go`): `docs-sync` (§11.4.61), `readme-sync` (§11.4.59), `export` (§11.4.65 wrapping `scripts/export_docs.sh`), `fixed-summary-sync` (§11.4.53), `fixed-align` (§11.4.53 drift-detect) — detect-by-default + `--apply`. C3b verify/check (`cherald/cmd/cherald/checkops_cmds.go`): `submanifest-verify` (§11.4.31), `composite-gate` (§11.4.60), `spec-version-check` (§11.4.73 `checkSpecDrift`), `catalogue-check` (§11.4.74 `checkCatalogueMiss`), `script-docs-check` (→§11.4.60, §11.4.62 has no cherald rule), `creds-scan` (→§11.4.10 credential-leak; findings REDACTED — no secret value ever printed, per the no-leak mandate). All feed the HRD-019 cherald binding (`markerCheck` keys on `Subject.Kind`); the §107 fix mirrors sherald (verifier built lazily inside `serve`, so `cherald version` + the §43 CLI run bare without a JWT secret). Hermetic `-race` green; e2e **E110-E120** wired, each anchored to its `docs/qa/HRD-0NN-20260528T050000Z/transcript.txt`. With C1-C5 landed, the §43 command catalogue (HRD-029..056) is implemented except HRD-034 (sherald backup-snapshot) + HRD-047 (scherald status-digest). **Prior r20 (v1.0.0 Batch C clusters C4+C5) marked HRD-031/032/045 (rherald §43 release) + HRD-041/035 (bherald §43 build/CI) Open→in_progress.** rherald (`rherald/cmd/rherald/release_cmds.go`, HRD-022 binding): `tag-mirror` (§4 multi-mirror tag-parity → `.release.gate_blocked`), `changelog-generate` (§5 Conventional-Commits → `docs/changelogs/<v>.md`), `gate-retest` (§11.4.40 pre-tag full-suite retest gate) — reuse the shared `commons/gitops` (`TagExists`/`RemoteHasTag`/`LogSubjects`). bherald (`bherald/cmd/bherald/build_cmds.go`, HRD-021 binding): `test-tier-verify` (§11.4.27 8-tier matrix), `evidence-capture` (§11.4.2 anti-bluff recorded-evidence). Each DETECTS+classifies+exits-non-zero; all hermetic `-race` green; e2e **E105-E109** wired, each anchored to its `docs/qa/HRD-0NN-20260528T040000Z/transcript.txt`. **Prior r19 (v1.0.0 Batch C cluster C2) marked HRD-033/040/046/056 (sherald §43 system/safety commands) Open→in_progress + landed a §107 lazy-verifier usability fix.** The four sherald §43 commands landed as PRE-FLIGHT GATES (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding — `destructive-guard` (§9.1 rm/reset/force-push), `constitution-pull` (§11.4.26/.32 fetch+rebase+validate → `.bundle.updated`), `force-push-gate` (§11.4.41 merge-first+session-auth), `mem-budget-watch` (§12.6 60%-ceiling → `.host.safety_breach`); each DETECTS+classifies+exits-non-zero, never performs the op; reuse the shared `commons/gitops` + `commons/stresschaos`; hermetic `-race` green. **§107 fix:** sherald's `main.go` now builds the JWT verifier LAZILY inside `serve` (mirroring pherald) so the §43 CLI commands run bare without `HERALD_AUTH_MODE` — previously EVERY subcommand (even `sherald version`) died with "build verifier: HERALD_AUTH_MODE must be set", a §107 usability bluff (the cmd "exists" but no operator could run it). Also promoted `pherald/internal/gitops` → `commons/gitops` (commit `4a6b0ec`) so C2-C5 reuse the git primitives, and root-caused a flaky e2e E53 (cross-call byte-diff on the dynamic `/v1/safety_state` body). e2e: E31 rewritten (real HRD-033 §9.1 gate + bare-CLI §107 proof), **E101-E104** wired (destructive-guard / constitution-pull / force-push-gate / mem-budget-watch), each anchored to its `docs/qa/HRD-0NN-20260528T030000Z/transcript.txt`. **Prior r18 (v1.0.0 Batch C cluster C1) marked HRD-029/030/043/044/049/053 (pherald §43 gitops command catalogue) Open→in_progress.** The six commands landed (commit `502ebf6`) as `commons/gitops/{gitops.go,upstreams.go}` (promoted from `pherald/internal/gitops` to the L0 commons module so C2-C5 flavors can reuse the git primitives — §11.4.74) + `pherald/cmd/pherald/gitops_cmds.go`: `commit-push` (single locked entrypoint + multi-mirror push), `submodule propagate` (owned-submodule walk in propagation order), `install-upstreams` (§11.4.74 `FindScript`-resolved constitution-submodule script, git-binary fallback), `fetch-guard` (§11.4.37 pre-edit fetch + rebase enforcement), `reopen` (§11.4.55 Fixed→Issues reversal + `docs/Reopens/` history, reusing the Wave 6.5 inbound migration), `pre-push` (§11.4.71 fetch + investigate + integrate hook) — each `--emit` feeds the HRD-023 pherald binding, all hermetic temp-repo + `file://` fake-remote tested `-race -count=3` green. e2e invariants **E97-E100** wired (commit-push / reopen / install-upstreams / fetch-guard), each anchored to its `docs/qa/HRD-NNN-20260527T213201Z/transcript.txt`. **Prior r17 (v1.0.0 Batch A) atomically migrated HRD-018 + HRD-026 + HRD-027 Issues→Fixed per §11.4.19.** commons_constitution persistence layer completed: the Evaluator framework, 12 emit helpers, bundle-hash captureer, mode-ladder, and `constitution_state`/`constitution_bindings` migrations were already coded in r1 — this effort closed the remaining gaps: (1) `constitution_audit` write-through — the table existed in migration 000006 but the Runner never wrote a row (`RunOutcome.Audited` set true with nothing persisted = §107 PASS-bluff at the audit layer); now `state.MemoryAudit` + `state.PostgresAudit` back a new `AuditStore` interface and `Runner.Run` writes a durable RLS-scoped row for every CHANGED Warn/Enforce transition, capturing the emitted event ID for Enforce; (2) HRD-026 bundle-hash persisted on every emitted event AND audit row for replay (§42.1.3); (3) HRD-027 mode-ladder runtime config wired to 3 admin REST endpoints on cherald (`GET`/`PUT /v1/compliance/modes[/:rule_id]`, JWT-gated, flip allow/warn/enforce per tenant with no redeploy). TDD RED→GREEN, `-race -count=3` deterministic; real-PG integration green at :24100. Evidence `docs/qa/HRD-018-20260527T142556Z/` (real-PG emit→persist + audit round-trip with DB-level `SELECT` proof, admin-REST flip round-trip, §11.4.85 stress 512 emit→persist exactly-512-rows no-lost-writes + chaos 960 concurrent flips, all `-race` clean). Prior r16 atomically migrated the completed GAP-3 §11.4.85 stress+chaos suite (HRD-122..HRD-130, all Task→Completed) + the HRD-132 Runner idempotency-window fix (Bug→Fixed) Issues→Fixed per §11.4.19.** GAP-3 commits `02d272d`→`d2a60d3` (HRD-122 scaffold `commons/stresschaos/`, HRD-123 Gin `/v1/events`, HRD-124 `/v1/compliance`+`/v1/safety_state`, HRD-125 Runner, HRD-126 `pherald listen` inbound, HRD-127 claude_code dispatch, HRD-128 container/resource-exhaustion §12.6-safe, HRD-129 e2e E81-E88, HRD-130 paired §1.1 mutation gate); HRD-132 fix `354b883` (claim-before-dispatch at Runner Stage 2 — `INSERT events_processed … ON CONFLICT DO NOTHING` is now the authoritative exactly-once dispatch gate; latent `runner.go:132` `CachedRcpt.WasReplay` race closed; HRD-125 assertion upgraded to `sends==1`) + wave3-M4-retire `4f45f26`. Full `scripts/e2e_bluff_hunt.sh` reports 78 PASS / 0 FAIL; HRD-130 gate 6 PASS / 0 FAIL (5 load-bearing mutations); HRD-132 evidence `docs/qa/HRD-125-stress-chaos-2026-05-27T131251-gap3/` (`dispatch_exactly_once=1`, `shared_key_sends=1 want=1`). HRD-131 (SQLite SSoT migration Phase 1) remains OPEN — Phases 2-6 follow-on. Prior r15 captured Wave 6 (pherald inbound runtime + Claude Code headless bridge — code-doc closure; tag v0.4.0 deferred to T13b post live evidence). HRD-100 atomic Issues→Fixed in r15.** New Cobra subcommand `pherald listen` runs the closed-loop runtime: Telegram `getUpdates` long-poll (`OnText`/`OnPhoto`/`OnDocument`/`OnVoice`) + bot self-filter (`bot.Me.Username` anti-echo guard, fails Subscribe if `getMe` returned no username) + sha256 content-addressed attachment download into `~/.herald/inbox/<sha256>.<ext>` + Claude Code dispatch with **Opus pinned in argv** (`claude --model claude-opus-4-7 --resume <UUID> --print <envelope>`) + envelope pre-text (verbatim operator wording "We have received new message from our communication channel <channel-name>. ...") preceding the `<<<HERALD-DISPATCH-v1>>>` structured block + `<<<HERALD-REPLY>>>` parser with `action` routing (`reply` / `issue.open` / `event.emit`) + `tgram.Adapter.SendReply` wiring `reply_to_message_id` from the inbound `message_id`. T10a `--qa-out-dir` flag journals every inbound/outbound event as JSONL per §107.x. 12 commits (T1=`ad87d7f`..T12=`96c7c6b`). 8 new e2e invariants E63-E70 added (currently SKIP with documented reasons — converting to PASS at T10b when operator runs the live closed-loop and commits `docs/qa/HRD-100/`). Wave 6 mutation gate `tests/test_wave6_mutation_meta.sh` lands 3 paired hermetic mutations. Tag **v0.4.0 deferred to T13b** (operator-supplied live evidence per §107.x). Prior r14 captured Wave 4b TOON `application/toon` content negotiation (tag v0.3.0). Prior r13 captured Wave 4a (HTTP/3 + Brotli + Alt-Svc + TLS 1.3). r12 closed HRD-016 (Wave 3b pherald Runner live). r11 closed HRD-011 (Telegram live, message_id=5). |
| Issues | HRD-008, HRD-015, HRD-019..HRD-025, HRD-029..HRD-056, HRD-081, HRD-085..HRD-089, HRD-115..HRD-121, HRD-131, HRD-146, HRD-147, HRD-148..HRD-158 |
| Issues summary | 43 open / in-progress workable items. r35 (Tgram-completeness wave COMPLETE) atomically migrated HRD-133/134/135/136/137/138 Issues→Fixed per §11.4.19 — see `Fixed.md`. r34..r17 (v1.0.0 Batch A): HRD-018 (commons_constitution package — Evaluator+emit+migrations+bundle-hash+mode-ladder, completing the `constitution_audit` write-through that was the remaining §107 PASS-bluff: `RunOutcome.Audited` set true with nothing persisted), HRD-026 (bundle-hash now persisted on every emitted event AND audit row for replay), HRD-027 (mode-ladder runtime config + 3 admin REST endpoints `GET`/`PUT /v1/compliance/modes[/:rule_id]`) closed atomically Task→done, migrated Issues→Fixed per §11.4.19. Evidence `docs/qa/HRD-018-20260527T142556Z/` (real-PG emit→persist + admin-REST flip round-trips + §11.4.85 stress 512 emit→persist no-lost-writes + chaos 960 concurrent flips). Prior r16: HRD-122..HRD-130 (GAP-3 §11.4.85 stress+chaos suite) closed atomically as Completed + HRD-132 (Runner idempotency-window bug) closed as Fixed — 11 rows migrated Issues→Fixed; HRD-131 (SQLite SSoT migration Phase 1) remains OPEN (Phases 2-6 follow-on). r15: HRD-100 closed atomically (Wave 6 pherald inbound runtime — code-doc closure; live evidence operator-pending → T10b). r14: substrate upgrade only (Wave 4b TOON; no HRDs migrated). r12: HRD-016 closed atomically (Wave 3b pherald Runner live + e2e E37-E42 PASS or honest SKIP-with-reason when PG :24100 absent). r11: HRD-011 (Telegram live) closed atomically with live message_id=5 evidence. r10 doc-cleanup renumbered HRD-093 → HRD-099. r9: HRD-028 + HRD-098 closed (Wave 3a). Carry-over to Wave 3c: HRD-024 (iherald paging), HRD-033 (destructive-guard body), HRD-018..027 flavor-specific bindings, plus the rest of the §43 command catalogue. |
| Fixed | HRD-001..HRD-007, HRD-009, HRD-009b, HRD-010, HRD-011, HRD-012, HRD-013, HRD-014, HRD-016, HRD-017, HRD-018, HRD-026, HRD-027, HRD-028, HRD-080, HRD-092, HRD-093, HRD-094, HRD-095, HRD-096, HRD-097, HRD-098, HRD-099, HRD-100, HRD-110, HRD-111, HRD-112, HRD-113, HRD-114, HRD-122, HRD-123, HRD-124, HRD-125, HRD-126, HRD-127, HRD-128, HRD-129, HRD-130, HRD-132, HRD-133, HRD-134, HRD-135, HRD-136, HRD-137, HRD-138 |
| Fixed summary | see `Fixed.md`. |
| Continuation | see `CONTINUATION.md`. |

## Table of contents

- [Open](#open)
- [In progress](#in-progress)
- [Blocked](#blocked)
- [Conventions](#conventions)

## Open

Per Universal §11.4.74 every new row carries a `Catalogue-Check` line in its References cell (one of `reuse|extend|no-match <org/repo>@<sha>`). For HRDs whose catalogue-check is still pending, the row records `no-match (2026-05-20)` — operators reviewing the implementation PR will re-verify.

| ID | Type | Status | Criticality | Title | Opened | Last update | References |
|---|---|---|---|---|---|---|---|
| HRD-143 | task | Obsolete | low | `TestSubscribe_LiveBotAPI` legacy live test — superseded by §11.4.98 + HRD-140. **Obsolete-Details:** Since 2026-05-28; Reason=superseded-by-later-mandate; Superseding-item=Universal §11.4.98 (full-automation anti-bluff mandate) + Herald §108.m + HRD-140 (`TestMTProto_Subscribe_AutonomousRoundTrip`, Fixed.md row, commit `367d58b`); Triple-check evidence: (a) git-grep verified the test still exists at `commons_messaging/channels/tgram/subscribe_integration_test.go:30` with the literal source comment "requires the operator to hand-send a message to the configured chat within the 60s window" (direct §11.4.98 violation — runtime human action required during test execution), (b) git-log shows `367d58b` (HRD-140) introduced the MTProto-driven autonomous replacement, (c) runtime path: the new HRD-140 test PASSED LIVE in 1.30s end-to-end with zero human action per `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/E135_subscribe/transcript.txt`. Action: file is retained as a documentation artefact (commented `t.Skipf(§11.4.98 supersession)` body) — operators MUST use HRD-140's MTProto path. | 2026-05-28 | 2026-05-29 | Universal §11.4.98 (inherited per §11.4.35); Herald §108.m / CLAUDE.md §11.4.98 restatement; superseding HRD-140 in Fixed.md; Catalogue-Check: n/a (Obsolete classification, not new code) |
| HRD-144 | task | Obsolete | low | `tests/test_wave6_live_loop.sh` legacy live closed-loop script — superseded by §11.4.98 + HRD-141. **Obsolete-Details:** Since 2026-05-28; Reason=superseded-by-later-mandate; Superseding-item=Universal §11.4.98 + Herald §108.m + HRD-141 (`TestMTProto_Wave6_AutonomousClosedLoop`, Fixed.md row, commits `7e50ed0`+`d7d6524`+`0797e85`); Triple-check evidence: (a) `ls tests/test_wave6_live_loop.sh` confirms file exists (6782 bytes), (b) git-log shows `7e50ed0` (HRD-141) introduced the MTProto-driven autonomous closed-loop replacement with journal-based §11.4.98 autonomy assertion, (c) runtime path: HRD-141 PASSED LIVE in 48.58s with full bidirectional `tgram.message in` + `cc.dispatch` + `cc.reply` journal trace per `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/E136_wave6_closed_loop/transcript*.txt`. Action: shell script retained as historical reference; new e2e flow goes through HRD-141. | 2026-05-28 | 2026-05-29 | Universal §11.4.98 (inherited per §11.4.35); Herald §108.m / CLAUDE.md §11.4.98 restatement; superseding HRD-141 in Fixed.md; Catalogue-Check: n/a (Obsolete classification, not new code) |
| HRD-145 | task | Obsolete | low | `qaherald lifecycle --manual` interactive mode — superseded by §11.4.98 + HRD-142. **Obsolete-Details:** Since 2026-05-28; Reason=superseded-by-later-mandate; Superseding-item=Universal §11.4.98 + Herald §108.m + HRD-142 (`TestMTProto_Wave65_LifecycleAutonomous`, Fixed.md row, commit `1f2e75d`); Triple-check evidence: (a) `grep -nE '"--manual"' qaherald/cmd/qaherald/lifecycle.go:174` confirms `cmd.Flags().BoolVar(&f.Manual, "manual", false, ...)` is wired in the Cobra subcommand, (b) git-log shows `1f2e75d` (HRD-142) introduced the 3-scenario MTProto-driven autonomous replacement (Help/Status/Continue fast-paths, journal-based assertions), (c) runtime path: HRD-142 PASSED LIVE in 26.04s with 3/3 subtests PASS per `docs/qa/HRD-LIVE-MTPROTO-20260528T125321Z/E137_wave65_lifecycle/transcript*.txt`. Action: `--manual` flag retained for ad-hoc operator debugging but explicitly NOT a §11.4.98-compliant test path — CI MUST use the MTProto build-tag path from HRD-142. | 2026-05-28 | 2026-05-29 | Universal §11.4.98 (inherited per §11.4.35); Herald §108.m / CLAUDE.md §11.4.98 restatement; superseding HRD-142 in Fixed.md; Catalogue-Check: n/a (Obsolete classification, not new code) |
| HRD-015 | task | open | low | Inheritance gate I8 invariants for Go scaffold (go.work + commons/types.go + null adapter test passes) | 2026-05-20 | 2026-05-20 | spec V3 §40 + gate I7 pattern; Catalogue-Check: no-match (2026-05-20) |
| HRD-019 | task | in_progress | high | `cherald` constitution bindings — `cherald/internal/bindings` Pipeline registers the 32 cherald-owned §42.3 rules as concrete `commons_constitution.Evaluator` impls routed through 5 event classes (`.policy.violation`/`.gate.failed`/`.gate.recovered`/`.credential.leak`/`.spec.revision_drift`/`.catalogue.miss`) over the Batch-A emit+persist+audit foundation; `POST /v1/compliance/evaluate` write surface drives detect→emit→persist→audit, visible on the existing `GET /v1/compliance` pull surface. Vertical-slice bespoke detectors landed (§11.4.29 naming, §11.4.10/.10.A credentials, §11.4.44 doc-revision-header, §11.4.18 script-docs, §11.4.73 spec-drift, §11.4.74 catalogue-miss); remaining 25 rules use the shared marker-check mechanism (verdict decided upstream by the §43 catalogue command / sweep) with the full emit-routing framework in place — deepening each remaining bespoke detector is per-rule follow-up. | 2026-05-20 | 2026-05-27 | spec V3 §42.3 cherald rows + §42.5 step 2; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A wiring per §11.4.74). Evidence: docs/qa/HRD-019-20260527T200036Z/ (REST round-trip transcript + stress_chaos/). e2e: E90 proposed. |
| HRD-020 | task | in_progress | high | `sherald` host-safety + repo-safety constitution bindings — `sherald/internal/bindings` Pipeline registers the 15 sherald-owned §42.3 host/repo-safety rules (§2, §9.1/.2/.3, §11.4.24/.26/.32/.36/.41/.47/.71, §12.1/.2/.3/.6) as concrete `commons_constitution.Evaluator` impls routed through 6 SAFETY event classes (`.host.safety.breach`/`.repo.safety.breach`/`.gate.recovered`/`.gate.failed`/`.bundle.updated`/`.policy.violation`) over the Batch-A emit+persist+audit foundation, mirroring the proven HRD-019 cherald Pipeline pattern. The 3 named DETECTION hooks landed as REAL pure classifiers — destructive-op detector (§9.1: rm/git-reset/clean without backup → `.repo.safety.breach`), force-push interceptor (§11.4.41/§9.2: force-push without merge-first OR session-auth → `.repo.safety.breach`), mem-budget watcher (§12.6: reported used_fraction > 60% → `.host.safety.breach`) — plus forbidden-host-op (§12.1) + prereq-marker detectors for the remaining rows. CRITICAL §12-safety: every detector DETECTS+REPORTS from a Subject string description; NONE performs the destructive op, force-pushes, suspends the host, or allocates memory (the mem-budget watcher reads a fabricated fraction — no real exhaustion). The §43 command bodies (HRD-033/046/056) supply the live op-interception feeding these detectors. | 2026-05-20 | 2026-05-27 | spec V3 §42.3 sherald rows + §42.5 step 3; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A wiring + reuses the existing `commons_constitution.SafetyEvent`/`HostSafetyBreach`/`RepoSafetyBreach` emitter surface per §11.4.74). Evidence: docs/qa/HRD-020-20260527T201932Z/ (detect→emit→persist→audit round-trip transcript for §9.1/§11.4.41/§12.6/§12.1 + §11.4.85 stress_chaos/). §11.4.92 5-pass — Pass-1: TDD RED (no-impl build-fail) → GREEN, `go test -race -count=3 ./sherald/internal/bindings/...` deterministic 15/15 PASS ×3. Pass-2 blast-radius: new pkg `sherald/internal/bindings` only; sherald/go.mod gains commons_constitution + transitive replace block; existing sherald main/routes/safety untouched (Pipeline not yet wired into serve — REST `/v1/safety/*` surface is follow-up). Pass-3 cross-feature: independent of the existing in-memory safety.Aggregator + GET /v1/safety_state (Wave 3b); no shared state; the §32 Runner pipeline is unaffected (sherald owns a separate Pipeline like cherald). Pass-5 anti-bluff: real MemoryBus+store+ladder+audit round-trip with captured emitted wire-event payloads (NOT metadata-only); §12-safe — TestDetectors_NeverPerformOps + structural witness confirm no real destructive op performed. e2e: E91 proposed (conductor to wire — asserts a sherald §9.1 destructive-op Subject → `.repo.safety.breach` detected+persisted+audited via the bindings Pipeline). NOTE: surfaced a latent race in `commons_constitution/eventbus.go` Publish PublishedByType sync.Map check-then-act (under-counts by 1 under load) — out of this unit's edit scope, flagged for conductor. |
| HRD-021 | task | in_progress | middle | `bherald` CI/test bindings — `bherald/internal/bindings` Pipeline registers the 22 bherald-owned §42.3 CI/test gate-result rules (§1, §1.1, §11.4.2/.3/.4/.5/.7/.9/.13/.14/.24/.27/.30/.39/.43/.46/.48–.52/.67) as concrete `commons_constitution.Evaluator` impls routed through 3 event classes (`.gate.failed`/`.gate.recovered` via the build/test gate-result class + `.policy.violation` + `.repo.safety.breach` for the shared §11.4.30 build-artifact row) over the Batch-A emit+persist+audit foundation, mirroring the proven HRD-019/020 Pipeline pattern. bherald is the BUILD/CI flavor; it emits CI/test gate-result constitution events that integrate with the consuming project's CI workflow. 3 bespoke pure detectors landed: gate-result classification (§1/§11.4.50 — PASS/FAIL/flaky from a recorded gate outcome → `.gate.failed`/`.gate.recovered`), test-tier-verify (§11.4.27/§40.2 — the 8-tier test matrix; a missing tier FAILs), anti-bluff-PASS detection (§11.4.2/.5 — a gate that PASSes WITHOUT a captured-evidence artefact is a §11.4 PASS-bluff → FAIL). Remaining rows use the shared marker-check mechanism (verdict decided upstream by the §43 command/CI gate) with the full emit-routing framework in place. | 2026-05-20 | 2026-05-28 | spec V3 §42.3 bherald rows + §42.5 step 4; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A `commons_constitution` wiring + reuses the existing `GateEvent`/`GateFailed`/`GateRecovered` emitter surface per §11.4.74). v1.0.0 Batch B unit 3. Evidence: docs/qa/HRD-021-<run-id>/ (gate-fail→detect→emit `.gate.failed`→persist→audit→query round-trip transcript + §11.4.85 stress_chaos/). §11.4.92 5-pass — Pass-1: TDD RED→GREEN, `go test -race -count=3 ./bherald/internal/bindings/...` deterministic PASS ×3. Pass-2 blast-radius: new pkg `bherald/internal/bindings` only; bherald/go.mod gains commons_constitution + transitive replace block; existing bherald main/stubs untouched. Pass-3 cross-feature: mirrors HRD-019/020 emit-class routing; no shared state with cherald/sherald (each flavor owns a separate Pipeline). Pass-5 anti-bluff: real MemoryBus+store+ladder+audit round-trip with captured emitted wire-event payloads (NOT metadata-only). e2e: E92 proposed (conductor to wire — asserts a bherald gate-result FAIL Subject → `.gate.failed` detected+persisted+audited via the bindings Pipeline). |
| HRD-022 | task | in_progress | middle | `rherald` release constitution bindings — `rherald/internal/bindings` Pipeline registers the 4 rherald-owned §42.3 release-lifecycle rules (§4 tag mirroring, §5 changelog + multi-format export [shared with cherald], §11.4.38 installable-asset evidence, §11.4.40 full-suite retest before release tag) as concrete `commons_constitution.Evaluator` impls routed through 2 event classes (`.release.gate.blocked` via the existing `ReleaseEvent`/`ReleaseGateBlocked` emitter surface for §4/§11.4.38/§11.4.40 + `.policy.violation` for the shared §5 changelog row; a release recovery maps to the shared `.gate.recovered` companion since the release class family has no distinct "recovered" leaf) over the Batch-A emit+persist+audit foundation, mirroring the proven HRD-019/020/021 Pipeline pattern. rherald is the RELEASE flavor. 4 bespoke PURE detectors landed: tag-mirror-parity (§4 — tag present on parent but missing on any owned mirror, `with_tag<mirrors`, → `.release.gate.blocked`), changelog-conformance (§5 — non-Conventional-Commits changelog OR stale multi-format export → `.policy.violation`), pre-tag-retest-gate (§11.4.40 CRITICAL — tag attempted without a green all-8-§40.2-tier retest → `.release.gate.blocked`), installable-asset-evidence (§11.4.38 — release asset fails its install check → `.release.gate.blocked`). CRITICAL §12-safety: every detector CLASSIFIES a recorded release-op outcome string; NONE tags, pushes, force-pushes, runs git, downloads, or installs — the live release-op interception is scope-locked to the §43 follow-ups (HRD-031/032/045). | 2026-05-20 | 2026-05-28 | spec V3 §42.3 rherald rows + §42.5 step 5; Catalogue-Check: no-match (bindings are bespoke Helix-governance Evaluator impls; emit/persist/audit path is 100% Batch-A `commons_constitution` wiring + reuses the existing `ReleaseEvent`/`ReleaseGateBlocked` emitter surface per §11.4.74). v1.0.0 Batch B unit 4. Evidence: docs/qa/HRD-022-20260528T015202Z/ (release-gate-blocked detect→emit `.release.gate.blocked`→persist→audit→query round-trip transcript for §4/§5/§11.4.38/§11.4.40 + §11.4.85 stress_chaos/ — 1200 concurrent release-blocks 0 errors, 1200 emits + 1200 audit rows, bus-drop chaos 800/800 faults surface 0 swallowed). §11.4.92 5-pass — Pass-1: TDD RED (injected always-pass bluff into §11.4.40 retest detector → 4 sub-cases FAIL) → reverted → GREEN, `go test -race -count=3 ./rherald/internal/bindings/...` deterministic 14/14 PASS ×3. Pass-2 blast-radius: new pkg `rherald/internal/bindings` only; rherald/go.mod gains commons_constitution + google/uuid + transitive replace block (database submodule); existing rherald main/stubs untouched (Pipeline not yet wired into a serve plane — rherald is CLI-only, DefaultPort=0; the §43 `rherald tag mirror`/`changelog generate`/`gate retest` command bodies are HRD-031/032/045 follow-ups). Pass-3 cross-feature: mirrors HRD-019/020/021 emit-class routing; no shared state with cherald/sherald/bherald (each flavor owns a separate Pipeline); the shared §5 row is registered independently in both rherald (conformance/export-staleness facet) and cherald packages. Pass-5 anti-bluff: real MemoryBus+store+ladder+audit round-trip with captured emitted wire-event payloads (NOT metadata-only); §12-safe — no real tag/push/git/install performed. e2e: E93 proposed (conductor to wire — asserts a rherald §11.4.40 tag-without-retest Subject → `.release.gate.blocked` detected+persisted+audited via the bindings Pipeline). |
| HRD-023 | task | in_progress | middle | `pherald` project bindings (§2 commit+push, §3 submodule propagation, §11.4.11/.15/.21/.22/.34/.37/.42/.55/.66/.71/.74) — Batch B: `pherald/internal/bindings/` Pipeline + §42.3-owned rule set + 5 bespoke PURE detectors (commit-push-discipline / submodule-propagation-order / pre-push-fetch-guard / fetch-before-edit) wired through the commons_constitution Evaluator+audit foundation; -race -count=3 green, e2e E94. Live op-interception deferred to §43 HRD-029/030/043/044/049/053. | 2026-05-20 | 2026-05-28 | spec V3 §42.3 pherald rows + §42.5 step 6; evidence `docs/qa/HRD-023-20260527T210354Z/`; Catalogue-Check: reuse commons_constitution (Batch-A) |
| HRD-024 | task | in_progress | middle | `iherald` constitution-rule escalation bindings (§11.4.10/.10.A credential-leak page-out, §11.4.21 + §11.4.66 operator-blocked escalation) — Batch B: `iherald/internal/bindings/` Pipeline routing `.credential.leak` (forced-critical) + `.policy.violation` + 5 bespoke detectors wired through commons_constitution Evaluator+audit; -race -count=3 green, e2e E95, no-real-secret attestation. Live paging deferred to §43 / HRD-024-paging. | 2026-05-20 | 2026-05-28 | spec V3 §42.3 iherald rows + §42.5 step 7; evidence `docs/qa/HRD-024-20260527T210714Z/`; Catalogue-Check: reuse commons_constitution (Batch-A) |
| HRD-025 | task | in_progress | low | `scherald` scheduled-audit bindings (§11.4.45 periodic Status.md sweep + daily/weekly/monthly compliance digest) — Batch B: `scherald/internal/bindings/` Pipeline routing `.policy.violation` + 3 bespoke detectors (status-sweep / digest-cadence / stale-item) wired through commons_constitution Evaluator+audit; -race -count=3 green, e2e E96. Live cron/scheduler deferred to §43 HRD-047. | 2026-05-20 | 2026-05-28 | spec V3 §42.3 scherald rows + §42.5 step 8; evidence `docs/qa/HRD-025-20260528-hrd025-batchb-u7/`; Catalogue-Check: reuse commons_constitution (Batch-A) |
| HRD-029 | task | in_progress | middle | §2 `pherald commit-push` — single-entrypoint locked commit + multi-mirror push — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §2; Catalogue-Check: TBD (likely existing as constitution-submodule script); evidence `docs/qa/HRD-029-20260527T213201Z/` |
| HRD-030 | task | in_progress | middle | §3 `pherald submodule propagate` — owned-submodule walk in propagation order — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §3; Catalogue-Check: TBD; evidence `docs/qa/HRD-030-20260527T213201Z/` |
| HRD-031 | task | in_progress | middle | §4 `rherald tag mirror` — assert tag exists on every owned submodule — Batch C: implemented as rherald §43 release command (`rherald/cmd/rherald/release_cmds.go`) feeding the HRD-022 rherald binding; reuses shared `commons/gitops`; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §4; Catalogue-Check: TBD; docs/qa/HRD-031-20260528T040000Z/ |
| HRD-032 | task | in_progress | low | §5 `rherald changelog generate` — Conventional Commits → `docs/changelogs/<v>.md` + multi-format export — Batch C: implemented as rherald §43 release command (`rherald/cmd/rherald/release_cmds.go`) feeding the HRD-022 rherald binding; reuses shared `commons/gitops`; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §5 / §36; Catalogue-Check: TBD; docs/qa/HRD-032-20260528T040000Z/ |
| HRD-033 | task | in_progress | high | §9.1 `sherald destructive guard <op>` — wrap rm/git-reset/git-push-force with prerequisite checks — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-20 | 2026-05-28 | spec §43.2 / §9.1; Catalogue-Check: TBD; docs/qa/HRD-033-20260528T030000Z/ |
| HRD-034 | task | in_progress | middle | §9.3 `sherald backup-snapshot <target>` — hardlinked snapshot helper — Batch C straggler: implemented in `sherald/cmd/sherald/sysops_cmds.go` (os.Link hardlinks, refuses overwrite, classifies §9.3 SubjectBackup → `.gate.recovered`); hermetic inode-equality -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §9.3; Catalogue-Check: TBD; evidence `docs/qa/HRD-034-20260528T060000Z/` |
| HRD-035 | task | in_progress | middle | §11.4.2 `bherald evidence capture <test_id>` — captured-evidence recorder — Batch C: implemented as bherald §43 build/CI command (`bherald/cmd/bherald/build_cmds.go`) feeding the HRD-021 bherald binding; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.2 + §11.4.5; Catalogue-Check: TBD; docs/qa/HRD-035-20260528T040000Z/ |
| HRD-036 | task | in_progress | high | §11.4.10 `cherald creds scan` — gitleaks/trufflehog integration, emits `.credential.leak` — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green (routed to §11.4.10 credential-leak; findings REDACTED — no secret value printed) | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.10; Catalogue-Check: TBD — likely an existing OSS scanner; docs/qa/HRD-036-20260528T050000Z/ |
| HRD-037 | task | in_progress | low | §11.4.12 `cherald docs sync` — regen Issues_Summary / Fixed_Summary / Status_Summary — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.12; Catalogue-Check: TBD (likely existing constitution-submodule script); docs/qa/HRD-037-20260528T050000Z/ |
| HRD-038 | task | in_progress | low | §11.4.18 `cherald script-docs check` — assert sibling .md for every scripts/**/*.sh — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green (routed to §11.4.60 — §11.4.62 has no cherald rule) | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.18; Catalogue-Check: TBD; docs/qa/HRD-038-20260528T050000Z/ |
| HRD-039 | task | in_progress | low | §11.4.19 / .23 `cherald fixed align` + `cherald colorize` — Issues/Fixed format + HTML colorizer — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.19 + §11.4.23; Catalogue-Check: TBD; docs/qa/HRD-039-20260528T050000Z/ |
| HRD-040 | task | in_progress | high | §11.4.26 `sherald constitution pull` — wrap fetch + rebase + validation gate, emits `.bundle.updated` — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.26 + §11.4.32; Catalogue-Check: TBD; docs/qa/HRD-040-20260528T030000Z/ |
| HRD-041 | task | in_progress | middle | §11.4.27 `bherald test-tier verify` — 8-tier matrix verification — Batch C: implemented as bherald §43 build/CI command (`bherald/cmd/bherald/build_cmds.go`) feeding the HRD-021 bherald binding; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.27 + §40.2; Catalogue-Check: TBD; docs/qa/HRD-041-20260528T040000Z/ |
| HRD-042 | task | in_progress | low | §11.4.31 `cherald submanifest verify` — Submodule-Dependency-Manifest gate — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.31; Catalogue-Check: TBD; docs/qa/HRD-042-20260528T050000Z/ |
| HRD-043 | task | in_progress | high | §11.4.36 `pherald install-upstreams` — install_upstreams wrapper — Batch C: implemented as gitops command (§11.4.74 FindScript-resolved constitution script); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.36; Catalogue-Check: extend constitution submodule's install_upstreams.sh; evidence `docs/qa/HRD-043-20260527T213201Z/` |
| HRD-044 | task | in_progress | middle | §11.4.37 `pherald fetch-guard` — pre-edit fetch + rebase enforcement — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.37; Catalogue-Check: TBD; evidence `docs/qa/HRD-044-20260527T213201Z/` |
| HRD-045 | task | in_progress | high | §11.4.40 `rherald gate retest` — pre-tag full-suite retest gate — Batch C: implemented as rherald §43 release command (`rherald/cmd/rherald/release_cmds.go`) feeding the HRD-022 rherald binding; reuses shared `commons/gitops`; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.40; Catalogue-Check: TBD; docs/qa/HRD-045-20260528T040000Z/ |
| HRD-046 | task | in_progress | high | §11.4.41 `sherald force-push gate` — merge-first + per-session-auth enforcement — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.41 + §9.2; Catalogue-Check: TBD; docs/qa/HRD-046-20260528T030000Z/ |
| HRD-047 | task | in_progress | middle | §11.4.45 / .56 `scherald status-digest` — periodic Status.md sweep + Status_Summary regen — Batch C straggler: implemented in `scherald/cmd/scherald/digest_cmds.go` feeding the HRD-025 scherald binding (§11.4.45 status-sweep; detect/report + `--apply` regenerates Status_Summary); hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.45 + §11.4.56; Catalogue-Check: TBD; evidence `docs/qa/HRD-047-20260528T060000Z/` |
| HRD-048 | task | in_progress | low | §11.4.53 `cherald fixed-summary sync` — standalone Fixed_Summary backfill — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.53; Catalogue-Check: TBD (likely existing script); docs/qa/HRD-048-20260528T050000Z/ |
| HRD-049 | task | in_progress | middle | §11.4.55 `pherald reopen <HRD-NNN>` — Issues→Fixed reversal + Reopens history — Batch C: implemented as gitops command (the Wave 6.5 inbound Fixed→Issues migration); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.55; Catalogue-Check: no-match (Herald-specific HRD flow); evidence `docs/qa/HRD-049-20260527T213201Z/` |
| HRD-050 | task | in_progress | middle | §11.4.59 `cherald readme sync` — README doc-link regen + multi-format re-export — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.57 + §11.4.59; Catalogue-Check: TBD; docs/qa/HRD-050-20260528T050000Z/ |
| HRD-051 | task | in_progress | high | §11.4.60 `cherald composite-gate` — canonical implementation of CM-DOCS-COMPOSITE-SYNC — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.60; Catalogue-Check: TBD; docs/qa/HRD-051-20260528T050000Z/ |
| HRD-052 | task | in_progress | middle | §11.4.65 `cherald export` — bulk Markdown export wrapper (md/html/pdf/docx) — Batch C: implemented as cherald §43 docs-pipeline command (`cherald/cmd/cherald/docops_cmds.go`) feeding the HRD-019 cherald binding; detect-by-default + `--apply` to regenerate; ships the §107 lazy-verifier fix so the CLI runs without a JWT secret; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.65 + §36; Catalogue-Check: extend (built atop Pandoc + WeasyPrint); docs/qa/HRD-052-20260528T050000Z/ |
| HRD-053 | task | in_progress | high | §11.4.71 `pherald pre-push` — fetch + investigate + integrate hook — Batch C: implemented as gitops command (§11.4.74 wrap of git binary); --emit feeds HRD-023 binding; hermetic temp-repo tests -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.71; Catalogue-Check: TBD; evidence `docs/qa/HRD-053-20260527T213201Z/` |
| HRD-054 | task | in_progress | middle | §11.4.73 `cherald spec-version check` — Revision-vs-edits drift detection — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.73; Catalogue-Check: no-match (very-new mandate); docs/qa/HRD-054-20260528T050000Z/ |
| HRD-055 | task | in_progress | middle | §11.4.74 `cherald catalogue-check <pr>` — scan PR for Catalogue-Check + survey runner over vasic-digital + HelixDevelopment — Batch C: implemented as cherald §43 verify/check command (`cherald/cmd/cherald/checkops_cmds.go`) feeding the HRD-019 cherald binding; detect-only; hermetic -race green | 2026-05-20 | 2026-05-28 | spec §43.2 / §11.4.74; Catalogue-Check: no-match (very-new mandate); docs/qa/HRD-055-20260528T050000Z/ |
| HRD-056 | task | in_progress | high | §12.6 `sherald mem-budget watch` — daemon-mode 60% threshold watcher emitting `.host.safety_breach` — Batch C: implemented as sherald §43 command (`sherald/cmd/sherald/sysops_cmds.go`) feeding the HRD-020 sherald binding; PRE-FLIGHT GATE (detects+classifies+exits-nonzero, never performs the op); hermetic -race green; §107 lazy-verifier fix so the CLI runs without a JWT secret | 2026-05-20 | 2026-05-28 | spec §43.2 / §12.6; Catalogue-Check: TBD; docs/qa/HRD-056-20260528T030000Z/ |
| HRD-081 | task | open | low | Extend `digital.vasic.containers/pkg/compose` to detect podman-compose vs docker compose runtime and either emit runtime-appropriate flags (no `--wait` for podman-compose) or fall back to host-side healthcheck polling. Composes with §11.4.76 — required so `compose.WithWait(true)` works across both backends. Also fix `Status()` parsing for podman-compose ps output (returns 0 services even when containers are visible to podman directly). Per §11.4.76: extend upstream submodule, never reimplement. | 2026-05-20 | 2026-05-20 | spec V3 §44 + Universal §11.4.76; Catalogue-Check: extend `vasic-digital/containers` (workaround applied in `commons_infra/boot.go` — dropped `WithWait`, TCP-probe used for healthcheck). |
| HRD-085 | task | in_progress | middle | Implement single-task admin operations on `commons_infra.pgxTaskRepository` — `GetByID`, `Update`, `Delete`. Required for Requeue + MoveToDeadLetter call chains and operator tooling. Currently all three return `ErrUnsupported (HRD-085)`. Scope: read one BackgroundTask row by ID; full-row Update including JSONB columns; soft- or hard-Delete decision per §107. | 2026-05-20 | 2026-05-20 | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (Herald already implements Enqueue/Dequeue/LogEvent on the same interface; these three rows finish the single-task surface). |
| HRD-086 | task | in_progress | middle | Implement worker-side running-task state mutations on `commons_infra.pgxTaskRepository` — `UpdateStatus`, `UpdateProgress`, `UpdateHeartbeat`, `SaveCheckpoint`. Required by River workers + the stuck-detector reaper. Currently all four return `ErrUnsupported (HRD-086)`. Scope: status transitions guarded by valid-state-machine check; progress as `(float64, message)`; heartbeat as `updated_at = now()` writeback; checkpoint as opaque `[]byte` blob persisted alongside the task row. | 2026-05-20 | 2026-05-20 | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (worker-side write surface — the hot path for long-running task progress reporting). |
| HRD-087 | task | in_progress | low | Implement admin / stats / audit reads on `commons_infra.pgxTaskRepository` — `GetByStatus`, `GetPendingTasks`, `CountByStatus`, `GetTaskHistory`. Drives the `/v1/queue/*` introspection endpoints + Peek delegate. Currently all four return `ErrUnsupported (HRD-087)`. Scope: status-filtered paginated reads; pending-task slice for Peek; status-bucketed counts; per-task execution-history rows from the `task_execution_history` table. | 2026-05-20 | 2026-05-20 | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (read-only introspection — composes with HRD-016 `/v1/queue/*` Gin handlers). |
| HRD-088 | task | in_progress | middle | Implement queue-recovery reads on `commons_infra.pgxTaskRepository` — `GetStaleTasks`, `GetByWorkerID`. Drives the stuck-task reaper + worker-crash recovery loop. Currently both return `ErrUnsupported (HRD-088)`. Scope: stale-threshold filter against `updated_at`; per-worker assignment lookup for re-dispatch after a worker disappears. | 2026-05-20 | 2026-05-20 | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (recovery-loop surface — required before any worker is allowed to crash without operator intervention). |
| HRD-089 | task | in_progress | low | Implement resource-monitor I/O on `commons_infra.pgxTaskRepository` — `SaveResourceSnapshot`, `GetResourceSnapshots`. Persists per-task CPU/memory/IO samples for capacity-planning + post-mortem. Currently both return `ErrUnsupported (HRD-089)`. Scope: append-only insert keyed by `(task_id, captured_at)`; per-task paginated read in reverse-chronological order. Schema target: a `task_resource_snapshots` table not yet in the §9.6 migration set — adding it is part of this HRD. | 2026-05-20 | 2026-05-20 | spec V3 §44 + upstream interface `TaskRepository` in `submodules/background/interfaces.go`; Catalogue-Check: extend `digital.vasic.background.TaskRepository@2d46dd60b2ffcb9d3b584b029b711a6fbc71b296` (resource telemetry — composes with `digital.vasic.observability` OTel pipeline but is durable, not stream-only). |
| HRD-147 | task | open | low | Governance-plane subscriber for `.queue.dead_letter` — wire a durable PRODUCTION consumer that drains the HRD-090 dead-letter events into the `constitution_audit` sink (or equivalent durable store) so dead-letter moves are auditable beyond the in-process emit. HRD-090 emits the event + proves fan-out to a REAL bus subscriber in its integration test; this follow-up adds the production subscriber + persistence and wires the emitter into the production `pgxTaskRepository` construction (boot.go currently uses the nil-emitter — the Foundation plane has no governance bus; this HRD decides where the bus + subscriber live: the cherald binding plane vs a dedicated consumer). | 2026-05-29 | 2026-05-29 | follow-up from HRD-090 (now in `Fixed.md`); spec V3 §42.1 + §44; source `commons_infra/boot.go` + `commons_infra/task_repository.go` (`newPgxTaskRepositoryWithEmitter`) + `commons_constitution` audit sink (HRD-018); Catalogue-Check: reuse `commons_constitution` EventBus + `constitution_audit` (2026-05-29). |
| HRD-115 | task | in_progress | high | Wave 7 T6 — Slack channel adapter (Socket Mode): `commons_messaging/channels/slack/` implements `channels.Channel` — `New`/`NewWithBaseURL`/`NewFromURL` parsing `slack://<bot-token>@<workspace>/<channel-id>[?app_token=…]`, `Send` (chat.postMessage), `SendReply`(thread_ts) + `SendReplyGeneric` (interface delegate w/ files.uploadV2 attachment fan-out), `Subscribe` (Socket Mode), `BotSelfIdentity` (auth.test → IdentityUserID, cached), `DownloadAttachment` (files.info → content-addressed `inbox/slack/<sha>.<ext>` via channels.WriteContentAddressed), `HealthCheck`. Vendored `submodules/slack-go` @ v0.16.0 (commit `209ec09`) + `commons_messaging/go.mod` replace per Herald §11.4.74. 22 hermetic httptest tests (33 incl. subtests) `-race` green; inheritance gate 15/0 post-.gitmodules-edit; `TestSlackSatisfiesChannel` confirms interface satisfaction. | 2026-05-28 | 2026-05-28 | docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T6; spec V3 §11 Slack + §32.2; evidence `docs/qa/HRD-115-20260528T080000Z/`; Catalogue-Check: reuse `slack-go/slack@209ec09` (vendored submodule v0.16.0, 2026-05-28). |
| HRD-116 | task | in_progress | middle | Wave 7 T7 — qaherald Slack `MessengerClient` + channel-keyed builder (`qaherald/internal/messenger/{slack.go,builder.go}`): raw-HTTP client implementing all 11 `MessengerClient` methods (Me/Send/SendPhoto/SendDocument/SendVoice/GetUpdates/WaitForReply/Download/Preflight/GetChatMember/Close); `GetUpdates` via `conversations.history`+`oldest=<ts>` watermark (Slack has no `getUpdates`); `MessageID` = dotted-float ts string; channel-keyed `Build(BuildConfig)` (tgram/slack/unknown→error). Token NEVER in errors — `sanitizeError()` defense + `TestSlack_Send_ErrorDoesNotLeakToken` pins it with a planted `xoxb-SUPERSECRET-must-not-leak-7777`. 30 hermetic httptest tests `-race` green; compile-time pin `var _ MessengerClient = (*SlackClient)(nil)`. KNOWN-FUTURE-CLEANUP: ships `files.upload` V1 (deprecated by Slack 2025-Q1 in favor of two-step V2) — functionally equivalent for the httptest seam, swappable without API breakage; documented in `slack.go` header. | 2026-05-28 | 2026-05-28 | docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T7; mirrors `qaherald/internal/messenger/telegram.go`; evidence `docs/qa/HRD-116-20260528T090000Z/`; Catalogue-Check: reuse Slack Web API endpoints via raw HTTP (NOT slack-go SDK — different layer from HRD-115; offset bookkeeping is qaherald-controlled; 2026-05-28). |
| HRD-117 | task | in_progress | low | Wave 7 T8 — spec V3 r12→r13: §11.0 `channels.Channel` inbound-runtime extension note + §32.2 Telegram/Slack marked LIVE + §43 multi-channel `pherald listen` row. Documents the Wave 7 framework + Slack adapter (T6) + qaherald Slack client (T7) shipped reality — spec catches up to shipped code (no new product code in T8). | 2026-05-28 | 2026-05-28 | spec V3 r13 §11.0/§32.2/§43; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T8; Catalogue-Check: n/a (doc-only). |
| HRD-118 | task | in_progress | high | Wave 7 T9 — e2e_bluff_hunt.sh E124-E131 multi-channel invariants: E124 channels registry resolution, E125 tgram self-filter post-Wave-7 (pure-refactor invariant), E126 slack adapter (interface satisfied + auth.test + chat.postMessage wire bytes + registry wiring), **E127 LIVE Slack** (env-gated `HERALD_SLACK_BOT_TOKEN`+`HERALD_SLACK_CHANNEL_ID`+`TestSlack_Live_Send` triple-guard to prevent silent "no tests to run" PASS-bluff), E128 per-channel inbox isolation, E129 generalized IsSelfEcho (Username+UserID+empty-self), E130 multi-channel pherald listen wiring, E131 qaherald messenger.Build dispatch. sc_anchor on the T6+T7 committed transcripts. | 2026-05-28 | 2026-05-28 | spec V3 r13 §11.4; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T9; Catalogue-Check: n/a (e2e wiring against shipped tests). |
| HRD-119 | task | in_progress | high | Wave 7 T10 — paired §1.1 mutation gate `tests/test_wave7_mutation_meta.sh` (4 PASS / 0 FAIL): **M1** blank `IsSelfEcho` → `TestIsSelfEchoUsername` FAILs (echo-loop hazard caught) → restored → PASS; **M2** drop slack `channels.Register` in `init()` → new `TestSlackRegisteredViaInit` FAILs → restored → PASS; **M3** drop slack `thread_ts` append in `send.go` → `TestSlackSendReplyUsesThreadTS` FAILs → restored → PASS; **+ quiescence** assertion (no `MUTATED W7-` residue in 3 production target files post-restore). Reuses Wave 6 canonical `.git/MUTATION_IN_PROGRESS` lockfile + trap-on-EXIT + pre-flight dirty-tree-and-residue guard + byte-for-byte `assert_restored`. FOREGROUND-ONLY (§107.y / no-concurrent-mutation operator mandate). | 2026-05-28 | 2026-05-28 | spec V3 §11.4 + §1.1 + §107.y; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T10; canonical precedent `tests/test_wave6_mutation_meta.sh`; evidence `docs/qa/HRD-119-20260528T100000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-119-WAVE7-MUTATION-GATE-20260528`); Catalogue-Check: n/a (gate). |
| HRD-120 | task | in_progress | high | Wave 7 T11 — §11.4.85 multi-channel stress + chaos. Extended HRD-126's `pherald/cmd/pherald/listen_stress_chaos_test.go` (NOT duplicated to a new file): `TestListen_MultiChannel_StressBurst` (2 channels × 200 concurrent events through ONE runListen — pins no-loss/no-double + **per-channel attribution** via `perChannelCountingReplier` keyed on `rcpt.Channel` proving the channelRouter does NOT cross-wire; throughput ~9-19k ev/s under -race; clean shutdown <3s + no goroutine leak) + `TestListen_MultiChannel_ChaosOneChannelDies_OtherKeepsGoing` (tgram fires 1 event then errors; slack ticks 50 events at 2ms — pins **concurrency-of-fan-in under fault** via conjunction: ≥5 slack dispatches landed BEFORE fail-loud teardown AND tgram error surfaces tagged `channel="tgram-stub"` + `subscribe`). 12/12 PASS over `-race -count=3` (3 iterations × 2 new + 2 sibling HRD-126); race-detector clean. runListen contract found: **fail-loud-tears-down-siblings** (per-channel supervised restart is a deferred Wave 8 design, doc-comment warns against silent change). Evidence anchors: `multichannel_no_drop_under_burst=1` (stress) + `multichannel_concurrency_proven=1` + `multichannel_one_channel_death_surfaced=1` (chaos). | 2026-05-28 | 2026-05-28 | spec V3 §11.4.85; docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T11; canonical precedent HRD-126 + commons/stresschaos; evidence `docs/qa/HRD-120-20260528T110000Z/stress_chaos/transcript.txt` (anchor `QA-ANCHOR: HRD-120-WAVE7-MULTICHANNEL-STRESS-CHAOS-20260528`); Catalogue-Check: extend commons/stresschaos + HRD-126 burstSubscriber. |
| HRD-121 | task | in_progress | low | Wave 7 T12 — `docs/guides/MESSENGER_CHANNELS.md` operator guide (610 lines, 11 §-sections + 3 siblings .html/.pdf/.docx): §1 architecture (commons.Channel outbound vs commons_messaging/channels.Channel inbound + registry + IsSelfEcho + per-channel inbox; ASCII fan-in diagram), §2 `HERALD_CHANNELS` selector (fail-loud on unknown name), §3 Telegram live setup (BotFather → token → chat-id), §4 Slack live setup (api.slack.com/apps → Socket Mode + scopes + `xoxb-`/`xapp-` tokens; §107 token-redaction sanitization note citing `TestSlack_Send_ErrorDoesNotLeakToken`), §5 inbox isolation under `${HERALD_INBOX_DIR}/<channel>/<sha256>.<ext>`, §6 anti-echo-loop self-filter (cross-the-wire-then-cache contract), §7 URL schemes for both adapters, §8 14-item "add a new channel" implementer checklist + companion "what you do NOT touch" list (framework hot paths come for free), §9 10-entry troubleshooting cookbook (echo-loop / channel_not_found / invalid_auth / wrong inbox / HERALD_CHANNELS=unknown), §10 6-checklist operator pre-deploy audit mirroring OPERATOR_CREDENTIALS.md style, §11 references (cross-links spec V3 §11/§32.2/§43 + Wave 7 plan + adapter source paths + every HRD-110..121). Generated via `REGEN_ALL=1 bash scripts/export_docs.sh` (new-guide bootstrap). | 2026-05-28 | 2026-05-28 | docs/superpowers/plans/2026-05-27-wave7-generic-messenger.md §T12; evidence `docs/qa/HRD-121-20260528T120000Z/transcript.txt` (anchor `QA-ANCHOR: HRD-121-WAVE7-T12-MESSENGER-CHANNELS-DOC-20260528`); Catalogue-Check: n/a (operator doc). |
| HRD-131 | task | open | middle | Migrate text HRD trackers to versioned SQLite single-source-of-truth (§11.4.93 / Herald §108.h, 6-phase) — DB committed as authoritative SSoT per operator mandate 2026-05-27. Phase 1 (this item): file the scope-locked tracking HRD. Phase 2: scaffold/adopt the migration Go binary that already lives in the constitution submodule (`constitution/scripts/workable-items/`) — Herald references it per §11.4.74 catalogue-first and never reimplements it. Phase 3: sync md→db (load `docs/Issues.md` / `docs/Fixed.md` / `*_Summary.md` / `docs/Status.md` / `docs/CONTINUATION.md` §3 into `docs/.workable_items.db`). Phase 4: sync db→md byte-identical (round-trip regeneration so MD↔DB drift is mechanically impossible). Phase 5: generator shims (the `*_Summary.md` variants + Status/CONTINUATION become derived from the DB). Phase 6: prohibit text-direct edits (release-gate guard that the DB is the only authoring surface). Operator divergence from the parent §11.4.93 gitignored-with-regeneration default: for Herald the DB IS the authoritative artefact — `docs/.workable_items.db` is version-controlled (committed), NOT regenerated-on-clone; only the transient WAL/SHM sidecars (`docs/.workable_items.db-wal` / `-shm`) stay gitignored ("We should not git ignore our workable items database since it is our single source of truth regarding items we have to do on the project!"). **Scope-lock: this HRD covers Phase 1 (this row) + Phase 2 adoption ONLY.** Phases 3-6 (md→db sync, db→md byte-identical regeneration, generator shims, text-direct-edit prohibition) are follow-on work-items to be opened as Phase 2 completes, so the §106 spec-change / §107.x evidence invariants are never broken mid-migration. **2026-05-29 re-assessment:** Phase 2 remains EXTERNALLY BLOCKED — `constitution/scripts/workable-items/` is still a non-functional scaffold (0/7 subcommands implemented, `go build` succeeds but every command exits 2 "not yet implemented"; full assessment `docs/research/workable-items-phase2-assessment-2026-05-27.md`). Herald cannot proceed beyond Phase 1 without reimplementing the binary, which §11.4.74 forbids. The `docs/.workable_items.db` → canonical `docs/workable_items.db` path reconciliation is DEFERRED BY DESIGN per CLAUDE.md §11.4.95 ("reconcile to the canonical path when the DB is implemented") — reconciling now would churn 4 governance docs (inheritance-gate risk) for a file that does not yet exist and could create a new dot/no-dot split. | 2026-05-27 | 2026-05-29 | Universal §11.4.93 + Herald §108.h `docs/guides/HERALD_CONSTITUTION.md`; migration binary `constitution/scripts/workable-items/` (§11.4.74 reference, never reimplement); operator mandate 2026-05-27 (version-controlled DB); Catalogue-Check: reuse `constitution/scripts/workable-items/` (constitution-submodule tool, 2026-05-27). |
| HRD-146 | task | open | middle | `pherald listen` dispatch hardening — bound per-message resource use in the inbound→`claude_code.Dispatch` path. Latent unbounded-growth risk surfaced by the 2026-05-29 "worker OOM" Phase-1 investigation (`docs/research/oom-investigation-2026-05-29.md`): `dispatch.go` uses `cmd.Output()` (full child-stdout buffering in memory) with NO per-message timeout beyond the Subscribe-lifetime ctx; `subscribe.go` runs handlers on telebot dispatch goroutines. Under a hung/slow `claude` child + sustained inbound load, goroutines + stdout buffers could accumulate for the process lifetime. **Hypothesis-grade (NOT a confirmed defect)** — untested under real hung-child load (in-repo stress tests use stub dispatchers). Scope: add a per-message `context.WithTimeout` around dispatch + cap/stream child stdout (`cmd.Output()`→bounded reader), then a §11.4.85 stress/chaos test driving a deliberately-slow fake `claude` to prove bounded memory + goroutine count. NOTE: the originating "hourly OOM + telemetry-drop + 5-path triage + heap profile" premise did NOT match committed reality (no hourly worker, no OTel telemetry, no heap profile in-repo). | 2026-05-29 | 2026-05-29 | `docs/research/oom-investigation-2026-05-29.md`; source `commons_messaging/dispatch/claude_code/dispatch.go` + `commons_messaging/channels/tgram/subscribe.go`; Catalogue-Check: no-match (Herald-specific dispatch path, 2026-05-29). |
| HRD-148 | feature | in_progress | high | **ATMOSphere integration WS-1 — `commons_workable` SQLite SSoT foundation (LANDED + tested 2026-05-29).** New L1 module: SQLite store (pure-Go `modernc.org/sqlite`, no CGO) mirroring the workable-items schema v2 (`items` PK `(atm_id,current_location)` + `item_history` + `meta`, WAL, 10-value status closed set); full CRUD repo (Create/GetByID/Update/Delete/List, loud not-found, closed-set status validation); per-property `Diff(prev,curr)` change-feed emitting `item.created`/`item.deleted`/`item.status.changed`/`item.field.changed`/`item.content.updated` with exact old→new; `ParseTracker` matching ATMOSphere's REAL `## §… [ATM-NNN]` + `**Status:**`/`**Type:**`/`**Severity:**` format. 18 real-SQLite tests `-race -count=3` green. Keystone for R1/R3/R6. | 2026-05-29 | 2026-05-29 | Plan `~/Documents/ATMOSphere_Herald_Integration_Plan.md` §4 (HRD-W1/W2); Catalogue-Check: reuse `modernc.org/sqlite`, schema mirrors `constitution/scripts/workable-items/schema.sql` (2026-05-29). |
| HRD-149 | feature | in_progress | high | **ATMOSphere integration WS-2 — `commons_watch` file/DB watcher (LANDED + tested 2026-05-29).** New L1 module: fsnotify watcher (direct dep) over the `.db` SSoT + MD trackers, debounce-coalesce + SQLite-WAL polling fallback (WAL writes hit the `-wal` sidecar + may not raise reliable fsnotify Write on the main `.db`); graceful ctx-cancel, leak-free. 4 real-fsnotify tests `-race` green. Feeds R1/R2. | 2026-05-29 | 2026-05-29 | Plan §4 (HRD-W3); Catalogue-Check: reuse `github.com/fsnotify/fsnotify` (2026-05-29). |
| HRD-150 | task | open | high | ATMOSphere integration WS-1 — bidirectional MD↔SQLite regenerator + drift resolution (reference `constitution/scripts/workable-items/` per §11.4.74, supply the ATMOSphere-format parser the tool lacks). | 2026-05-29 | 2026-05-29 | Plan §4 (HRD-W4); Catalogue-Check: extend constitution workable-items tool. |
| HRD-151 | feature | open | high | ATMOSphere integration WS-3 — change→CloudEvent→Runner bridge + Jira/ClickUp-style per-property-diff message formatter (reuse the existing `pherald/internal/runner` 7-stage fan-out; add only the workable-event producer + formatter). | 2026-05-29 | 2026-05-29 | Plan §3.1 + §4 (HRD-W5); Catalogue-Check: reuse `pherald/internal/runner`. |
| HRD-152 | feature | open | high | ATMOSphere integration WS-4 — inbound action registry + `item.update`/`item.delete`/`investigation.start` + `ItemMutator` (writes `commons_workable`) + investigation orchestrator (**act-with-confirmation** per operator 2026-05-29). | 2026-05-29 | 2026-05-29 | Plan §3.2 + §4 (HRD-W6); Catalogue-Check: extend `pherald/internal/inbound` action router. |
| HRD-153 | feature | open | middle | ATMOSphere integration WS-2 — watcher daemon entrypoint (`pherald watch` running the watch→diff→notify loop; promote to a `wherald` flavor if it grows). | 2026-05-29 | 2026-05-29 | Plan §3.3 + §4 (HRD-W7); Catalogue-Check: extend `pherald` CLI. |
| HRD-154 | task | open | middle | ATMOSphere integration WS-3 — enforce preference/quiet-hours routing (`PreferenceSet`/`QuietHours` types exist but the resolver ignores them). | 2026-05-29 | 2026-05-29 | Plan §4 (HRD-W10); Catalogue-Check: extend `pherald/internal/runner/subscriber.go`. |
| HRD-155 | task | open | high | ATMOSphere integration WS-1 — operationalize the workable-items SQLite tool + reconcile HRD-131 (implement the tool's `add`/`close`/`report` + ATMOSphere-format parser upstream in the constitution repo per §11.4.74; external blocker for SSoT materialization). | 2026-05-29 | 2026-05-29 | Plan §4 (HRD-W11) + §10; composes with HRD-131; Catalogue-Check: extend constitution workable-items tool. |
| HRD-156 | task | open | high | ATMOSphere integration WS-5 — anti-bluff full-automation test suite (T-A real-SQLite→real-Telegram via MTProto; T-B inbound→real-row-mutation; T-C exact-diff byte-assert; T-D/T-E §11.4.85 stress/chaos; T-F paired §1.1 mutation gate; wrap as Challenges for HelixQA; all `docs/qa/<run-id>/`). | 2026-05-29 | 2026-05-29 | Plan §6 Part A; Catalogue-Check: reuse Herald e2e + §11.4.85 + MTProto harness + ATMOSphere HelixQA/Challenges. |
| HRD-157 | task | open | high | ATMOSphere integration Phase 3 — register Herald as a proper `tools/herald` submodule (replace the broken orphan gitlink, operator-chosen 2026-05-29), materialize `docs/workable_items.db`, wire host-side runner (`pherald watch` + `pherald listen`, mirroring `tools/oom-protect`), seed Subscribers, run live T-A..T-F evidence. | 2026-05-29 | 2026-05-29 | Plan §5; Catalogue-Check: mirror `tools/oom-protect` host-tool pattern. |
| HRD-158 | task | open | middle | ATMOSphere integration WS-5 — anti-bluff covenant verbatim-phrase propagation: add the operator phrase to root `constitution/QWEN.md` (HelixConstitution) → cascade to ATMOSphere root `QWEN.md` + all 25 submodules' `QWEN.md` (6 helixqa confirmed-missing; audit the other ~18) + Herald README. | 2026-05-29 | 2026-05-29 | Plan §6 Part B; §11.4.26 fetch-first governance workflow; Catalogue-Check: n/a. |

## In progress

| ID | Type | Status | Criticality | Title | Opened | Last update | References |
|---|---|---|---|---|---|---|---|
| HRD-008 | task | in_progress | middle | Operator-side quickstart compose validation (Postgres + Redis + OTel + pherald container) | 2026-05-20 | 2026-05-20 | spec V3 §26.5 — scaffold shipped, live end-to-end run pending operator; Catalogue-Check: no-match (2026-05-20) — bespoke to Herald |

## Blocked

(none)

## Conventions

See [`Fixed.md`](Fixed.md) for closed items + Universal §11.4.12/.15/.16/.19/.33/.55/.73/.74 composition rules.
